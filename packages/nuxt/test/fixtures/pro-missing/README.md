# pro-missing

`includePro: true` with `@dzup-ui-pro/pro` **not installed** - the state every
consumer is in today, because Pro is not published.

The build must succeed with Core registered, and must emit the diagnostic that
names the package, the option, and the install command. A cryptic Vite
resolution error is the failure this fixture exists to prevent.
