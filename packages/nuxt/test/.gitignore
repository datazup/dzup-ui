# Generated by packages/nuxt/scripts/pack-fixtures.mjs. Real packed artifacts and
# the package.json files rendered from each fixture's package.template.json --
# both carry absolute paths for this machine, so neither is committed.
.tarballs/
fixtures/*/package.json
fixtures/*/node_modules/
fixtures/*/.nuxt/
fixtures/*/.output/
