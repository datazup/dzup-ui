---
title: "DzTimePicker"
description: "dropdown time picker (CoreUI-style) built on Reka UI Popover."
outline: [2, 3]
---

<!-- GENERATED by `yarn generate:docs-pages` from packages/core/docs/component-meta.json.
     Every API fact on this page is a projection of that artifact (constraint B9); edits are
     overwritten on the next run. Hand-written prose belongs in components/_usage/<Name>.md. -->

<script setup>
import DzPlayground from '../.vitepress/theme/components/DzPlayground.vue'
</script>

# DzTimePicker

dropdown time picker (CoreUI-style) built on Reka UI Popover.

- **Family:** Forms
- **Source:** `packages/core/src/components/forms/DzTimePicker.vue` · types `packages/core/src/components/forms/DzTimePicker.types.ts`
- **Install:** `npm i @dzup-ui/core` — then `import { DzTimePicker } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/forms`
- **Risk tier:** C · **Status:** stable
- **Taxonomy:** variant: `outline` `filled` `underlined` · size: `icon` `xs` `sm` `md` `lg` `xl`
- **v-model:** `v-model` (`string | undefined`)
- **Anatomy parts (ADR-19):** `action`, `clear`, `content`, `control`, `error`, `footer`, `group`, `icon`, `input`, `item`, `label`, `list`, `panel`, `root`, `separator`, `trigger`

## Intent and selection guidance

**Not declared.** `DzTimePicker` declares no `@intent` block in its source header, so
nothing here says what it is for or when to reach for something else. That is a gap in the
component, not in this page: the guidance is authored in the SFC header and extracted by
`yarn generate:component-meta`, never typed into the site.

::: info How to read the tables on this page
Everything below is extracted from source by `vue-component-meta` and published as measured,
never as asserted.

- **Declared default** is what the component *declares*, not what you necessarily get. Many props
  declare `undefined` on purpose so the surrounding `DzProvider` supplies the effective value at
  runtime (ADR-20). `—` means no default is declared at all; those are different facts and are not
  collapsed here.
- **An empty description cell (`—`) means no prose exists in source**, not that the member is
  self-explanatory. The per-page counts under *Extraction fidelity* say how many are in that state.
- **Event descriptions are recovered from the `Dz*Emits` interface**, because Vue's `ShortEmits`
  mapped type erases the JSDoc before any extractor can see it. Events written by `defineModel`
  (ADR-16) have no authored description anywhere and say so.
- **`defineExpose` members are published as names and types only.** No description exists in
  source for any of them, catalog-wide, so the column is omitted rather than shown empty.
- **Usage snippets are verbatim slices of the component's Storybook story.** Nothing on this site
  synthesises example markup; a component without a story says so.
:::


## Props (31, of which 11 inherited from `@dzup-ui/contracts`)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `ariaDescribedby` | `string \| undefined` | no | `undefined` | ID of element that describes this component |
| `ariaInvalid` | `boolean \| "grammar" \| "spelling" \| undefined` | no | `undefined` | Indicates the component has invalid input |
| `ariaLabel` | `string \| undefined` | no | `undefined` | Accessible label |
| `ariaLabelledby` | `string \| undefined` | no | `undefined` | ID of element that labels this component |
| `cancelText` | `string \| undefined` | no | `undefined` | Cancel button label |
| `cleaner` | `boolean \| undefined` | no | `true` | Show the clear ("cleaner") button when a value is selected |
| `confirmText` | `string \| undefined` | no | `undefined` | Confirm button label |
| `disabled` | `boolean \| undefined` | no | `false` | Disabled state — prevents interaction |
| `error` | `string \| undefined` | no | `undefined` | Error message to display |
| `footer` | `boolean \| undefined` | no | `true` | Show the Cancel / Confirm footer (staged selection). When false, picks apply immediately |
| `hour12` | `boolean \| undefined` | no | `undefined` | Use 12-hour format with AM/PM (default: follows locale) |
| `id` | `string \| undefined` | no | `undefined` | Unique element ID (prefer `useId()` from Vue 3.5 when auto-generated) |
| `indicator` | `boolean \| undefined` | no | `true` | Show the clock indicator icon on the trigger |
| `invalid` | `boolean \| undefined` | no | `false` | Whether the field value is invalid |
| `locale` | `string \| undefined` | no | `undefined` | Locale for time display formatting (BCP 47 tag) |
| `max` | `string \| undefined` | no | `undefined` | Maximum selectable time (`HH:mm` or `HH:mm:ss`) |
| `min` | `string \| undefined` | no | `undefined` | Minimum selectable time (`HH:mm` or `HH:mm:ss`) |
| `modelValue` | `string \| undefined` | no | `""` | The selected time as a canonical 24-hour `HH:mm` or `HH:mm:ss` string; the default empty string selects no time. |
| `name` | `string \| undefined` | no | `undefined` | Form field name (submits the canonical 24h value via a hidden input) |
| `placeholder` | `string \| undefined` | no | `"Select time"` | Placeholder text shown on the trigger when no time is selected |
| `portalDefer` | `boolean \| undefined` | no | `false` | Defer target resolution until the application has mounted. |
| `portalDisabled` | `boolean \| undefined` | no | `false` | Render inline instead of teleporting to the portal target. |
| `portalTo` | `string \| HTMLElement \| undefined` | no | `undefined` | Portal target. Defaults to `document.body` when omitted. |
| `required` | `boolean \| undefined` | no | `false` | Whether the field is required |
| `seconds` | `boolean \| undefined` | no | `false` | Show a seconds column — value becomes `HH:mm:ss` |
| `secondStep` | `number \| undefined` | no | `1` | Second step interval (only relevant when `seconds` is enabled) |
| `selection` | `TimePickerSelection \| undefined` | no | `"roll"` | Popover selection layout: scrollable wheel (`roll`) or native dropdowns (`select`) |
| `size` | `CanonicalSize \| undefined` | no | `"md"` | Component size |
| `step` | `number \| undefined` | no | `1` | Minute step interval (e.g. 15 → :00, :15, :30, :45) |
| `ui` | `Partial<Record<"icon" \| "root" \| "item" \| "trigger" \| "content" \| "action" \| "clear" \| "list" \| "error" \| "footer" \| "separator" \| "group" \| "control" \| "label" \| "panel" \| "input", DzClassValue>> \| undefined` | no | — | Per-part class overrides, keyed by the names in `DzTimePicker.anatomy.ts` (ADR-19 §5). `ui.list` names the region of choices in **both** the roll and the select layout, so a theme does not have to branch on `selection`. |
| `variant` | `InputVariant \| undefined` | no | `"outline"` | Visual style variant of the trigger |

## Events (8)

| Event | Payload | Description |
| --- | --- | --- |
| `blur` | `[event: FocusEvent]` | Focus lost |
| `change` | `[value: string, metadata?: ChangeMetadata \| undefined]` | Value committed (after user finishes editing, not during typing) |
| `clear` | `[]` | Value was cleared/reset |
| `close` | `[]` | Popup/overlay closed |
| `focus` | `[event: FocusEvent]` | Focus gained |
| `open` | `[]` | Popup/overlay opened |
| `select` | `[value: string]` | An item was selected from a list/collection |
| `update:modelValue` | `[value: string]` | Emitted when the `v-model` binding changes, with the new value. Synthesised by `defineModel` (ADR-16); `v-model` consumes it for you. |

## Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `trigger` | `{ value: string; display: string; }` | Override the trigger value display. Receives the formatted display string. |

## Usage (from `packages/core/stories/forms/DzTimePicker.stories.ts`, story `Default`)

```vue
<DzTimePicker v-bind="args" class="max-w-xs" />
```

## Playground

Editable, running the **Roll vs Select Layout** story from `packages/core/stories/forms/DzTimePicker.stories.ts` (lines 184–201) **verbatim**. Edits run in a sandbox; nothing here is saved.

<DzPlayground component="DzTimePicker" />

## Variants and controlled state

**Recipe axes.** Each axis is mirrored onto the root as `data-{axis}` carrying the
**resolved** value — after group and provider inheritance, not the raw prop — which is what
makes `[data-size="lg"]` a selector you can rely on.

| Axis | Root attribute | Prop |
| --- | --- | --- |
| `size` | `[data-size="…"]` | `size` |
| `variant` | `[data-variant="…"]` | `variant` |

**Controlled and uncontrolled — `modelValue`.** Both forms are supported, and they are
different contracts rather than two spellings of one.

```vue
<!-- Uncontrolled: the component owns the value. -->
<DzTimePicker />

<!-- Controlled: you own it, and the component only ever asks. -->
<DzTimePicker v-model="value" />

<!-- Controlled, long form — the same thing, written out. -->
<DzTimePicker :modelValue="value" @update:modelValue="value = $event" />
```

**Where each variant is shown.** 16 stories in
`packages/core/stories/forms/DzTimePicker.stories.ts`: `Default`, `Roll vs Select Layout`, `Variant Gallery`, `Size Gallery`, `12-Hour Format (AM/PM)`, `With Seconds`, `Business Hours Only`, `Immediate (no footer)`, `No Indicator / No Cleaner`, `Disabled`, `Invalid State`, `Dark Mode Preview`, `Interactive`, `Real World: Meeting Scheduler`, `States`, `Accessibility: Keyboard-Only Panel`.

## Parts, states and tokens

**Parts** — addressable nodes, emitted as `data-part`. Reach one with the selector, or pass
classes by name through the typed `ui` prop; a typo in `ui` is a type error rather than a class
that lands nowhere.

When your `class` and a `ui` entry set the same Tailwind utility, **your `class` wins**: the
merge order is recipe → `ui` → `class`, and `cn()` is tailwind-merge, so the last one through
takes effect. That is what lets you restyle a wrapper someone else built without `!important`.

| Part | Selector | Always present |
| --- | --- | --- |
| `action` | `[data-part="action"]` | no — renders zero or more than once |
| `clear` | `[data-part="clear"]` | no — renders zero or more than once |
| `content` | `[data-part="content"]` | no — renders zero or more than once |
| `control` | `[data-part="control"]` | yes |
| `error` | `[data-part="error"]` | no — renders zero or more than once |
| `footer` | `[data-part="footer"]` | no — renders zero or more than once |
| `group` | `[data-part="group"]` | no — renders zero or more than once |
| `icon` | `[data-part="icon"]` | no — renders zero or more than once |
| `input` | `[data-part="input"]` | no — renders zero or more than once |
| `item` | `[data-part="item"]` | no — renders zero or more than once |
| `label` | `[data-part="label"]` | no — renders zero or more than once |
| `list` | `[data-part="list"]` | no — renders zero or more than once |
| `panel` | `[data-part="panel"]` | no — renders zero or more than once |
| `root` | `[data-part="root"]` | yes |
| `separator` | `[data-part="separator"]` | no — renders zero or more than once |
| `trigger` | `[data-part="trigger"]` | yes |

```vue
<DzTimePicker :ui="{ 'action': 'ring-2', 'clear': 'ring-2', 'content': 'ring-2', 'control': 'ring-2', 'error': 'ring-2', 'footer': 'ring-2', 'group': 'ring-2', 'icon': 'ring-2', 'input': 'ring-2', 'item': 'ring-2', 'label': 'ring-2', 'list': 'ring-2', 'panel': 'ring-2', 'root': 'ring-2', 'separator': 'ring-2', 'trigger': 'ring-2' }" />
```

**States** — the values `data-state` may take, plus the presence-only boolean attributes.

| State | Selector |
| --- | --- |
| `closed` | `[data-state="closed"]` |
| `disabled` | `[data-state="disabled"]` |
| `invalid` | `[data-state="invalid"]` |
| `open` | `[data-state="open"]` |
| `required` | `[data-state="required"]` |
| `selected` | `[data-state="selected"]` |

**Component tokens** — the custom properties this component reads, and therefore every one you
may set. The list is the complete supported override surface; any other `--dz-*` it inherits is
not a promise.

This component declares no component tokens of its own: it is styled entirely from the global
semantic layer, which the theme owns.

Declared in `packages/core/src/components/forms/DzTimePicker.anatomy.ts`.

## Provider defaults and context

This component reads the following contexts from the surrounding `DzProvider` (ADR-20). The
precedence is fixed and not per-component: **prop, then any group context, then the provider,
then the component's own default.**

| Reader | What the provider supplies through it |
| --- | --- |
| `useDzFormats` | number, date and currency formatting |
| `useDzMessages` | the translated string catalogue |
| `useDzPortalTarget` | where teleported content is mounted |
| `useDzTestIds` | the test-id attribute name and prefix |

## Locale, direction and formats

| Axis | Declared | What it means |
| --- | --- | --- |
| `mirrors` | `layout` | Margins, padding, borders and insets are logical, so the box flips with the document. |
| `keyboard` | `none` | The arrow keys do not swap: they move on the block axis, or map to a direction the user can see. |
| `icons` | `icon` | These parts render a direction-bearing icon and mirror with the layout. |

**Locale and formats.** Reads `useDzFormats`, `useDzMessages` from the surrounding `DzProvider`, so its strings and formatted values follow the application locale.

**Measured:** `rtl-contract` is `present` — `packages/core/src/components/forms/DzTimePicker.anatomy.ts`.

## Server rendering, portals, performance and security

| Concern | State |
| --- | --- |
| **Server rendering** | `present` — `packages/core/tests/ssr/form-controls-ssr.spec.ts`. |
| **Portal / teleport** | `present` — `packages/core/tests/ssr/form-controls-ssr.spec.ts`. |
| **Performance baseline** | `stale` — `packages/core/perf/baselines.json`. 1/1 metric(s) have a derived threshold |
| **Security boundary** | `none` — no host-supplied HTML, file, URL or payload reaches a sink. |

**Peer packages.** Which external packages this component can reach is a property of the built
artifact, not of its source, and is measured by `yarn report:peer-surface` over `dist/` — a
report with no baseline and no ratchet, deliberately outside `validate:all`, because the
numbers depend on decisions the owner has not taken. It is not summarised here rather than
summarised wrongly.

## States and migration

`DzTimePicker` advertises 6 states:
`closed`, `disabled`, `invalid`, `open`, `required`, `selected`. Each is emitted as `data-state` or as a
presence-only boolean attribute, so it is selectable in CSS and assertable in a test.

**Published examples:** `state-stories` is `pass` — `packages/core/stories/forms/DzTimePicker.stories.ts`.

**Migration.** Breaking changes to this component are recorded in the repository's changesets
and published in the release notes; nothing is restated here, because a hand-typed migration
note drifts from the release it describes the first time the release changes. This component
last changed at `2d51eec`.

## Extraction fidelity

Published rather than assumed. These are this component's own numbers, measured by the
extraction that produced the tables above.

| Member kind | Extracted | With a description | Notes |
| --- | --- | --- | --- |
| Props | 31 | 31 | 16 declare a default, of which 14 declare `undefined` (ADR-20 provider supplies the value) |
| Events | 8 | 8 | 7 recovered from the `Dz*Emits` interface · 1 synthesised by `defineModel` |
| Slots | 1 | 1 | 1 carry slot props |
| Exposed on `ref` | 0 | 0 | nothing is exposed on the template ref |

## Accessibility and evidence

::: warning How far this evidence goes
Every state on this page is read from a generated artifact and bound to the commit that
artifact records — `527dbd15` for the capability matrix,
`527dbd15` for the quality matrix. It is **locally qualified**:
produced by a local run on one machine, against a worktree carrying uncommitted work. It is **not** continuous-integration evidence, **not** release evidence and **not**
production evidence, and it must not be read as a conformance claim.
:::

- **Risk tier:** `C` — adds `a11y-narrative`, `real-world-story`, `perf-baseline` on top of the tiers below it.
- **APG pattern:** [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/)
- **Traits:** `teleports`
- **Security boundary:** `none`
- **Declared anatomy:** `declared`
- **Component last changed at:** `2d51eec4`

**Why this pattern:** A text field over a teleported list of times, where the list is generated from a step and a locale rather than supplied.

### WCAG 2.2 criteria in scope (21)

The success criteria **this component can fail**, derived from its tier and its traits. This is
a scope list, not a conformance claim: a criterion appearing here means it applies, not that it
has been verified. What has been verified, and by which lane, is the evidence table below.

| SC | Criterion | Level | New in |
| --- | --- | --- | --- |
| [1.1.1](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html) | Non-text Content | A | WCAG 2.0 |
| [1.3.1](https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships.html) | Info and Relationships | A | WCAG 2.0 |
| [1.3.5](https://www.w3.org/WAI/WCAG22/Understanding/identify-input-purpose.html) | Identify Input Purpose | AA | WCAG 2.1 |
| [1.4.1](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html) | Use of Color | A | WCAG 2.0 |
| [1.4.3](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) | Contrast (Minimum) | AA | WCAG 2.0 |
| [1.4.4](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html) | Resize Text | AA | WCAG 2.0 |
| [1.4.10](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html) | Reflow | AA | WCAG 2.1 |
| [1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) | Non-text Contrast | AA | WCAG 2.1 |
| [1.4.12](https://www.w3.org/WAI/WCAG22/Understanding/text-spacing.html) | Text Spacing | AA | WCAG 2.1 |
| [2.1.1](https://www.w3.org/WAI/WCAG22/Understanding/keyboard.html) | Keyboard | A | WCAG 2.0 |
| [2.1.2](https://www.w3.org/WAI/WCAG22/Understanding/no-keyboard-trap.html) | No Keyboard Trap | A | WCAG 2.0 |
| [2.4.3](https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html) | Focus Order | A | WCAG 2.0 |
| [2.4.7](https://www.w3.org/WAI/WCAG22/Understanding/focus-visible.html) | Focus Visible | AA | WCAG 2.0 |
| [2.4.11](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) | Focus Not Obscured (Minimum) | AA | WCAG 2.2 |
| [2.5.3](https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html) | Label in Name | A | WCAG 2.1 |
| [2.5.8](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) | Target Size (Minimum) | AA | WCAG 2.2 |
| [3.2.1](https://www.w3.org/WAI/WCAG22/Understanding/on-focus.html) | On Focus | A | WCAG 2.0 |
| [3.2.2](https://www.w3.org/WAI/WCAG22/Understanding/on-input.html) | On Input | A | WCAG 2.0 |
| [3.3.1](https://www.w3.org/WAI/WCAG22/Understanding/error-identification.html) | Error Identification | A | WCAG 2.0 |
| [3.3.2](https://www.w3.org/WAI/WCAG22/Understanding/labels-or-instructions.html) | Labels or Instructions | A | WCAG 2.0 |
| [4.1.2](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html) | Name, Role, Value | A | WCAG 2.0 |

### Keyboard interaction

**7 declared bindings.** Rendered from the
component's own keyboard contract, not from the APG pattern it is held to — where the two
differ, the difference is the point.

| Key | Where | Action | WCAG | Pattern |
| --- | --- | --- | --- | --- |
| `ArrowDown` | — | Open the time list when closed, otherwise move to the next option. | `2.1.1` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `ArrowUp` | — | Open the time list when closed, otherwise move to the previous option. | `2.1.1` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `Home` | `list open` | Move to the first option. | `2.1.1` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `End` | `list open` | Move to the last option. | `2.1.1` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `Enter` | — | Select the highlighted option and close the time list. | `2.1.1` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `Escape` | — | Close the time list without changing the value. | `2.1.1`, `2.1.2` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |
| `Tab` | — | Move out of the control, closing the time list. | `2.1.2` | [`combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) |

Declared in `packages/core/src/components/forms/DzTimePicker.anatomy.ts`.

- **Pattern:** [APG — `combobox`](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) · its *Keyboard Interaction* section is
  the contract this component is audited against.
- **Measured:** `keyboard-spec` is **unrun** — The component declares 7 binding(s); the unit spec asserts no key event for `ArrowDown`, `ArrowUp`, `Home`, `End`, `Enter`, `Escape`, `Tab`. The contract is the yardstick, not the presence of any key at all.

### Assistive technology

**0 of 6 AT/browser pairs executed.** Nothing below has been run: every row is `unrun`, which means the pairing was not available and not attempted — a different fact from a failure.

Tasks this component owes on each pair: `reach`, `open`, `navigate`, `typeahead`, `select`, `dismiss`, `error`.

| Pair | AT | Browser | Platform | Result | Tester | Date | AT/browser versions | Recorded at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `nvda-firefox` | NVDA | Firefox | Windows | `unrun` | — | — | — | — |
| `nvda-chrome` | NVDA | Chrome | Windows | `unrun` | — | — | — | — |
| `jaws-chrome` | JAWS | Chrome | Windows | `unrun` | — | — | — | — |
| `voiceover-safari` | VoiceOver | Safari | macOS | `unrun` | — | — | — | — |
| `voiceover-ios` | VoiceOver | Safari | iOS | `unrun` | — | — | — | — |
| `talkback-android` | TalkBack | Chrome | Android | `unrun` | — | — | — | — |

Script: `e2e/at-matrix/scripts/DzTimePicker.at-script.md` · record: `e2e/at-matrix/DzTimePicker.md` (append-only).

A row here is written by a human after a real screen-reader session, never by a generator.
See [the AT matrix](/evidence/at-matrix) for the state of all pairs across the catalogue.

### Evidence cells (17)

Every kind of evidence required of this component — by Tier C, by its traits (`teleports`) — and what was found. The states are `pass` (a lane ran and passed), `present` (an artifact exists and is bound to the component), `stale` (it exists but predates the component's last change), `excepted` (the requirement was waived with a recorded reason) and `unrun` (nothing has measured it).

| Evidence | Required by | State | Where |
| --- | --- | --- | --- |
| `contract-spec` | tier A | `present` | `packages/core/src/components/forms/DzTimePicker.contract.spec.ts` |
| `unit-spec` | tier A | `present` | `packages/core/src/components/forms/DzTimePicker.spec.ts` |
| `axe` | tier A | **`unrun`** | — |
| `story-light-dark` | tier A | `pass` | `packages/core/stories/forms/DzTimePicker.stories.ts` |
| `ssr-sample` | tier A | `present` | `packages/core/tests/ssr/form-controls-ssr.spec.ts` |
| `token-contrast` | tier A · corpus-wide | `pass` | `packages/tooling/src/token-checks/intent-text-contrast.ts` — Corpus gate: `yarn validate:tokens` covers every pair in the catalog at once. |
| `keyboard-spec` | tier B | **`unrun`** | `packages/core/src/components/forms/DzTimePicker.spec.ts` — The component declares 7 binding(s); the unit spec asserts no key event for `ArrowDown`, `ArrowUp`, `Home`, `End`, `Enter`, `Escape`, `Tab`. The contract is the yardstick, not the presence of any key at all. |
| `state-stories` | tier B | `pass` | `packages/core/stories/forms/DzTimePicker.stories.ts` |
| `controlled-uncontrolled` | tier B | **`unrun`** | The unit spec does not exercise both a controlled and an uncontrolled value path. |
| `browser-play` | tier B | `pass` | `packages/core/stories/forms/DzTimePicker.stories.ts` |
| `rtl-contract` | tier B | `present` | `packages/core/src/components/forms/DzTimePicker.anatomy.ts` · `packages/core/docs/rtl-matrix.md` |
| `browser-matrix` | tier B | `pass` | `e2e/matrix/conditions.spec.ts` · `e2e/matrix/browser-evidence.json` · `e2e/matrix/known-failures.json` · `e2e/matrix/engine-ratchets.json` — 24/24 projects measured green at 2d51eec (worktree dirty). chromium 149.0.7827.55 (playwright chromium v1228): all 8 conditions, no expected failure in what it ran. firefox 151.0 (playwright firefox v1532): all 8 conditions, no expected failure in what it ran. webkit 26.5 (playwright webkit v2311): all 8 conditions, no expected failure in what it ran |
| `portal-hydration` | trait teleports | `present` | `packages/core/tests/ssr/form-controls-ssr.spec.ts` |
| `a11y-narrative` | tier C | `pass` | `packages/core/stories/forms/DzTimePicker.stories.ts` |
| `real-world-story` | tier C | `pass` | `packages/core/stories/forms/DzTimePicker.stories.ts` |
| `at-manual` | tier B | **`unrun`** | `e2e/at-matrix/DzTimePicker.md` — 6 AT/browser pairs, none executed. |
| `perf-baseline` | tier C | `stale` | `packages/core/perf/baselines.json` — 1/1 metric(s) have a derived threshold |

**4 unrun:** `axe`, `keyboard-spec`, `controlled-uncontrolled`, `at-manual` · **1 stale:** `perf-baseline`. They are named rather than counted, because a total tells a reader nothing about what is missing.

The `at-manual` row above is the matrix's **summary** of the screen-reader lane. This page does
not rely on it: the *Assistive technology* section is rendered from the append-only run records
themselves, and the generator refuses to build this page if the summary claims more than those
records support. [Why](/evidence/accessibility#why-this-site-reads-the-raw-scaffold-and-not-the-summary-cell).
