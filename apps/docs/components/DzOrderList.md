---
title: "DzOrderList"
description: "reorderable list with drag, control-button, and keyboard reorder."
outline: [2, 3]
---

<!-- GENERATED by `yarn generate:docs-pages` from packages/core/docs/component-meta.json.
     Every API fact on this page is a projection of that artifact (constraint B9); edits are
     overwritten on the next run. Hand-written prose belongs in components/_usage/<Name>.md. -->

<script setup>
import DzPlayground from '../.vitepress/theme/components/DzPlayground.vue'
</script>

# DzOrderList

reorderable list with drag, control-button, and keyboard reorder.

- **Family:** Data display
- **Source:** `packages/core/src/components/data/DzOrderList.vue` · types `packages/core/src/components/data/DzOrderList.types.ts`
- **Install:** `npm i @dzup-ui/core` — then `import { DzOrderList } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/data`
- **Risk tier:** C · **Status:** experimental
- **Taxonomy:** size: `icon` `xs` `sm` `md` `lg` `xl`
- **v-model:** `v-model:value` (`T[] | undefined`)
- **Anatomy parts (ADR-19):** `control`, `empty`, `group`, `item`, `item-label`, `list`, `root`

## Intent and selection guidance

**Not declared.** `DzOrderList` declares no `@intent` block in its source header, so
nothing here says what it is for or when to reach for something else. That is a gap in the
component, not in this page: the guidance is authored in the SFC header and extracted by
`yarn generate:component-meta`, never typed into the site.

::: info How to read the tables on this page
Everything below is extracted from source by `vue-component-meta` and published as measured,
never as asserted.

- **Declared default** is what the component *declares*, not what you necessarily get. Many props
  declare `undefined` on purpose so the surrounding `DzProvider` supplies the effective value at
  runtime (ADR-20). `—` means no default is declared at all; those are different facts and are not
  collapsed here.
- **An empty description cell (`—`) means no prose exists in source**, not that the member is
  self-explanatory. The per-page counts under *Extraction fidelity* say how many are in that state.
- **Event descriptions are recovered from the `Dz*Emits` interface**, because Vue's `ShortEmits`
  mapped type erases the JSDoc before any extractor can see it. Events written by `defineModel`
  (ADR-16) have no authored description anywhere and say so.
- **`defineExpose` members are published as names and types only.** No description exists in
  source for any of them, catalog-wide, so the column is omitted rather than shown empty.
- **Usage snippets are verbatim slices of the component's Storybook story.** Nothing on this site
  synthesises example markup; a component without a story says so.
:::


## Props (19, of which 4 inherited from `@dzup-ui/contracts`)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `ariaDescribedby` | `string \| undefined` | no | `undefined` | ID of element that describes this component |
| `ariaLabel` | `string \| undefined` | no | `undefined` | Accessible label |
| `ariaLabelledby` | `string \| undefined` | no | `undefined` | ID of element that labels this component |
| `controlsPosition` | `OrderListControlsPosition \| undefined` | no | `"start"` | Which side the control button column sits on |
| `dataKey` | `string \| undefined` | no | `undefined` | Property name used to derive a stable key from each item (e.g. `'id'`). Falls back to the item's index when omitted — note that index-based keys do not survive reordering, so provide `dataKey` when using `selectable`. |
| `disabled` | `boolean \| undefined` | no | `false` | Disabled state — prevents all reordering and selection |
| `dragHandle` | `boolean \| undefined` | no | `true` | Show a drag handle and enable pointer drag-and-drop reordering |
| `dragHandleLabel` | `string \| undefined` | no | `undefined` | Label for each row's drag handle, rendered as the handle's `title`. The handle is a **pointer-only** affordance and stays `aria-hidden="true"`: its function is already reachable from the keyboard through the Move controls and the row's own space-to-grab, and giving it an accessible name would fold that name into every row's accessible name under `selectable` (where each row is `role="option"`, a name-from-content role). So this is a tooltip for the audience the handle actually serves, not an ARIA label — which is what the prop had always claimed to be and never was. |
| `id` | `string \| undefined` | no | `undefined` | Unique element ID (prefer `useId()` from Vue 3.5 when auto-generated) |
| `moveBottomLabel` | `string \| undefined` | no | `undefined` | Accessible label for the Move To Bottom control |
| `moveDownLabel` | `string \| undefined` | no | `undefined` | Accessible label for the Move Down control |
| `moveTopLabel` | `string \| undefined` | no | `undefined` | Accessible label for the Move To Top control |
| `moveUpLabel` | `string \| undefined` | no | `undefined` | Accessible label for the Move Up control |
| `selectable` | `boolean \| undefined` | no | `false` | Allow multi-selection so a group of rows can be moved together |
| `showControls` | `boolean \| undefined` | no | `true` | Render the Move Up / Down / Top / Bottom control buttons |
| `size` | `CanonicalSize \| undefined` | no | `"md"` | Component size |
| `ui` | `Partial<Record<"root" \| "item" \| "list" \| "item-label" \| "group" \| "empty" \| "control", DzClassValue>> \| undefined` | no | — | Per-part class overrides, keyed by the names in `DzOrderList.anatomy.ts` (ADR-19 §5). `class` keeps its existing target on the wrapper; the toolbar, the list, the rows, the drag handle and the placeholder are all rendered by this component. |
| `value` | `T[] \| undefined` | no | `[]` | modelValue = the ordered array (never mutated in place) |
| `variant` | `OrderListVariant \| undefined` | no | `"bordered"` | Visual style variant |

## Events (5)

| Event | Payload | Description |
| --- | --- | --- |
| `blur` | `[event: FocusEvent]` | Focus left the list |
| `focus` | `[event: FocusEvent]` | Focus entered the list |
| `reorder` | `[payload: OrderListReorderPayload]` | A reorder completed (drag, control button, or keyboard drop) |
| `selectionChange` | `[keys: OrderListKey[]]` | The selected key set changed (only fired when `selectable`) |
| `update:value` | `[value: T[]]` | Emitted when the `v-model:value` binding changes, with the new value. Synthesised by `defineModel` (ADR-16); `v-model:value` consumes it for you. |

## Slots (3)

| Slot | Slot props | Description |
| --- | --- | --- |
| `empty` | — | Content shown when the list is empty |
| `header` | — | Heading content rendered above the list |
| `item` | `DzOrderListItemSlotProps<T>` | Per-row content (defaults to the item rendered as text) |

## Usage (from `packages/core/stories/data/DzOrderList.stories.ts`, story `Default`)

```vue

      <div style="max-width: 26rem">
        <DzOrderList v-bind="args" v-model:value="value" data-key="id">
          <template #item="{ item }">{{ item.label }}</template>
        </DzOrderList>
      </div>
    
```

## Playground

Editable, running the **States** story from `packages/core/stories/data/DzOrderList.stories.ts` (lines 214–310) **verbatim**. Edits run in a sandbox; nothing here is saved.

<DzPlayground component="DzOrderList" />

## Variants and controlled state

**Recipe axes.** Each axis is mirrored onto the root as `data-{axis}` carrying the
**resolved** value — after group and provider inheritance, not the raw prop — which is what
makes `[data-size="lg"]` a selector you can rely on.

| Axis | Root attribute | Prop |
| --- | --- | --- |
| `size` | `[data-size="…"]` | `size` |
| `variant` | `[data-variant="…"]` | `variant` |

**Controlled and uncontrolled — `value`.** Both forms are supported, and they are
different contracts rather than two spellings of one.

```vue
<!-- Uncontrolled: the component owns the value. -->
<DzOrderList />

<!-- Controlled: you own it, and the component only ever asks. -->
<DzOrderList v-model:value="value" />

<!-- Controlled, long form — the same thing, written out. -->
<DzOrderList :value="value" @update:value="value = $event" />
```

**Where each variant is shown.** 7 stories in
`packages/core/stories/data/DzOrderList.stories.ts`: `Default`, `WithControls`, `MultiSelect`, `CustomItem`, `States`, `Accessibility: Keyboard-Only Reorder`, `Real World: Dashboard Widget Order`.

## Parts, states and tokens

**Parts** — addressable nodes, emitted as `data-part`. Reach one with the selector, or pass
classes by name through the typed `ui` prop; a typo in `ui` is a type error rather than a class
that lands nowhere.

When your `class` and a `ui` entry set the same Tailwind utility, **your `class` wins**: the
merge order is recipe → `ui` → `class`, and `cn()` is tailwind-merge, so the last one through
takes effect. That is what lets you restyle a wrapper someone else built without `!important`.

| Part | Selector | Always present |
| --- | --- | --- |
| `control` | `[data-part="control"]` | no — renders zero or more than once |
| `empty` | `[data-part="empty"]` | no — renders zero or more than once |
| `group` | `[data-part="group"]` | no — renders zero or more than once |
| `item` | `[data-part="item"]` | no — renders zero or more than once |
| `item-label` | `[data-part="item-label"]` | no — renders zero or more than once |
| `list` | `[data-part="list"]` | yes |
| `root` | `[data-part="root"]` | yes |

```vue
<DzOrderList :ui="{ 'control': 'ring-2', 'empty': 'ring-2', 'group': 'ring-2', 'item': 'ring-2', 'item-label': 'ring-2', 'list': 'ring-2', 'root': 'ring-2' }" />
```

**States** — the values `data-state` may take, plus the presence-only boolean attributes.

| State | Selector |
| --- | --- |
| `disabled` | `[data-state="disabled"]` |
| `grabbed` | `[data-state="grabbed"]` |
| `selected` | `[data-state="selected"]` |

**Component tokens** — the custom properties this component reads, and therefore every one you
may set. The list is the complete supported override surface; any other `--dz-*` it inherits is
not a promise.

| Custom property |
| --- |
| `--dz-order-list-border` |
| `--dz-order-list-control-gap` |
| `--dz-order-list-disabled-opacity` |
| `--dz-order-list-drop-indicator-color` |
| `--dz-order-list-drop-indicator-size` |
| `--dz-order-list-gap` |
| `--dz-order-list-grab-shadow` |
| `--dz-order-list-handle-color` |
| `--dz-order-list-handle-hover` |
| `--dz-order-list-item-bg` |
| `--dz-order-list-item-gap` |
| `--dz-order-list-item-grab-bg` |
| `--dz-order-list-item-hover-bg` |
| `--dz-order-list-item-padding-x` |
| `--dz-order-list-item-padding-y` |
| `--dz-order-list-item-selected-bg` |
| `--dz-order-list-item-selected-fg` |
| `--dz-order-list-radius` |

Declared in `packages/core/src/components/data/DzOrderList.anatomy.ts`.

## Provider defaults and context

This component reads the following contexts from the surrounding `DzProvider` (ADR-20). The
precedence is fixed and not per-component: **prop, then any group context, then the provider,
then the component's own default.**

| Reader | What the provider supplies through it |
| --- | --- |
| `useDzMessages` | the translated string catalogue |
| `useDzTestIds` | the test-id attribute name and prefix |

## Locale, direction and formats

| Axis | Declared | What it means |
| --- | --- | --- |
| `mirrors` | `layout` | Margins, padding, borders and insets are logical, so the box flips with the document. |
| `keyboard` | `none` | The arrow keys do not swap: they move on the block axis, or map to a direction the user can see. |
| `icons` | — | No icon on this component carries direction, so none is mirrored. |

**Locale and formats.** Reads `useDzMessages` from the surrounding `DzProvider`, so its strings and formatted values follow the application locale.

**Measured:** `rtl-contract` is `present` — `packages/core/src/components/data/DzOrderList.anatomy.ts`.

## Server rendering, portals, performance and security

| Concern | State |
| --- | --- |
| **Server rendering** | `present` — `packages/core/tests/ssr/aria-attribute-casing-ssr.spec.ts`. |
| **Portal / teleport** | Does not teleport: it renders in place, so there is no portal to hydrate. |
| **Performance baseline** | `stale` — `packages/core/perf/baselines.json`. 1/1 metric(s) have a derived threshold |
| **Security boundary** | `none` — no host-supplied HTML, file, URL or payload reaches a sink. |

**Peer packages.** Which external packages this component can reach is a property of the built
artifact, not of its source, and is measured by `yarn report:peer-surface` over `dist/` — a
report with no baseline and no ratchet, deliberately outside `validate:all`, because the
numbers depend on decisions the owner has not taken. It is not summarised here rather than
summarised wrongly.

## States and migration

`DzOrderList` advertises 3 states:
`disabled`, `grabbed`, `selected`. Each is emitted as `data-state` or as a
presence-only boolean attribute, so it is selectable in CSS and assertable in a test.

**Published examples:** `state-stories` is `pass` — `packages/core/stories/data/DzOrderList.stories.ts`.

**Migration.** Breaking changes to this component are recorded in the repository's changesets
and published in the release notes; nothing is restated here, because a hand-typed migration
note drifts from the release it describes the first time the release changes. This component
last changed at `63c1325`.

## Extraction fidelity

Published rather than assumed. These are this component's own numbers, measured by the
extraction that produced the tables above.

| Member kind | Extracted | With a description | Notes |
| --- | --- | --- | --- |
| Props | 19 | 19 | 8 declare a default, of which 10 declare `undefined` (ADR-20 provider supplies the value) |
| Events | 5 | 5 | 4 recovered from the `Dz*Emits` interface · 1 synthesised by `defineModel` |
| Slots | 3 | 3 | 1 carry slot props |
| Exposed on `ref` | 0 | 0 | nothing is exposed on the template ref |

## Accessibility and evidence

::: warning How far this evidence goes
Every state on this page is read from a generated artifact and bound to the commit that
artifact records — `6cbc6f17` for the capability matrix,
`527dbd15` for the quality matrix. It is **locally qualified**:
produced by a local run on one machine, against a worktree carrying uncommitted work. It is **not** continuous-integration evidence, **not** release evidence and **not**
production evidence, and it must not be read as a conformance claim.
:::

- **Risk tier:** `C` — adds `a11y-narrative`, `real-world-story`, `perf-baseline` on top of the tiers below it.
- **APG pattern:** [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/)
- **Traits:** `dataset`, `drags`
- **Security boundary:** `none`
- **Declared anatomy:** `declared`
- **Component last changed at:** `63c1325d`

**Why this pattern:** Reorder over a consumer-supplied collection, with three input paths (drag, control buttons, keyboard grab/move/drop) that must agree on one model.

### WCAG 2.2 criteria in scope (22)

The success criteria **this component can fail**, derived from its tier and its traits. This is
a scope list, not a conformance claim: a criterion appearing here means it applies, not that it
has been verified. What has been verified, and by which lane, is the evidence table below.

| SC | Criterion | Level | New in |
| --- | --- | --- | --- |
| [1.1.1](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html) | Non-text Content | A | WCAG 2.0 |
| [1.3.1](https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships.html) | Info and Relationships | A | WCAG 2.0 |
| [1.4.1](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html) | Use of Color | A | WCAG 2.0 |
| [1.4.3](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) | Contrast (Minimum) | AA | WCAG 2.0 |
| [1.4.4](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html) | Resize Text | AA | WCAG 2.0 |
| [1.4.10](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html) | Reflow | AA | WCAG 2.1 |
| [1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) | Non-text Contrast | AA | WCAG 2.1 |
| [1.4.12](https://www.w3.org/WAI/WCAG22/Understanding/text-spacing.html) | Text Spacing | AA | WCAG 2.1 |
| [2.1.1](https://www.w3.org/WAI/WCAG22/Understanding/keyboard.html) | Keyboard | A | WCAG 2.0 |
| [2.1.2](https://www.w3.org/WAI/WCAG22/Understanding/no-keyboard-trap.html) | No Keyboard Trap | A | WCAG 2.0 |
| [2.1.4](https://www.w3.org/WAI/WCAG22/Understanding/character-key-shortcuts.html) | Character Key Shortcuts | A | WCAG 2.1 |
| [2.4.3](https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html) | Focus Order | A | WCAG 2.0 |
| [2.4.7](https://www.w3.org/WAI/WCAG22/Understanding/focus-visible.html) | Focus Visible | AA | WCAG 2.0 |
| [2.4.11](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) | Focus Not Obscured (Minimum) | AA | WCAG 2.2 |
| [2.5.1](https://www.w3.org/WAI/WCAG22/Understanding/pointer-gestures.html) | Pointer Gestures | A | WCAG 2.1 |
| [2.5.3](https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html) | Label in Name | A | WCAG 2.1 |
| [2.5.7](https://www.w3.org/WAI/WCAG22/Understanding/dragging-movements.html) | Dragging Movements | AA | WCAG 2.2 |
| [2.5.8](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) | Target Size (Minimum) | AA | WCAG 2.2 |
| [3.2.1](https://www.w3.org/WAI/WCAG22/Understanding/on-focus.html) | On Focus | A | WCAG 2.0 |
| [3.2.2](https://www.w3.org/WAI/WCAG22/Understanding/on-input.html) | On Input | A | WCAG 2.0 |
| [4.1.2](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html) | Name, Role, Value | A | WCAG 2.0 |
| [4.1.3](https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html) | Status Messages | AA | WCAG 2.1 |

::: tip WCAG 2.5.7 Dragging Movements — met
DzOrderList drags (reorder an item by dragging its grip), so SC 2.5.7 applies and was audited.

- **Keyboard (SC 2.1.1):** Space grabs the row, ArrowUp/ArrowDown move it, Space drops, Escape cancels and restores, Home/End move to the ends; each step announced in a live region.
- **Single pointer, no dragging (SC 2.5.7):** Four always-visible controls -- move to top / up / down / bottom -- each a single tap.

**Caveat:** The single-pointer path is the move controls, rendered under `v-if="showControls"`. `showControls` defaults to true, so the component conforms as shipped; a consumer who sets `:show-controls="false"` and keeps the drag handle leaves reorder drag-only for a pointer user, and nothing warns them. The keyboard path survives that configuration; SC 2.5.7 does not.
:::

### Keyboard interaction

**10 declared bindings.** Rendered from the
component's own keyboard contract, not from the APG pattern it is held to — where the two
differ, the difference is the point.

| Key | Action | WCAG | Pattern |
| --- | --- | --- | --- |
| `ArrowDown` | Move focus to the next option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `ArrowUp` | Move focus to the previous option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `Home` | Move focus to the first option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `End` | Move focus to the last option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `Enter` | Select the focused option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `Space` | Select the focused option. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| any character key | Move focus to the next option whose label starts with that character. | `2.1.1` | [`listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) |
| `Alt` + `ArrowUp` | Move the selected item one position earlier. | `2.1.1`, `2.5.7` | — *(component-specific)* |
| `Alt` + `ArrowDown` | Move the selected item one position later. | `2.1.1`, `2.5.7` | — *(component-specific)* |
| `Escape` | Cancel the reorder and restore the original position. | `2.1.1`, `2.1.2` | — *(component-specific)* |

Declared in `packages/core/src/components/data/DzOrderList.anatomy.ts`.

- **Pattern:** [APG — `listbox`](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/) · its *Keyboard Interaction* section is
  the contract this component is audited against.
- **Measured:** `keyboard-spec` is **unrun** — The component declares 10 binding(s); the unit spec asserts no key event for `Home`, `Enter`. The contract is the yardstick, not the presence of any key at all.

### Assistive technology

**0 of 6 AT/browser pairs executed.** Nothing below has been run: every row is `unrun`, which means the pairing was not available and not attempted — a different fact from a failure.

Tasks this component owes on each pair: `reach`, `navigate`, `typeahead`, `select`, `non-drag`, `live`.

| Pair | AT | Browser | Platform | Result | Tester | Date | AT/browser versions | Recorded at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `nvda-firefox` | NVDA | Firefox | Windows | `unrun` | — | — | — | — |
| `nvda-chrome` | NVDA | Chrome | Windows | `unrun` | — | — | — | — |
| `jaws-chrome` | JAWS | Chrome | Windows | `unrun` | — | — | — | — |
| `voiceover-safari` | VoiceOver | Safari | macOS | `unrun` | — | — | — | — |
| `voiceover-ios` | VoiceOver | Safari | iOS | `unrun` | — | — | — | — |
| `talkback-android` | TalkBack | Chrome | Android | `unrun` | — | — | — | — |

Script: `e2e/at-matrix/scripts/DzOrderList.at-script.md` · record: `e2e/at-matrix/DzOrderList.md` (append-only).

A row here is written by a human after a real screen-reader session, never by a generator.
See [the AT matrix](/evidence/at-matrix) for the state of all pairs across the catalogue.

### Evidence cells (18)

Every kind of evidence required of this component — by Tier C, by its traits (`dataset`, `drags`) — and what was found. The states are `pass` (a lane ran and passed), `present` (an artifact exists and is bound to the component), `stale` (it exists but predates the component's last change), `excepted` (the requirement was waived with a recorded reason) and `unrun` (nothing has measured it).

| Evidence | Required by | State | Where |
| --- | --- | --- | --- |
| `contract-spec` | tier A | `present` | `packages/core/src/components/data/DzOrderList.contract.spec.ts` |
| `unit-spec` | tier A | `present` | `packages/core/src/components/data/DzOrderList.spec.ts` |
| `axe` | tier A | **`unrun`** | — |
| `story-light-dark` | tier A | `pass` | `packages/core/stories/data/DzOrderList.stories.ts` |
| `ssr-sample` | tier A | `present` | `packages/core/tests/ssr/aria-attribute-casing-ssr.spec.ts` |
| `token-contrast` | tier A · corpus-wide | `pass` | `packages/tooling/src/token-checks/intent-text-contrast.ts` — Corpus gate: `yarn validate:tokens` covers every pair in the catalog at once. |
| `keyboard-spec` | tier B | **`unrun`** | `packages/core/src/components/data/DzOrderList.spec.ts` — The component declares 10 binding(s); the unit spec asserts no key event for `Home`, `Enter`. The contract is the yardstick, not the presence of any key at all. |
| `state-stories` | tier B | `pass` | `packages/core/stories/data/DzOrderList.stories.ts` |
| `controlled-uncontrolled` | tier B | **`unrun`** | The unit spec does not exercise both a controlled and an uncontrolled value path. |
| `browser-play` | tier B | `pass` | `packages/core/stories/data/DzOrderList.stories.ts` |
| `rtl-contract` | tier B | `present` | `packages/core/src/components/data/DzOrderList.anatomy.ts` · `packages/core/docs/rtl-matrix.md` |
| `browser-matrix` | tier B | `pass` | `e2e/matrix/conditions.spec.ts` · `e2e/matrix/browser-evidence.json` · `e2e/matrix/known-failures.json` · `e2e/matrix/engine-ratchets.json` — 24/24 projects measured green at 3ee3d5f. chromium 149.0.7827.55 (playwright chromium v1228): all 8 conditions, no expected failure in what it ran. firefox 151.0 (playwright firefox v1532): all 8 conditions, no expected failure in what it ran. webkit 26.5 (playwright webkit v2311): all 8 conditions, no expected failure in what it ran |
| `data-scenarios` | trait dataset | **`unrun`** | `packages/core/stories/data/DzOrderList.stories.ts` |
| `a11y-narrative` | tier C | `pass` | `packages/core/stories/data/DzOrderList.stories.ts` |
| `real-world-story` | tier C | `pass` | `packages/core/stories/data/DzOrderList.stories.ts` |
| `at-manual` | tier B | **`unrun`** | `e2e/at-matrix/DzOrderList.md` — 6 AT/browser pairs, none executed. |
| `perf-baseline` | tier C | `stale` | `packages/core/perf/baselines.json` — 1/1 metric(s) have a derived threshold |
| `non-drag-alternative` | trait drags | `present` | `packages/core/src/components/data/DzOrderList.spec.ts` — A keyboard path is asserted; whether it covers the whole drag interaction is a review question this cannot answer. |

**5 unrun:** `axe`, `keyboard-spec`, `controlled-uncontrolled`, `data-scenarios`, `at-manual` · **1 stale:** `perf-baseline`. They are named rather than counted, because a total tells a reader nothing about what is missing.

The `at-manual` row above is the matrix's **summary** of the screen-reader lane. This page does
not rely on it: the *Assistive technology* section is rendered from the append-only run records
themselves, and the generator refuses to build this page if the summary claims more than those
records support. [Why](/evidence/accessibility#why-this-site-reads-the-raw-scaffold-and-not-the-summary-cell).
