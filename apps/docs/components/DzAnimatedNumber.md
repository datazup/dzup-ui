---
title: "DzAnimatedNumber"
description: "a figure that counts from its previous value to the next."
outline: [2, 3]
---

<!-- GENERATED by `yarn generate:docs-pages` from packages/core/docs/component-meta.json.
     Every API fact on this page is a projection of that artifact (constraint B9); edits are
     overwritten on the next run. Hand-written prose belongs in components/_usage/<Name>.md. -->

# DzAnimatedNumber

a figure that counts from its previous value to the next.

- **Family:** Data display
- **Source:** `packages/core/src/components/data/DzAnimatedNumber.vue` · types `packages/core/src/components/data/DzAnimatedNumber.types.ts`
- **Install:** `npm i @dzup-ui/core` — then `import { DzAnimatedNumber } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/data`
- **Risk tier:** A · **Status:** experimental
- **Taxonomy:** size: `icon` `xs` `sm` `md` `lg` `xl` · tone: `neutral` `primary` `success` `warning` `danger` `info`

::: info How to read the tables on this page
Everything below is extracted from source by `vue-component-meta` and published as measured,
never as asserted.

- **Declared default** is what the component *declares*, not what you necessarily get. Many props
  declare `undefined` on purpose so the surrounding `DzProvider` supplies the effective value at
  runtime (ADR-20). `—` means no default is declared at all; those are different facts and are not
  collapsed here.
- **An empty description cell (`—`) means no prose exists in source**, not that the member is
  self-explanatory. The per-page counts under *Extraction fidelity* say how many are in that state.
- **Event descriptions are recovered from the `Dz*Emits` interface**, because Vue's `ShortEmits`
  mapped type erases the JSDoc before any extractor can see it. Events written by `defineModel`
  (ADR-16) have no authored description anywhere and say so.
- **`defineExpose` members are published as names and types only.** No description exists in
  source for any of them, catalog-wide, so the column is omitted rather than shown empty.
- **Usage snippets are verbatim slices of the component's Storybook story.** Nothing on this site
  synthesises example markup; a component without a story says so.
:::


## Props (14, of which 5 inherited from `@dzup-ui/contracts`)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `ariaDescribedby` | `string \| undefined` | no | `undefined` | ID of element that describes this component |
| `ariaInvalid` | `boolean \| "grammar" \| "spelling" \| undefined` | no | `undefined` | Indicates the component has invalid input |
| `ariaLabel` | `string \| undefined` | no | `undefined` | Accessible label |
| `ariaLabelledby` | `string \| undefined` | no | `undefined` | ID of element that labels this component |
| `duration` | `number \| undefined` | no | `1000` | Animation duration in milliseconds (`0` snaps with no animation) |
| `easing` | `AnimatedNumberEasing \| undefined` | no | `"ease-out"` | Easing curve — mirrors the `--dz-ease-*` design tokens |
| `format` | `Intl.NumberFormatOptions \| undefined` | no | `undefined` | `Intl.NumberFormat` options (currency, percent, decimals, …) |
| `from` | `number \| undefined` | no | `0` | Value the first animation starts from (the count-up origin). Subsequent changes animate from the currently-displayed value, not this. |
| `id` | `string \| undefined` | no | `undefined` | Unique element ID (prefer `useId()` from Vue 3.5 when auto-generated) |
| `locale` | `string \| string[] \| undefined` | no | `undefined` | BCP-47 locale(s) for formatting (defaults to the runtime locale) |
| `size` | `CanonicalSize \| undefined` | no | `"md"` | Component size — scales the figure |
| `startOnView` | `boolean \| undefined` | no | `true` | Begin the first animation only once scrolled into view, via IntersectionObserver. Later value changes always animate immediately. |
| `tone` | `CanonicalTone \| undefined` | no | `"neutral"` | Semantic colour tone of the figure |
| `value` | `number` | yes | — | The target value; animates from the previous value whenever this changes |

## Events (2)

| Event | Payload | Description |
| --- | --- | --- |
| `complete` | `[value: number]` | The figure has settled on its target value |
| `start` | `[value: number]` | An animation toward a new target has begun |

## Slots (2)

| Slot | Slot props | Description |
| --- | --- | --- |
| `prefix` | `DzAnimatedNumberAffixSlotProps` | Content rendered before the figure (e.g. a currency glyph or icon) |
| `suffix` | `DzAnimatedNumberAffixSlotProps` | Content rendered after the figure (e.g. a unit or "+" badge) |

## Exposed on `ref` (2)

| Member | Type |
| --- | --- |
| `displayText` | `string` |
| `displayValue` | `number` |

## Usage (from `packages/core/stories/data/DzAnimatedNumber.stories.ts`, story `Integer`)

```vue

      <div class="flex flex-col items-start gap-4 p-6">
        <DzAnimatedNumber v-bind="args" :value="value" aria-label="Active users" />
        <button
          type="button"
          class="rounded-[var(--dz-radius-md)] border border-[var(--dz-border)] bg-[var(--dz-surface)] px-3 py-1.5 text-[length:var(--dz-text-sm)] text-[var(--dz-foreground)]"
          @click="reroll"
        >
          Animate to a new value
        </button>
      </div>
    
```

## Playground

No editable playground is published for this component.

Every story in `packages/core/stories/data/DzAnimatedNumber.stories.ts` either has a computed template or binds Storybook's `args`, neither of which can be mounted outside Storybook. A playground here would have to be written by hand, and a hand-written example is not evidence of anything.

## Extraction fidelity

Published rather than assumed. These are this component's own numbers, measured by the
extraction that produced the tables above.

| Member kind | Extracted | With a description | Notes |
| --- | --- | --- | --- |
| Props | 14 | 14 | 6 declare a default, of which 7 declare `undefined` (ADR-20 provider supplies the value) |
| Events | 2 | 2 | 2 recovered from the `Dz*Emits` interface · 0 synthesised by `defineModel` |
| Slots | 2 | 2 | 2 carry slot props |
| Exposed on `ref` | 2 | 0 | no description exists in source for any exposed member, catalog-wide |

## Accessibility and evidence

::: warning How far this evidence goes
Every state on this page is read from a generated artifact and bound to the commit that
artifact records — `51dec93c` for the capability matrix,
`51dec93c` for the quality matrix. It is **locally qualified**:
produced by a local run on one machine, against a worktree carrying uncommitted work. It is **not** continuous-integration evidence, **not** release evidence and **not**
production evidence, and it must not be read as a conformance claim.
:::

- **Risk tier:** `A` — the base tier, which every component owes: `contract-spec`, `unit-spec`, `axe`, `story-light-dark`, `ssr-sample`, `token-contrast`.
- **APG pattern:** `none` — no WAI-ARIA Authoring Practices pattern describes this component.
- **Traits:** none declared
- **Security boundary:** `none`
- **Declared anatomy:** `absent` — the component has not declared its parts, which is not the same claim as having none
- **Component last changed at:** `4c9fb7a1`

### WCAG 2.2 criteria in scope (11)

The success criteria **this component can fail**, derived from its tier and its traits. This is
a scope list, not a conformance claim: a criterion appearing here means it applies, not that it
has been verified. What has been verified, and by which lane, is the evidence table below.

| SC | Criterion | Level | New in |
| --- | --- | --- | --- |
| [1.1.1](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html) | Non-text Content | A | WCAG 2.0 |
| [1.3.1](https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships.html) | Info and Relationships | A | WCAG 2.0 |
| [1.4.1](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html) | Use of Color | A | WCAG 2.0 |
| [1.4.3](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) | Contrast (Minimum) | AA | WCAG 2.0 |
| [1.4.4](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html) | Resize Text | AA | WCAG 2.0 |
| [1.4.10](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html) | Reflow | AA | WCAG 2.1 |
| [1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) | Non-text Contrast | AA | WCAG 2.1 |
| [1.4.12](https://www.w3.org/WAI/WCAG22/Understanding/text-spacing.html) | Text Spacing | AA | WCAG 2.1 |
| [2.2.2](https://www.w3.org/WAI/WCAG22/Understanding/pause-stop-hide.html) | Pause, Stop, Hide | A | WCAG 2.0 |
| [4.1.2](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html) | Name, Role, Value | A | WCAG 2.0 |
| [4.1.3](https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html) | Status Messages | AA | WCAG 2.1 |

### Keyboard interaction

**Not yet derived.** This library has no machine-readable keyboard table: the only generated
keyboard signal is whether a spec asserts *some* key, not which key does what. Rather than
hand-type a table that nothing could check, this page links the pattern the component is held
to and states what has actually been measured.

- **Pattern:** `none` — **no APG pattern applies**, so there is no external
  keyboard contract to link.
- **Measured:** Tier A does not owe a `keyboard-spec`, so nothing measures this
  component's keyboard behaviour.

### Assistive technology

Tier A does not owe a manual screen-reader run, so this component has no row in
the AT matrix. Only Tier B and above do.

### Evidence cells (6)

Every kind of evidence required of this component — by Tier A — and what was found. The states are `pass` (a lane ran and passed), `present` (an artifact exists and is bound to the component), `stale` (it exists but predates the component's last change), `excepted` (the requirement was waived with a recorded reason) and `unrun` (nothing has measured it).

| Evidence | Required by | State | Where |
| --- | --- | --- | --- |
| `contract-spec` | tier A | `present` | `packages/core/src/components/data/DzAnimatedNumber.contract.spec.ts` |
| `unit-spec` | tier A | `present` | `packages/core/src/components/data/DzAnimatedNumber.spec.ts` |
| `axe` | tier A | **`unrun`** | — |
| `story-light-dark` | tier A | `pass` | `packages/core/stories/data/DzAnimatedNumber.stories.ts` |
| `ssr-sample` | tier A | **`unrun`** | — |
| `token-contrast` | tier A · corpus-wide | `pass` | `packages/tooling/src/token-checks/intent-text-contrast.ts` — Corpus gate: `yarn validate:tokens` covers every pair in the catalog at once. |

**2 unrun:** `axe`, `ssr-sample`. They are named rather than counted, because a total tells a reader nothing about what is missing.
