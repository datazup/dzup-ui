---
title: "DzDialog"
description: "Root compound component wrapping Reka UI DialogRoot (ADR-07)."
outline: [2, 3]
---

<!-- GENERATED by `yarn generate:docs-pages` from packages/core/docs/component-meta.json.
     Every API fact on this page is a projection of that artifact (constraint B9); edits are
     overwritten on the next run. Hand-written prose belongs in components/_usage/<Name>.md. -->

# DzDialog

Root compound component wrapping Reka UI DialogRoot (ADR-07).

- **Family:** Overlays
- **Source:** `packages/core/src/components/overlays/DzDialog.vue` · types `packages/core/src/components/overlays/DzDialog.types.ts`
- **Compound parts:** `DzDialogClose`, `DzDialogContent`, `DzDialogDescription`, `DzDialogOverlay`, `DzDialogTitle`, `DzDialogTrigger`
- **Install:** `npm i @dzup-ui/core` — then `import { DzDialog } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Risk tier:** B · **Status:** stable
- **v-model:** `v-model:open` (`boolean | undefined`)

::: info How to read the tables on this page
Everything below is extracted from source by `vue-component-meta` and published as measured,
never as asserted.

- **Declared default** is what the component *declares*, not what you necessarily get. Many props
  declare `undefined` on purpose so the surrounding `DzProvider` supplies the effective value at
  runtime (ADR-20). `—` means no default is declared at all; those are different facts and are not
  collapsed here.
- **An empty description cell (`—`) means no prose exists in source**, not that the member is
  self-explanatory. The per-page counts under *Extraction fidelity* say how many are in that state.
- **Event descriptions are recovered from the `Dz*Emits` interface**, because Vue's `ShortEmits`
  mapped type erases the JSDoc before any extractor can see it. Events written by `defineModel`
  (ADR-16) have no authored description anywhere and say so.
- **`defineExpose` members are published as names and types only.** No description exists in
  source for any of them, catalog-wide, so the column is omitted rather than shown empty.
- **Usage snippets are verbatim slices of the component's Storybook story.** Nothing on this site
  synthesises example markup; a component without a story says so.
:::


## Props (5)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `animated` | `boolean \| undefined` | no | `true` | Whether open/close transitions are enabled (default true) |
| `contentTransition` | `string \| undefined` | no | `"dz-dialog-content"` | CSS transition name for the content panel (default 'dz-dialog-content') |
| `modal` | `boolean \| undefined` | no | `true` | Whether the dialog is modal (default true) |
| `open` | `boolean \| undefined` | no | `false` | — |
| `overlayTransition` | `string \| undefined` | no | `"dz-dialog-overlay"` | CSS transition name for the overlay backdrop (default 'dz-dialog-overlay') |

## Events (1)

| Event | Payload | Description |
| --- | --- | --- |
| `update:open` | `[value: boolean]` | synthesised by `defineModel` (ADR-16) — no authored description exists |

## Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | DzDialogTrigger, DzDialogContent, and other dialog parts |

## Usage (from `packages/core/stories/overlays/DzDialog.stories.ts`, story `Default`)

```vue

      <DzDialog v-bind="args">
        <DzDialogTrigger as-child>
          <DzButton>Open Dialog</DzButton>
        </DzDialogTrigger>
        <DzDialogContent>
          <DzDialogTitle>Dialog Title</DzDialogTitle>
          <DzDialogDescription>This is a description of what this dialog is about.</DzDialogDescription>
          <div class="flex justify-end gap-3 mt-4">
            <DzDialogClose as-child>
              <DzButton variant="outline" tone="neutral">Cancel</DzButton>
            </DzDialogClose>
            <DzDialogClose as-child>
              <DzButton tone="primary">Confirm</DzButton>
            </DzDialogClose>
          </div>
        </DzDialogContent>
      </DzDialog>
    
```

## Playground

Editable, running the **Size Gallery** story from `packages/core/stories/overlays/DzDialog.stories.ts` (lines 103–134) **verbatim**. Edits run in a sandbox; nothing here is saved.

<DzPlayground component="DzDialog" />

## Compound parts

`DzDialog` is used with 6 sub-parts, documented here rather than on pages of their own because they are not usable outside it.

### DzDialogClose

Close button for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogClose } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`

#### Props (1)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `asChild` | `boolean \| undefined` | no | `false` | When true, the close styling (absolute top-right positioning, default X icon) is skipped and the close behaviour is forwarded onto the slotted child element. Use for action buttons (Cancel / Confirm) inside a dialog footer. |

#### Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Close button content |

A compound sub-part of `DzDialog`; see that component's usage snippet.

### DzDialogContent

Content panel for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogContent } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`
- **Anatomy parts (ADR-19):** `content`, `footer`, `header`, `overlay`, `viewport`

#### Props (12, of which 8 inherited from `@dzup-ui/contracts`)

| Prop | Type | Required | Declared default | Description |
| --- | --- | --- | --- | --- |
| `ariaDescribedby` | `string \| undefined` | no | `undefined` | ID of element that describes this component |
| `ariaInvalid` | `boolean \| "grammar" \| "spelling" \| undefined` | no | `undefined` | Indicates the component has invalid input |
| `ariaLabel` | `string \| undefined` | no | `undefined` | Accessible label |
| `ariaLabelledby` | `string \| undefined` | no | `undefined` | ID of element that labels this component |
| `id` | `string \| undefined` | no | `undefined` | Unique element ID (prefer `useId()` from Vue 3.5 when auto-generated) |
| `overlayClass` | `string \| undefined` | no | `undefined` | **Deprecated:** since the `ui` prop landed (ADR-19 §5) — prefer `:ui="{ overlay: … }"`, which names the part rather than singling one node out with a prop of its own. Kept working for one minor series; removing it needs a major changeset. Both apply, with `ui.overlay` last. TODO(remove-after: 0.3.0). Additional class applied to the overlay owned by DzDialogContent. |
| `portalDefer` | `boolean \| undefined` | no | `false` | Defer target resolution until the application has mounted. |
| `portalDisabled` | `boolean \| undefined` | no | `false` | Render inline instead of teleporting to the portal target. |
| `portalTo` | `string \| HTMLElement \| undefined` | no | `undefined` | Portal target. Defaults to `document.body` when omitted. |
| `scrollable` | `boolean \| undefined` | no | `false` | When true, the dialog body scrolls within a capped max-height (80vh) and `#header` / `#footer` slots are pinned to the top and bottom of the panel. The default slot is used as the scrollable body. |
| `size` | `DialogContentSize \| undefined` | no | `"md"` | Size of the dialog content panel |
| `ui` | `Partial<Record<"content" \| "footer" \| "header" \| "overlay" \| "viewport", DzClassValue>> \| undefined` | no | `undefined` | Per-part class overrides, keyed by the names in `DzDialogContent.anatomy.ts` (ADR-19). `class` continues to apply to the content panel. |

#### Events (5)

| Event | Payload | Description |
| --- | --- | --- |
| `closeAutoFocus` | `[event: Event]` | — |
| `escapeKeyDown` | `[event: KeyboardEvent]` | — |
| `interactOutside` | `[event: Event]` | — |
| `openAutoFocus` | `[event: Event]` | — |
| `pointerDownOutside` | `[event: Event]` | — |

#### Slots (3)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Dialog body content (DzDialogTitle, DzDialogDescription, etc.) |
| `footer` | — | Sticky footer region, rendered below the body when `scrollable` is true |
| `header` | — | Sticky header region, rendered above the body when `scrollable` is true |

A compound sub-part of `DzDialog`; see that component's usage snippet.

### DzDialogDescription

Description element for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogDescription } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`

#### Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Description text content |

A compound sub-part of `DzDialog`; see that component's usage snippet.

### DzDialogOverlay

Backdrop overlay for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogOverlay } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`

#### Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Optional overlay content |

A compound sub-part of `DzDialog`; see that component's usage snippet.

### DzDialogTitle

Title element for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogTitle } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`

#### Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Title text content |

A compound sub-part of `DzDialog`; see that component's usage snippet.

### DzDialogTrigger

Trigger element for DzDialog compound.

- **Install:** `npm i @dzup-ui/core` — then `import { DzDialogTrigger } from '@dzup-ui/core'`
- **Entry points:** `@dzup-ui/core`, `@dzup-ui/core/overlays`
- **Compound part of:** `DzDialog`

#### Slots (1)

| Slot | Slot props | Description |
| --- | --- | --- |
| `default` | — | Trigger element |

A compound sub-part of `DzDialog`; see that component's usage snippet.

## Extraction fidelity

Published rather than assumed. These are this component's own numbers, measured by the
extraction that produced the tables above.

| Member kind | Extracted | With a description | Notes |
| --- | --- | --- | --- |
| Props | 5 | 4 | 5 declare a default |
| Events | 1 | 0 | 0 recovered from the `Dz*Emits` interface · 1 synthesised by `defineModel` |
| Slots | 1 | 1 | 0 carry slot props |
| Exposed on `ref` | 0 | 0 | nothing is exposed on the template ref |

## Accessibility and evidence

::: warning How far this evidence goes
Every state on this page is read from a generated artifact and bound to the commit that
artifact records — `51dec93c` for the capability matrix,
`51dec93c` for the quality matrix. It is **locally qualified**:
produced by a local run on one machine, against a worktree carrying uncommitted work. It is **not** continuous-integration evidence, **not** release evidence and **not**
production evidence, and it must not be read as a conformance claim.
:::

- **Risk tier:** `B` — adds `keyboard-spec`, `state-stories`, `controlled-uncontrolled`, `browser-play`, `rtl-contract`, `browser-matrix`, `at-manual` on top of the tiers below it.
- **APG pattern:** [`dialog`](https://www.w3.org/WAI/ARIA/apg/patterns/dialog/)
- **Traits:** `teleports`
- **Security boundary:** `none`
- **Declared anatomy:** `declared`
- **Component last changed at:** `80ce3012`

**Compound sub-parts are not matrix rows.** `DzDialogClose`, `DzDialogContent`, `DzDialogDescription`, `DzDialogOverlay`, `DzDialogTitle`, `DzDialogTrigger` are documented on this page and carry no evidence row of its own. Everything below describes `DzDialog`. Whether sub-parts should become rows — some of them own a sink their parent declares — is an open owner decision.

### WCAG 2.2 criteria in scope (18)

The success criteria **this component can fail**, derived from its tier and its traits. This is
a scope list, not a conformance claim: a criterion appearing here means it applies, not that it
has been verified. What has been verified, and by which lane, is the evidence table below.

| SC | Criterion | Level | New in |
| --- | --- | --- | --- |
| [1.1.1](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html) | Non-text Content | A | WCAG 2.0 |
| [1.3.1](https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships.html) | Info and Relationships | A | WCAG 2.0 |
| [1.4.1](https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html) | Use of Color | A | WCAG 2.0 |
| [1.4.3](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) | Contrast (Minimum) | AA | WCAG 2.0 |
| [1.4.4](https://www.w3.org/WAI/WCAG22/Understanding/resize-text.html) | Resize Text | AA | WCAG 2.0 |
| [1.4.10](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html) | Reflow | AA | WCAG 2.1 |
| [1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) | Non-text Contrast | AA | WCAG 2.1 |
| [1.4.12](https://www.w3.org/WAI/WCAG22/Understanding/text-spacing.html) | Text Spacing | AA | WCAG 2.1 |
| [2.1.1](https://www.w3.org/WAI/WCAG22/Understanding/keyboard.html) | Keyboard | A | WCAG 2.0 |
| [2.1.2](https://www.w3.org/WAI/WCAG22/Understanding/no-keyboard-trap.html) | No Keyboard Trap | A | WCAG 2.0 |
| [2.4.3](https://www.w3.org/WAI/WCAG22/Understanding/focus-order.html) | Focus Order | A | WCAG 2.0 |
| [2.4.7](https://www.w3.org/WAI/WCAG22/Understanding/focus-visible.html) | Focus Visible | AA | WCAG 2.0 |
| [2.4.11](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) | Focus Not Obscured (Minimum) | AA | WCAG 2.2 |
| [2.5.3](https://www.w3.org/WAI/WCAG22/Understanding/label-in-name.html) | Label in Name | A | WCAG 2.1 |
| [2.5.8](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) | Target Size (Minimum) | AA | WCAG 2.2 |
| [3.2.1](https://www.w3.org/WAI/WCAG22/Understanding/on-focus.html) | On Focus | A | WCAG 2.0 |
| [3.2.2](https://www.w3.org/WAI/WCAG22/Understanding/on-input.html) | On Input | A | WCAG 2.0 |
| [4.1.2](https://www.w3.org/WAI/WCAG22/Understanding/name-role-value.html) | Name, Role, Value | A | WCAG 2.0 |

### Keyboard interaction

**Not yet derived.** This library has no machine-readable keyboard table: the only generated
keyboard signal is whether a spec asserts *some* key, not which key does what. Rather than
hand-type a table that nothing could check, this page links the pattern the component is held
to and states what has actually been measured.

- **Pattern:** [APG — `dialog`](https://www.w3.org/WAI/ARIA/apg/patterns/dialog/) · its *Keyboard Interaction* section is
  the contract this component is audited against.
- **Measured:** `keyboard-spec` is **unrun** — The unit spec exists and asserts no key sequence.

### Assistive technology

**0 of 6 AT/browser pairs executed.** Nothing below has been run: every row is `unrun`, which means the pairing was not available and not attempted — a different fact from a failure.

Tasks this component owes on each pair: `open`, `reach`, `dismiss`.

| Pair | AT | Browser | Platform | Result | Tester | Date | AT/browser versions | Recorded at |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `nvda-firefox` | NVDA | Firefox | Windows | `unrun` | — | — | — | — |
| `nvda-chrome` | NVDA | Chrome | Windows | `unrun` | — | — | — | — |
| `jaws-chrome` | JAWS | Chrome | Windows | `unrun` | — | — | — | — |
| `voiceover-safari` | VoiceOver | Safari | macOS | `unrun` | — | — | — | — |
| `voiceover-ios` | VoiceOver | Safari | iOS | `unrun` | — | — | — | — |
| `talkback-android` | TalkBack | Chrome | Android | `unrun` | — | — | — | — |

Record: `e2e/at-matrix/DzDialog.md` (append-only). No executable script exists for this component yet; scripts were authored for the Tier C and D components first.

A row here is written by a human after a real screen-reader session, never by a generator.
See [the AT matrix](/evidence/at-matrix) for the state of all pairs across the catalogue.

### Evidence cells (14)

Every kind of evidence required of this component — by Tier B, by its traits (`teleports`) — and what was found. The states are `pass` (a lane ran and passed), `present` (an artifact exists and is bound to the component), `stale` (it exists but predates the component's last change), `excepted` (the requirement was waived with a recorded reason) and `unrun` (nothing has measured it).

| Evidence | Required by | State | Where |
| --- | --- | --- | --- |
| `contract-spec` | tier A | `present` | `packages/core/src/components/overlays/DzDialog.contract.spec.ts` |
| `unit-spec` | tier A | `present` | `packages/core/src/components/overlays/DzDialog.spec.ts` |
| `axe` | tier A | `present` | `packages/core/tests/a11y/overlays.a11y.spec.ts` |
| `story-light-dark` | tier A | `pass` | `packages/core/stories/overlays/DzDialog.stories.ts` |
| `ssr-sample` | tier A | `present` | `packages/core/tests/ssr/ssr-smoke.spec.ts` |
| `token-contrast` | tier A · corpus-wide | `pass` | `packages/tooling/src/token-checks/intent-text-contrast.ts` — Corpus gate: `yarn validate:tokens` covers every pair in the catalog at once. |
| `keyboard-spec` | tier B | **`unrun`** | The unit spec exists and asserts no key sequence. |
| `state-stories` | tier B | `pass` | `packages/core/stories/overlays/DzDialog.stories.ts` |
| `controlled-uncontrolled` | tier B | **`unrun`** | The unit spec does not exercise both a controlled and an uncontrolled value path. |
| `browser-play` | tier B | `pass` | `packages/core/stories/overlays/DzDialog.stories.ts` |
| `rtl-contract` | tier B | `present` | `packages/core/src/components/overlays/DzDialog.anatomy.ts` · `packages/core/docs/rtl-matrix.md` |
| `browser-matrix` | tier B | `pass` | `e2e/matrix/conditions.spec.ts` · `e2e/matrix/known-failures.json` · `e2e/matrix/engine-ratchets.json` — chromium 149.0.7827.55 (playwright chromium v1228): all 6 conditions, no expected failure in what it ran. firefox 151.0 (playwright firefox v1532): all 6 conditions, no expected failure in what it ran. webkit 26.5 (playwright webkit v2311): all 6 conditions, no expected failure in what it ran |
| `portal-hydration` | trait teleports | `present` | `packages/core/tests/ssr/ssr-smoke.spec.ts` |
| `at-manual` | tier B | **`unrun`** | `e2e/at-matrix/DzDialog.md` — 6 AT/browser pairs, none executed. |

**3 unrun:** `keyboard-spec`, `controlled-uncontrolled`, `at-manual`. They are named rather than counted, because a total tells a reader nothing about what is missing.

The `at-manual` row above is the matrix's **summary** of the screen-reader lane. This page does
not rely on it: the *Assistive technology* section is rendered from the append-only run records
themselves, and the generator refuses to build this page if the summary claims more than those
records support. [Why](/evidence/accessibility#why-this-site-reads-the-raw-scaffold-and-not-the-summary-cell).
