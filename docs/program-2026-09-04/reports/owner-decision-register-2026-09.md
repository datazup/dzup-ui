# Owner-decision register — consolidated, 2026-09

**Task:** TASK-R0-O1 (`docs/program-2026-09-04/release-exit-tasks.md` §"Owner packets")
**Compiled:** 2026-09-22 · **Commit observed:** `527dbd1` (`git rev-parse HEAD`, 2026-09-22) with **278 uncommitted paths**
**Amended:** 2026-09-22 (release-exit fix pass) — §2.5, §3.5 and §5.6 append **D179**–**D192**, the fourteen decisions the R0 packets raised and neither appended; §5.7 raises **D193**–**D194** on two gates that structurally cannot fail; §1.2 and §7.5 correct the changeset count 36 → **37**; **D180**'s stated ground is corrected. Every amendment carries a dated blockquote naming what it used to say. Source: [`release-exit-verification-2026-09-22.md`](./release-exit-verification-2026-09-22.md) (findings **S3**, **S4**, **S5**).
**Status of this document:** the register is complete **as of the 2026-09-22 (b) amendment**; **no decision in it is taken by this task.** TASK-R0-O1 consolidates, measures and recommends. The owner decides. "Complete" is a claim with a shelf life — it was false within an hour of first being written, because the next packet raised six decisions and appended none (**D183**, finding **S3**). Read it as *complete through `D196`*, and check `grep -o 'D[0-9]\+' EXECUTION-STATUS.md` against this file before trusting it again.

> **Amended 2026-09-22 (b)** — TASK-R2-O1's completion pass (all 24 browser projects re-run at the first clean committed tree, `589be13`) appends **§5.8** with **D195** and **D196**. The shelf-life warning above held for a third time: this amendment exists because that task raised two decisions and the register did not yet carry them. Previously this line read *"complete through `D194`"*.

> **Amended 2026-09-26 — the owner took ten rows.** **N5-01 D2**, **D160**, **D174**, **D175**, **D176**, **D177**, **D180**, **D181**, **D182** and **D186** are `decided 2026-09-26`, each on the recommended option except **D176**, where the owner kept the current floor (D160's answer). Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md), which also executes N5-01 D2, D182 and D186. The rest are separate packets. The totals tables in §1 are not recomputed and still count these ten as open.

Companion: [`publication-decision-packet-2026-09.md`](./publication-decision-packet-2026-09.md) — the A4-D1 packet (the first row of §2).

---

## 0. How to read this register

| Column | Meaning |
|---|---|
| **Id** | The id **as its source report spells it.** Ids are not globally unique — see §7.1, the D-id collision. |
| **Raised by** | The report that raised it, by repo-relative path. Every row cites one. |
| **Gates** | What cannot move until it is decided: a 1.0 criterion (C1–C15), a task id, or a consumer surface. |
| **Options / recommendation** | Compressed from the source, which stays authoritative for the full argument. Where two reports disagree, the row says so in **bold** and §7 carries the detail. |
| **State** | `open` · `decided <date>` · `taken <date>` (agent choice under delegation, reversible) · `superseded by <id>` · `partially …`. |

Grouping is by **what the decision gates**, not by who raised it:

- [§2 Publication](#2-publication--gates-the-first-npm-publish-and-every-consumer-surface)
- [§3 1.0](#3-10--gates-a-criterion-c1c15-or-an-adr-acceptance)
- [§4 Evidence](#4-evidence--gates-a-claim-the-repository-makes-about-itself)
- [§5 Hygiene](#5-hygiene--real-but-gates-nothing-on-the-critical-path)
- [§6 Already decided](#6-already-decided--cite-do-not-reopen)
- [§7 Contradictions and collisions](#7-contradictions-and-collisions--flagged-not-resolved)

**Register totals at `527dbd1`, 2026-09-22, after the D179–D192 amendment.**

| Group | Open rows | Of which raised by the six 2026-09-21 R1 packets (`D154`–`D178`) | Of which added 2026-09-22 (`D179`–`D194`) |
|---|---|---|---|
| §2 Publication | 57 | 9 | 6 |
| §3 1.0 | 34 | 3 | 2 |
| §4 Evidence | 85 | 0 | 0 |
| §5 Hygiene | 57 | 13 | 8 |
| **Total open** | **233** | **25** | **16** |
| §6 Already decided | 45 | 0 | 0 |

The last column is 14 decisions the two R0 packets raised and never appended
(§2.5, §3.5, §5.6) plus **2** the fix pass raised itself (§5.7, `D193`–`D194`).

> The first three columns read **51 / 32 / 85 / 49 = 217** before 2026-09-22.
> They were an accurate count of the rows in the document and an inaccurate
> count of the decisions the programme had raised, because fourteen rows were
> missing. Both numbers are kept visible rather than one silently replacing the
> other, for the same reason every ratchet here records its old value.

The `<done_check>` asks for `grep -c '^| '` >= 100. This register answers with
**368** rows — but the count is not the measure, since 100 empty rows would
satisfy it too. The measure is coverage: **every decisions section of all 21
`program-2026-09` handoffs and all 29 `program-2026-09-04` reports has been read
and is represented**, and §1 states the two premises that turned out false.
Coverage is also the thing that failed here: the document satisfied the row
count from the first draft and was still missing fourteen decisions (§2.5).

---

## 1. What was counted, and the two false premises

Two numbers in the task prompt and the programme README are estimates. Both were measured for this register, and both are wrong.

### 1.1 "~125 open `[!owner]` items" → the real figure is **314 raised, 233 open**

> **Corrected 2026-09-22.** The heading read *"**298 raised, 253 open**"*. Two
> things were wrong with it. **253 was the pre-de-duplication figure** —
> the table's own last row said 217 — so the heading contradicted the document
> it introduced. And **both figures stopped at `D178`**: the fourteen decisions
> the R0 packets raised (`D179`–`D192`) were never counted, because they were
> never appended (§2.5, finding **S3**). The table below now runs to `D192`.

| Source | Decisions raised | Open at `527dbd1` |
|---|---|---|
| `docs/program-2026-09/` — N0/N1/N2/N5 ledgers and their 21 handoffs | **158** | **113** (45 closed by the 09-04 programme, §6) |
| `docs/program-2026-09-04/` — global sequence `D1`–`D194` (`D43`/`D44` never issued, so **192** exist) | **192** | **156** (115 of `D1`–`D153` are exactly `open`; all 25 of `D154`–`D178` are open; all 16 of `D179`–`D194` are open) |
| **Total, before de-duplication** | **350** | **269** |
| Less N-programme items an 09-04 id supersedes (one row carries both ids) | −36 | −36 |
| **Distinct decisions in this register** | **314** | **233** |

The per-report breakdown of the 158, counted from each handoff's own decisions section:
N0-05 5 · N1-O1 8 · N1-O2 8 · N1-O3 9 · N1-O4 11 · N1-O5 6 · N1-O6 7 ·
N2-A1 7 · N2-A2 9 · N2-A3 8 · N2-A4 7 · N2-D1 7 · N2-D2 7 · N2-D3 5 · N2-S1 7 · N2-T1 9 ·
N5-01 7 · N5-02 6 · N5-03 6 · N5-04 4 · N5-05 15.

**One count in the source ledgers does not reconcile, and it is recorded rather
than quietly corrected.** `EXECUTION-STATUS-N5.md`'s closing note says the N5
lane opened 38 *"on top of the **62** standing from N2"*. A per-handoff recount
of the nine N2 decisions sections gives **66** (A1 7 · A2 9 · A3 8 · A4 7 ·
D1 7 · D2 7 · D3 5 · S1 7 · T1 9). The four-item difference is most likely
N2-A4's seven `A4-D*` items being counted as a study rather than as lane
decisions, but **no document says so**, so both figures stand: **62 as the N2
lane self-reported, 66 as its handoffs enumerate.** This register uses 66,
because each of the 66 has a row.

**README §7's "~125" understates the N-programme alone by 33, and the whole
backlog by a factor of two.** It is not a rounding error: it predates the entire
09-04 programme, which raised 192 more.

### 1.2 "the 20 pending changesets" → there are **37**

> **Corrected 2026-09-22.** This subsection read **36**, measured at `527dbd1`
> before TASK-R0-O2 ran. R0-O2 then added the 37th —
> `.changeset/aria-invalid-is-a-validation-prop-and-only-validation-props-carry-it.md`
> — 45 minutes later, and its own §4 table records 37. **37 is the truth**; the
> committed-tree figure of 36 is now only of historical interest.
> (`release-exit-verification-2026-09-22.md` §C1, finding **S4**.)

Measured three ways at `527dbd1` **plus the working tree**, all agreeing:

```
ls .changeset/*.md | grep -v README | wc -l                                            → 37
git ls-tree --name-only 527dbd1 .changeset/ | grep '\.md$' | grep -v README | wc -l   → 36   (committed only)
git status --porcelain .changeset/
  ?? .changeset/aria-invalid-is-a-validation-prop-and-only-validation-props-carry-it.md
yarn validate:release-policy                                                           → exit 0
  "✓ release-policy: `changeset status` assembles; 6 published, 2 withheld, 5 private
   of 13 workspace packages; 37 pending changeset(s), 0 major, 0 mixed;
   changelog-format collisions at the ceiling of 1"
```

The gate counts the working tree, which is what `changeset version` will act on,
so **37** is the operative figure and the 36 in the git index is the pre-R0-O2
snapshot.

Three different counts are quoted across the programme; **two are wrong today,
and the third has become right**:

| Quoted | Where | Status today |
|---|---|---|
| 16 | `N5-01-release-policy-handoff.md` §6 | **Wrong.** Correct **on 2026-09-03**; 21 changesets have landed since, and **none of them has been audited against `VERSIONING.md`** — see the publication packet §3. |
| 20 | `release-exit-tasks.md` TASK-R0-O1 `<validation>` ("20 expected at `99b963a`") and the `_Gap:_` preamble | **Wrong.** Stale by two commits' worth of packets (**D184**). |
| 37 | `TASK-R1-O1-handoff.md` §7 ("37 changesets") | **Right, by coincidence and now on the merits.** It was arrived at by counting `.changeset/README.md` against 36 real ones; there are now 37 real ones. The number to quote is 37; the *derivation* in that handoff is still the wrong one. |

### 1.3 A third premise, and this one holds

The `_Gap:_` preamble's **"10 over-declared changesets (N5-01 D3)"** is right and
also incomplete. N5-01 measured **10 fully over-declared + 1 over-declared in a
secondary package** (`selection-controls-…`, core correct / contracts
over-declared), which is why its own §10 table says **11** and its §6 outcome
line says **10**. Both numbers are in the same document and neither is a
mistake: **10 changesets are wholly over-declared, 11 carry at least one
over-declaration.** This register uses 11 when counting rows to re-level.

---

## 2. Publication — gates the first npm publish and every consumer surface

**27 decisions gate the first publication.** The first six are the packet:
nothing below them can be sequenced until they are taken. Full costing in
[`publication-decision-packet-2026-09.md`](./publication-decision-packet-2026-09.md).

### 2.1 The publication gate itself

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **A4-D1** 🔴 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 | **Everything.** Criterion C12; all three registries; the docs site (**D172**); every `npm install @dzup-ui/*` string in the docs, `llms.txt`, MCP and `/r/**` | (a) publish 0.x now · (b) freeze and stop advertising install commands · (c) publish to a private registry first | Source recommends **publish, gated behind N5-01's 0.x policy**. This task costs all three in the packet and **does not choose** | **open** |
| **A4-D2** 🔴 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 | A4-D1's ordering. `dzup-ui.com` is **NXDOMAIN** at apex and www and is baked into 3 registry indexes, 87 markdown mirrors, both `llms*.txt`, `sitemap.xml` and every canonical link | register the domain · change `SITE_ORIGIN` and regenerate all 282 `/r/**` files | **Decide before the first publish.** Free to change today, expensive after — `origin.ts`'s own header says so. **Same subject as D166; see §7.2** | **open** |
| **D166** 🔴 | [`TASK-R1-O5-handoff.md`](./TASK-R1-O5-handoff.md) §8 | Everything; it is A4-D2 re-measured at `527dbd1` | (a) register `dzup-ui.com`, one origin (`/`, `/storybook/`, `/docs/`) · (b) `docs.dzup-ui.com` second origin · (c) `datazup.com/ui/` | **(a)** — `SITE_ORIGIN` is already `https://dzup-ui.com`; (c) costs a regeneration of all 282 registry files | **open** |
| **N5-01 D1** 🔴 / **D146** | [`N5-01-release-policy-handoff.md`](../../program-2026-09/reports/N5-01-release-policy-handoff.md) §10 · **proven** by [`TASK-R1-O1-handoff.md`](./TASK-R1-O1-handoff.md) §7 | The **first `changeset version`**; criterion C15 | (a) widen the `validate:mcp` heading regex · (b) **+ sync the three version mirrors** · (c) drop the ISO-date rule | **(a) + (b) together, ~20 lines, before any release is attempted.** D146 proves the failure is **five errors, not one** — N5-01 D1 understates it by four | **open** (proof complete) |
| **N5-01 D2** 🟠 | [`N5-01-release-policy-handoff.md`](../../program-2026-09/reports/N5-01-release-policy-handoff.md) §10 F5/F6 · restated as **N5-02 D6** | Whether `@dzup-ui/compat` and `@dzup-ui/codemods` can ever ship. Both are public, `publishConfig.access: public`, in CI's pack smoke test, and on the changesets `ignore` list | release them · formally withhold with a reason (status quo, `release-policy.json` already carries the reasons) | **Formally withhold and say so in the README**, unless the codemods are meant to be consumable — in which case release is the only honest answer. Measured cost in packet §4 | **decided 2026-09-26 — release `@dzup-ui/codemods`; `@dzup-ui/compat` stays withheld.** Executed by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md): codemods is on the `published` list and off the changesets `ignore` list |
| **N5-01 D3** 🟠 | [`N5-01-release-policy-handoff.md`](../../program-2026-09/reports/N5-01-release-policy-handoff.md) §6, §10 | The version numbers the first release ships | re-level the **11** over-declared changesets · keep them conservative | **Re-level, and audit the 20 that were never audited first** (§1.2). Re-levelling takes `@dzup-ui/core` `0.3.0 → 0.2.1`; doing it after publication is impossible | **open** |
| **D158** 🔴 | [`TASK-R1-O3-handoff.md`](./TASK-R1-O3-handoff.md) §10 | The published barrel; criterion C12. `generate:exports` would **drop** `useAffix`, `useCalendar`, `useInfiniteScroll`, `useScrollSpy`, `useScrollToTop` and **add** `useCountdown`, `useIntersection`; the manifest declares `"version": "0.0.1"` against a `0.2.0` package | (a) the barrel is right — add the 5, drop the 2, no bump · (b) the manifest is right — ship the removal as a **`minor`** · (c) retire `public-api.manifest.json` as the barrel's source | **Reported, not resolved, by design.** Which option is right depends on whether those five composables are intended public API — *"not a fact a tool can read"*. **(a) if they are.** This is the input TASK-R0-O1 was waiting for | **open** |

### 2.2 Ordering, evidence and policy the first publish needs

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D172** 🔴 | [`TASK-R1-O5-handoff.md`](./TASK-R1-O5-handoff.md) §8 | The order of the whole docs packet | (a) deploy after A4-D1 · (b) before, install commands suppressed · (c) before, as-is | **(a)** — *"a live site whose every install command returns `E404` is worse than NXDOMAIN, because NXDOMAIN is honest"* | **open** |
| **D154** 🔴 | [`TASK-R1-O3-handoff.md`](./TASK-R1-O3-handoff.md) §10 | Publishing `@dzup-ui/mcp`. **20 advisories, 8 `high`**, all transitive, all with a fix available, all in the one published package that ships a server | (a) bump the transitives + `patch` changeset + re-run `release:evidence` · (b) triage and record accepted risks · (c) withhold `@dzup-ui/mcp` from the first publication | **(a)** — mechanical, re-proven by the gate in 13 s. **Doc 08 names no severity threshold; setting one is part of this decision** | **open** |
| **D159** 🟠 | [`TASK-R1-O3-handoff.md`](./TASK-R1-O3-handoff.md) §10 | The first publication. OSS has no `docs/release/rollback-and-support.md`; Pro has one. `npm unpublish` is available for **72 h only** | (a) write it before the first publication · (b) fold into this packet · (c) defer | **(a) — a prerequisite, not a follow-up.** The first release is the first moment it can be needed | **open** |
| **N2-A1 D5** 🟠 | [`N2-A1-mcp-governance-handoff.md`](../../program-2026-09/reports/N2-A1-mcp-governance-handoff.md) §12 | The first **MCP registry** publish. `server.json` declares the `io.github.datazup/mcp` namespace; `PUBLISHING.md` requires it be one the publisher controls, verified by GitHub OAuth | verify the namespace · change it · do not publish to the registry | **Verify before any registry action. Nothing in the repo can check this** — it is an account fact, not a code fact | **open** |
| **N2-A2 D6** 🟠 / **D171** | [`N2-A2-component-meta-handoff.md`](../../program-2026-09/reports/N2-A2-component-meta-handoff.md) §10 · [`TASK-R1-O5-handoff.md`](./TASK-R1-O5-handoff.md) §8 | The three MCP tools over HTTP. `build:registry` was deliberately never run because it `rm -rf`s `apps/landing/public/r/` and rewrites 282 tracked files | (a) scope the wipe to the files it writes · (b) merge the two generators · (c) leave it, gated by `validate:registry` | **(a).** `validate:registry` now *detects* the hole (link exists in `validate:all`, 50 links at `527dbd1`), but a generator that deletes a sibling's output is still wrong | **open** |
| **N2-A3 D2** 🟠 | [`N2-A3-llms-gate-handoff.md`](../../program-2026-09/reports/N2-A3-llms-gate-handoff.md) §13 | Every deployed consumer surface. Three build steps outstanding; the packet is **local-green / production-stale** | run the three builds as part of the deploy job · keep them manual | **Fold all three into `docs-deploy` (D167).** A surface that is only correct on one developer's disk is the failure mode this programme keeps finding | **open** |
| **A4-D4** 🟢 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 | Registry content, not publication itself | 8 `registry:theme` items from `ThemeRecipe` in · out | **In (Option B)** — near-zero marginal cost; the projection function exists | **open** |
| **A4-D5** 🟠 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 | Pro distribution posture | (c) private GitHub repo · (d) self-hosted service · npm-only (status quo) | **(c), and only after N4-L1 fixes the tier model.** Cross-repo — **not this repository's to take alone** | **open** |
| **A4-D6** 🟢 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 | README §5's standing refusals | record "no per-component source distribution" as a standing refusal · leave unrecorded | **Yes** — same class as "a blanket unstyled mode" and "a second styling engine" | **open** |
| **A4-D7** 🟠 | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 (A4-F4) | Correctness of an installed theme. It is inert under `[data-theme="dark"]` and unlayered | fix `tokensItem.ts` · document the `.dark` requirement at the install site · both | **Both, folded into Option B.** A registry item that silently does nothing is worse than one that is absent | **open** |
| **A4-D3** ✅ | [`registry-evaluation-2026-09.md`](../../program-2026-09/reports/registry-evaluation-2026-09.md) §11 (A4-F2) | `/r/**` freshness | build `validate:registry` · accept no gate | **Built.** `validate:registry` = `tsx packages/tooling/src/validators/registry.ts`, in `validate:all` | **decided — executed, confirmed at `527dbd1`** |

### 2.3 The icon swap and the packaging hygiene that rides with it

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D174** 🟠 / **N5-04 D2** | [`TASK-R1-O6-handoff.md`](./TASK-R1-O6-handoff.md) §9 · [`icon-swap-contract-2026-09.md`](./icon-swap-contract-2026-09.md) | `validate:peers` (red by design — the defect is real and unwaived); criterion C12 | (a) swap all three manifests to `@lucide/vue ^1.47.0`, run the codemod, re-record visual + AT baselines, `minor` changeset · (b) defer the swap, align ranges only · (c) leave both defects | **(a)** — the contract is complete (18/18 mapped, codemod + 7 fixtures, runbook), the deprecation is upstream and permanent, and **visual baselines (R2-O6) and the AT matrix (R2-O2) are not yet locked, which makes now the cheapest moment.** Cost: **+1,068 gzip** and a full icon re-baseline | **decided 2026-09-26 — (a).** Swap, codemod and re-baseline are the next icon packet, not yet executed. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D175** 🟠 | [`TASK-R1-O6-handoff.md`](./TASK-R1-O6-handoff.md) §9 | The changeset level for D174 | (a) `patch` (N5-04's answer: "no public API change") · (b) **`minor`** | **(b)** — three consumer-visible changes: every icon's rendered `class`, a new default `aria-hidden="true"`, and three different drawings. **This re-levels N5-04's own answer — see §7.3** | **decided 2026-09-26 — (b) `minor`.** Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D177** 🟢 | [`TASK-R1-O6-handoff.md`](./TASK-R1-O6-handoff.md) §9 | Nothing on the critical path, but it breaks a changeset reference | (a) remove the `apps/sandbox` workspace, its deploy configs, its changeset-ignore entry and ~10 doc references · (b) keep, justification signed · (c) retire the deployment only, keep the tree | **(c) now; (a) only once the owner confirms the Coolify resource `dzup-ui-sandbox-production` and its DNS record can go.** (a) is not an agent action | **decided 2026-09-26 — (a), full removal**, including the Coolify resource and its DNS record. Not yet executed; the Coolify/DNS step is a production action and is confirmed again when it runs. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D178** 🟢 / **N5-04 D1** | [`TASK-R1-O6-handoff.md`](./TASK-R1-O6-handoff.md) §9 · [`peer-hygiene-2026-09.md`](../../program-2026-09/reports/peer-hygiene-2026-09.md) §1.5 | Nothing today — **it is the recording that has value** (the decision has now been re-derived three times) | (a) leave `reka-ui` a non-optional peer · (b) `peerDependenciesMeta.optional` alone · (c) optional **+** `./components/*` subpaths | **(a), recorded with its reopening condition.** **(b) is measurably worse than doing nothing** and `validate:peers` reports it green. (c) freezes 637 emitted module paths as public API in a package that has not shipped 1.0 | **open** (a formality) |
| **N5-04 D4** 🟠 | [`N5-04-peer-hygiene-handoff.md`](../../program-2026-09/reports/N5-04-peer-hygiene-handoff.md) §3 | The first non-`en` locale pack; `validate:all`'s link count (37 → 38 when the gate lands) | pack format + parity gate, ordered **export → gate → first pack** | **Do not check in a second locale before `validate:i18n-parity` exists.** The pseudo-locale is the reference pack the gate asserts against. `./i18n` export is **patch**-level and additive | **open** |

### 2.4 Further publication-gating decisions from the 09-04 sequence

Carried from `EXECUTION-STATUS.md` §"Owner decisions raised by this program". Each cites the handoff that raised it.

| Id | Raised by | Decision (what it gates is stated in the text) | Options / recommendation (source) | State |
|---|---|---|---|---|
| **D3** | [`TASK-R3-O1`](./TASK-R3-O1-handoff.md) | Once `OWNERSHIP_TIERS` gains `'pro'`, `validate:ownership` requires `DZUP_PRO_OWNERSHIP_MANIFEST` on every machine, so CI and OSS-only checkouts go red | (a) vendor the Pro manifest into OSS as a checked-in input (names + package strings only) · (b) publish it as an artifact CI fetches · (c) accept the red — **rec. (a)** | open |
| **D6** 🟠 | [`TASK-R3-O2`](./TASK-R3-O2-handoff.md) | `DZ_PROVIDER_DEFAULTS` grew a `sanitizer` key — the first growth since ADR-20 §2 published it, and `DzTestIds.prefix`'s docstring records a deliberate refusal to grow it | (a) accept under a `minor`, as shipped · (b) hold the sanitizer defaults in a second constant so §2's object stays frozen — **rec. (a)**: a defaults object that cannot describe a new concern stops answering "w… | open |
| **D10** 🟠 | [`TASK-R5-O1`](./TASK-R5-O1-handoff.md) | `ariaInvalid` was **added** to `BaseValidationProps` (its correct home, N5-02 D1) and left deprecated on `BaseAccessibilityProps`, so the change is additive and no prop surface moves. The **removal** touches *… | (a) do the removal now as its own `minor` · (b) additive now + removal as a named follow-up · (c) abandon the move and keep the nine `Omit<>` sites — **rec. (b) then (a)**, scheduled when `component-meta.json`… | open |
| **D22** 🟢 | [`TASK-R5-O2 session 2`](./TASK-R5-O2-handoff.md) | **`states` now carries presence-only attributes outside the ADR-19 §4 boolean vocabulary.** `DzCalendar` declares `today`, `in-range`, `outside-month` — presence-only attributes it sets, which is what §4 says… | (a) leave them declared and ungated — the docs page and the theme author gain the information · (b) add the three to `BOOLEAN_STATE_ATTRIBUTES`, growing a closed list that exists so a primitive's private marke… | open |
| **D35** 🟠✅ | [`TASK-R5-O5`](./TASK-R5-O5-handoff.md) | **TAKEN as (a) — each component declares its FULL table; each row names the APG pattern it implements in `apg`.** Provenance survives without inheritance, and where a component departs from its assigned patter… | **(a) flat per-component tables with per-row `apg` provenance — TAKEN** · (b) a pattern → keyboard dictionary in `@dzup-ui/contracts`, expanded per component — rejected: it publishes the APG's table rather tha… | open |
| **D36** 🟠 | [`TASK-R5-O5`](./TASK-R5-O5-handoff.md) | **Five components' `rtl.keyboard` disagrees with what their arrow keys do — prepared, not resolved.** The new in-file contradiction check fired on 7; two were an authoring error and are fixed. **`DzTreeSelect`… | **(a) drop the `rtl` marker from those rows — TAKEN PROVISIONALLY**, so no published row contradicts a shipped declaration and no other packet's ratcheted field is touched; cost: five components publish no RTL… | open |
| **D38** 🔴 | [`TASK-R5-O6`](./TASK-R5-O6-handoff.md) | **Ratify `UI_MERGE_ORDER = recipe → ui → class`, and sequence the 73-component migration.** Of **79** merge sites, **5** pass `ui` before `class` and **74** pass `class` first, so `<DzButton class="rounded-non… | (a) **confirm Order A and commission the 73-component migration — taken as far as an agent may**: ratified, gated, published, ceiling created; the migration is separate because re-ordering a merge changes rend… | open |
| **D40** 🟠 | [`TASK-R5-O6`](./TASK-R5-O6-handoff.md) | **`DzButton.asChild` is a published prop that does nothing.** `DzButton.types.ts:47` declares `asChild?: boolean`, `DzButton.vue:34` defaults it to `false`, and **the template never reads it**: the root is `<c… | (a) **remove the prop** — breaking, therefore a `minor` while 0.x, and the honest correction, since `as`/`href`/`to` already provide polymorphism and work · (b) implement it — a new `asChild` implementer, whic… | open |
| **D45** 🟠 | [`TASK-R5-O7`](./TASK-R5-O7-handoff.md) | **`DzCodeBlock.tokens.ts` documents a theming API the component does not implement.** Nine `--dz-codeblock-*` component tokens that `@dzup-ui/tokens` never emits; `codeBlockTokens` has **no consumer at all**… | (a) **wire the map** — emit the nine tokens and make the variants read them (restores the theming contract; a pixel-diff risk) · (b) **delete the map** — honest, and drops a published-looking API · (c) leave t… | open |
| **D52** 🟠 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **`DzAsyncBoundary.delay` is a published prop the component never reads** — declared at `DzAsyncBoundary.types.ts:6`, defaulted at `DzAsyncBoundary.vue:10`, **referenced nowhere else**; its own unit spec asser… | (a) **implement it** — the evident intent (wait this long before showing the `loading` slot) is real and useful, and is a new implementer this task's `<scope>` forbids · (b) **remove the prop** — breaking, the… | open |
| **D53** 🟢 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **`componentsWithoutStaticTemplate` stays at 80 and is not this pipeline's to lower.** Re-measured into three populations rather than restated: 64 compound parts with no stories file (by design), `DzThemeProvi… | (a) **leave it — taken**, and the `<stop_conditions>` instruction: a template needing a story that does not exist is a story-dod follow-up · (b) add a `Default` story with a literal template to the 15 — real w… | open |
| **D89** 🟢 | [`TASK-R5-O9 (phase 1)`](./TASK-R5-O9-handoff.md) | **Probe P-613 — Context7: WATCH, and one control is available today.** No `context7.json` anywhere; `https://context7.com/datazup/dzup-ui` is **404** (not indexed); the docs site emits **no per-page `.md` endp… | (a) land a minimal `context7.json` as a **control** before any submission — `disallow: true`, or N2-A3's `folders`/`excludeFolders` draft minus `branchVersions` — keeping submission sequenced after the docs de… | open |
| **D91** 🔴 | [`TASK-R5-O9 (phase 2)`](./TASK-R5-O9-handoff.md) | **Vitest 4 moves the coverage basis; four committed thresholds fail; only the owner may move them — the sole reason TASK-R5-O9 phase 2 refused an otherwise byte-neutral, fully-green migration.** Vitest 4 makes… | **(a) owner re-baselines the four thresholds against a Vitest-4 measurement, then re-runs the packet from handoff §7.1 — rec.** The "after" numbers are in handoff §7.4 and `--coverage.reportOnFailure=true` rep… | open |
| **D93** 🟢 | [`TASK-R5-O9 (phase 2)`](./TASK-R5-O9-handoff.md) | **An *empty* changeset fails `validate:release-policy` R7** (F-9), so phase 1's planned shape for the Vitest packet ("front-matter with no package lines") would have turned link 41 red. A devDependency-only to… | (a) when the migration is re-executed, write `.changeset/toolchain-vitest-4.md` naming **`"@dzup-ui/tooling": patch`** and nothing else — private *and* in `.changeset/config.json`'s `ignore`, so no bump and no… | open |
| **D96** 🟢 | `\` | **Vite 8 is discretionary even after D91: no trigger has fired, and one link is untested.** Memo **B1** is *"Vite 7 leaves security support, **or** a dependency drops Vite 7 — not 'Vite 8 is out'."* Measured 2… | ^6 \ | open |
| **D98** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **D4 -- interactive content nested inside a `role="combobox"` button.** `DzCascader`'s clear control and `DzTreeSelect`'s chip removers are `role="button"` spans rendered INSIDE the `<button role="combobox">`… | (a) **move the control out of the button**, positioned over the field -- the correct fix; it moves the published `data-part="clear"` out of `[data-part="trigger"]`, changes rendered layout and breaks `[data-pa… | open |
| **D100** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **`useFocusTrap` now restores focus on release by default** (the D7 fix). A behaviour change to a published composable | (a) **`restoreFocus: true` by default with an opt-out at the two call sites that own their restore (`DzBlockUI`, `DzPopconfirm`) -- taken**; their behaviour is byte-for-byte unchanged · (b) default `false`, op… | open |
| **D105** 🔴 | [`TASK-R2-O4`](./TASK-R2-O4-handoff.md) | **Finding F-C1 is an SSR defect, not a client-side one.** Measured in three engines: a strict `style-src-attr` drops a `style` attribute in *served markup* and does **not** touch the identical declaration writ… | (a) sweep the 78 `recipe-movable` sites into `tv()` recipes, closing it for both paths — mechanical, one component at a time, with `inline-style-inventory.json` as the worklist and its ceilings as the gate · (… | open |
| **D106** 🟠 | [`TASK-R2-O4`](./TASK-R2-O4-handoff.md) | **`ComponentQuality.securityBoundary` changed shape**, `string` → `string[]`, in a published type and in three generated artifacts (`quality-matrix.json`, `capability-matrix.json`, `component-meta.json`) plus… | (a) **ship under the same `minor` — taken**; it is inseparable from the fix it enables, and the corpus spec that filtered `!== 'none'` (true for all 144 rows once it was an array) is fixed and commented at the… | open |
| **D135** 🔴 | [`TASK-R2-O7`](./TASK-R2-O7-handoff.md) | **The 22 per-export `size:*` budgets are recorded and enforced by nothing — and 20 of them are breached at `2d51eec`.** Measured by TASK-R2-O7's capture: fresh medians against the **committed** thresholds, `cv… | (a) add a `size` arm to the perf gate that asserts each recorded `size:*` threshold — but it must land **with** an owner decision on the 20 existing breaches, or it lands red · (b) adopt the fresh sizes as the… | open |
| **D143** 🟠 | [`independent verification 2026-09-19 (TASK-R2-O1 / R2-O6 / R2-O7)`](./TASK-R2-O1-handoff.md) | **The `stale` cell count moved the wrong way and no gate noticed.** Capability matrix, HEAD `2d51eec` → working tree, measured from the two JSON artifacts: `unrun` **534 → 441** ✓ · `pass` **507 → 585** ✓ · `p… | (a) **rec.** — make `stale` **ratchet-gated**: record the count in a committed ceiling file (as `anatomy-parts` does with `maxUnreviewedPartNames`) and fail when it rises, so clearing it stays optional but gro… | open |
| **D146** | [`TASK-R1-O1 (proof) → TASK-R0-O1 (decision)`](./TASK-R1-O1-handoff.md) | **The first `changeset version` turns `validate:all` red for FIVE reasons, not one.** Proven in a throwaway worktree of `527dbd1`, not predicted (handoff §7). N5-01 D1 is confirmed — `validate:changelog` deman… | (a) widen the `mcp-surface.ts` heading regex to allow ` - YYYY-MM-DD` (one line; fixes the exclusivity, none of the other four) · (b) add a `version` script that runs `generate:mcp-surface` and syncs `server.j… | open |
| **D149** | [`TASK-R1-O2`](./TASK-R1-O2-handoff.md) | **Should `validate:all` require a *fresh* build for the import gate?** `validate:published-imports` warns on a stale `dist` and skips an unbuilt one, mirroring `validate:exports`; `--built` (wired into `ci.yml… | (a) keep as is — `validate:all` stays runnable on a freshly-edited tree and CI carries the strict lane — **rec.** · (b) pass `--built` in `validate:all`, making a full `yarn build` (minutes) a precondition of… | open |
| **D150** | [`TASK-R1-O2`](./TASK-R1-O2-handoff.md) | **A full-fidelity `--install` variant of the import gate.** The gate extracts the tarballs (never `npm install`) and resolves third-party peers through a junction to this repository’s `node_modules`, which is… | (a) add a separate CI-only lane running `npm install --ignore-scripts --legacy-peer-deps` over the tarballs, sequenced with TASK-R1-O3 / TASK-R1-O4 — **rec.** · (b) make it the default — loses offline determin… | open |
| **D151** | [`TASK-R1-O2`](./TASK-R1-O2-handoff.md) | **CI’s “Pack smoke test” has never been able to fail.** `.github/workflows/ci.yml` runs `npm pack --dry-run` per package and greps the output for `workspace:` / `link:`. Measured 2026-09-21: `npm pack --dry-ru… | (a) delete the step — `validate:published-imports --built`, added to the same job, now covers it with `yarn pack` and actually loads the result — **rec.** · (b) rewrite it to unpack the tarball and read its `p… | open |
| **D152** 🟠 | [`TASK-R1-O2 → TASK-R2-O3`](./TASK-R1-O2-handoff.md) | **21 errors in published declarations under `skipLibCheck: false`** — TypeScript’s default. Measured by re-running the gate’s types probe against the kept scratch consumer: 113 errors total, **92 from third-pa… | (a) route the 18 `TS2300` to the defect register: relabel the emits payload parameter in those two components’ `.types.ts`, one line each, `patch` changeset — **rec.** · (b) add a `--strict-lib-check` lane to… | open |
| **D153** | [`TASK-R1-O2`](./TASK-R1-O2-handoff.md) | **No changeset accompanies TASK-R1-O2, deliberately.** Everything it ships is in the private `@dzup-ui/tooling` package, a test script under `packages/nuxt/scripts/` that is not in any tarball, root `package.j… | (a) accept — **rec.** · (b) require changesets for tooling-only changes as policy: that is an edit to `release-policy.json` and a program-wide rule, not this task’s call | open |

### 2.5 The R0 owner packets' own publication-gating decisions (2026-09-22)

> **Why this subsection exists.** TASK-R0-O1 wrote this register and did not
> append its own eight decisions (**D179**–**D186**); TASK-R0-O2 appended none of
> its six (**D187**–**D192**) — an hour after **D183** proposed exactly that
> rule. Measured absent by the independent pass
> ([`release-exit-verification-2026-09-22.md`](./release-exit-verification-2026-09-22.md)
> §D2, finding **S3**): `grep -c "D179\|…\|D192"` → **0**, register max D-id
> **178**. All fourteen were appended on 2026-09-22 by the fix pass, grouped by
> what they gate like every other row. Nothing is resolved here; the handoffs
> stay authoritative for the full argument.

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D180** 🔴 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 · ground corrected by [`release-exit-verification-2026-09-22.md`](./release-exit-verification-2026-09-22.md) §C3 | **N5-01 D3**; the first `changeset version`; every consumer on `^0.2.0`. **Who audits the never-audited changesets, and is `the-six-cascade-layers-…` under-declared?** N5-01 audited 16 of the pending set. R0-O1's first pass found 2 over-declarations and **1 candidate under-declaration** | (a) accept this packet's first pass and re-level 13 · (b) commission a full audit of all 37 to N5-01's depth before any release · (c) re-level only N5-01's 11 and ship | **(b) for #33 specifically, (a) for the rest** — but on the **corrected** ground. **D180's stated ground does not hold:** `packages/core/src/styles/base.css:51` is `@layer dz-reset, dz-tokens, dz-base, dz-components, dz-utilities, dz-overrides;` — `dz-overrides` is still **last**, so a consumer sheet loading after the dzup sheets wins exactly as before, and one loading before already lost to `dz-components`. Precedence for consumer overrides is **unchanged in both load orders**, and the changeset says so itself. The **defensible** ground is one D180 never names: `packages/contracts/src/data-attributes.types.ts:65` types `DataAttributes['data-state']` as `string`, widened from the closed `DataState` union — *"if you were assigning `DataAttributes['data-state']` into a `DataState`-typed variable, that no longer narrows on its own"*. Consumer source that compiled stops compiling, which `packages/contracts/VERSIONING.md` §1 puts in the **minor** position. Re-levelling is still warranted; **the owner should not act on the cascade argument**. **(c) is worse than doing nothing** | **decided 2026-09-26 — (b) for #33, (a) for the rest.** The #33 audit is its own packet, not yet executed. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D182** 🟠 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | **N5-01 D2**; `VERSIONING.md` §3; every future prop removal. **`VERSIONING.md` §3 requires artifacts the release policy forbids shipping** — §3 clause 2 names `warnDeprecated` in **`@dzup-ui/compat`** and clause 3 names a codemod in **`@dzup-ui/codemods`**, both permanently unreleasable. `.changeset/nine-aria-props-that-did-nothing-are-gone.md` already ships a prop removal whose migration codemod no consumer can install | (a) release both withheld packages · (b) keep withholding and **amend §3** to name obtainable artifacts · (c) keep both as they are | **(b) at minimum, in the same change as whichever way N5-01 D2 goes.** A policy that mandates an artifact the policy forbids distributing is unsatisfiable by construction, and it is currently unsatisfied in a shipped changeset | **decided 2026-09-26 — (b), with N5-01 D2 releasing codemods.** `VERSIONING.md` §3 clause 2 is now written migration notes and clause 3 a released codemod. Executed by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D185** 🟢 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | A4-D1's option set; README §8. **Retire Option C (private registry) from the publication options?** It is dominated: its rehearsal value is largely available from **D150** (an `--install` variant of R1-O2's existing import gate) and it leaves all four consumer surfaces untrue | (a) record it as a standing refusal with this reasoning · (b) keep it on the table · (c) build it | **(a)**, folded into README §8's standing refusals — but **A4-D1 explicitly names three options, so this is the owner's to close, not a packet's** | **open** |
| **D186** 🟢 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | `@dzup-ui/mcp`'s changelog; **N5-01 D7**; criterion C15. **Three MCP changesets are still unwritten on an expired reason** — `N2-A1 D3`, `N2-A2 D5` and `N2-A3 D1` each describe a behaviour change to the published `@dzup-ui/mcp` and each deferred its changeset *"because N5-01 owns changelog reconciliation and 17 changesets are already unreleased"* | (a) write the three changesets now · (b) fold them into one `@dzup-ui/mcp: minor` · (c) accept the changes ship unannounced | **(b)** — one changeset naming all three behaviour changes. **The stated reason has expired**: N5-01 closed on 2026-09-03, and there are now **37** pending changesets, none of which covers these | **decided 2026-09-26 — (b).** Written: `.changeset/the-mcp-server-answers-from-the-full-catalog-and-checks-its-input.md`. Executed by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D190** 🟠 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | `@dzup-ui/core`'s next minor; the migration cost of the `ariaInvalid` changeset. **66 components now let `aria-invalid` fall through to `$attrs` silently.** The N5-02 removal gave its six components a one-time dev-mode warning naming the prop and the fall-through; this change did not, for 66 | (a) accept the silent fall-through and rely on the changeset's migration table · (b) add the same warning to all 66 · (c) add it to the ~12 where a rendered `aria-invalid` is actively harmful (`DzToast`, `DzAlert`, `DzNotification`, `DzProgress` and the other status roles) | **(c).** (b) is 66 files of boilerplate for a prop that never did anything; (a) leaves a consumer's binding rendering an ARIA attribute on an element with no role to carry it, with nothing to tell them | **open** |
| **D192** 🟠 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | `@dzup-ui/contracts`'s next minor; **N5-02 D1** (closed by A). **Confirm or reverse `D10`, taken under delegation.** `TASK-R5-O1-handoff.md` §D10 offered (A) do the removal now · (B) additive now, removal as a named follow-up · (C) abandon. R5-O1 shipped B and recommended "B now, then A". **TASK-R0-O2 did A** — its `<requirements><contracts_change>` directs it explicitly | (a) confirm A as shipped · (b) reverse to B — restore one line on `BaseAccessibilityProps` and drop the changeset · (c) reverse to C and keep the `Omit<>` sites permanently | **(a) confirm.** The two conditions R5-O1 named are met — no concurrent session holds the artifacts, and all four regenerated cleanly. Reversal is one line plus deleting a changeset, so this is cheap to undo and expensive to defer again | **open** |

---

## 3. 1.0 — gates a criterion C1–C15 or an ADR acceptance

### 3.1 The Node floor — **three reports, two answers.** Flagged, not resolved (§7.3)

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N5-04 D3** 🟠 | [`N5-04-peer-hygiene-handoff.md`](../../program-2026-09/reports/N5-04-peer-hygiene-handoff.md) §3 | Criterion **C10**; the ADR-18 amendment; the Nuxt fixture pin | move to `>=22.13.0` · keep `^20.19.0 \|\| >=22.13.0` | **`>=22.13.0`**, and stop coupling the floor to the RTL list — that needs Node ≥ **24.0.0**. **ADR-20 §4 and its "Alternatives considered" must be amended in the same change.** Level: **minor (breaking)** | **open** |
| **D160** 🔴 | [`TASK-R1-O4-handoff.md`](./TASK-R1-O4-handoff.md) §9 | Criterion **C10**; `validate-min-runtime`; an ADR-18 amendment either way | **(a) fix the one `globSync` import and keep `^20.19.0 \|\| >=22.13.0`** · (b) raise to `^22.13.0`, drop Node 20 · (c) leave it red | **(a)** — one import in one spec; `^20.19.0` is what every consumer's `engines` check reads. **A sweep is owed either way:** one `@since v22` API reached the tree unnoticed because nothing runs on the floor | **decided 2026-09-26 — (a), keep `^20.19.0 \|\| >=22.13.0`**, taken with D176. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D176** 🟠 | [`TASK-R1-O6-handoff.md`](./TASK-R1-O6-handoff.md) §9, §7.3 | Criterion **C10**; ADR-18 acceptance text (R0-O2); the Nuxt 4.4.5 pin (N5-03 D4) | (a) D160's answer · **(b) `>=22.13.0`** · (c) `>=24.0.0` | **(b)**, with (a) as a legitimate interim. **(c) rejected** — Node 22 LTS runs to April 2027. **Whichever is taken, ADR-20 §4 must be corrected in the same amendment: `getTextInfo()` needs Node 24, not 22.13** | **decided 2026-09-26 — keep `^20.19.0 \|\| >=22.13.0`** (D160's answer, not this row's (b)). ADR-20 §4 is still corrected, per D181. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **N5-03 D4** 🟠 | [`N5-03-toolchain-currency-handoff.md`](../../program-2026-09/reports/N5-03-toolchain-currency-handoff.md) §10 | The Nuxt fixture pin, which the floor unpins | hold at `4.4.5` · move | **Hold until a Nuxt security fix lands above 4.4.5.** Downstream of the floor decision above | **open** |

### 3.2 ADR acceptance — the 15 N5-05 packet decisions (TASK-R0-O2 executes)

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N5-05 D-A** | [`N5-05-adr-acceptance-handoff.md`](../../program-2026-09/reports/N5-05-adr-acceptance-handoff.md) §5 | Nothing (ADR-19 adjacent) | `data-scope`: adopt · defer · refuse | **Defer**; build it with the recipe-attribute emitter as one generated `useAnatomy()` packet — never hand-typed | **open** |
| **N5-05 D-B** 🔴 | ditto | **ADR-19 acceptance** | `DataState`: widen-then-accept · accept as forward commitment · reject §4 | **Widen, then accept.** Two lines, a `patch` | **open** — code half shipped by TASK-R5-O1 |
| **N5-05 D-C** 🔴 | ditto | **ADR-19 acceptance** | declare the three missing cascade layers · strike the Consequences bullet | **Declare them.** One additive line in two files | **open** — code half shipped by TASK-R5-O1 (six layers now declared; see `.changeset/the-six-cascade-layers-…`) |
| **N5-05 D-D** | ditto | Nothing | amend ADR-19 §6's release wording to `VERSIONING.md` §7.2 (N5-01 D4) | **Amend.** Wording only; intent unchanged | **open** |
| **N5-05 D-E** | ditto | The ADR ceiling (14 → 13) | copy ADR-17 into `docs/adr/` | **Do it.** ADR-19 extends it; the document already exists outside the repo | **open** |
| **N5-05 D-F** | ditto | Nothing; cost rises with time | fold the reviewed out-of-vocabulary part names into ADR-19 §3 (N2-S1 S1-D1) | **Fold in at acceptance** — renaming a shipped part name is breaking, so cost only rises. Hold the three `options-*` for D-G | **open** |
| **N5-05 D-G** 🔴 | ditto | The **`forms`** family (28 components) | `DzOptionsState`: inline · compound part · new ownership kind (N2-S1 S1-D4) | Add the ADR-19 §3 rule for unexported internals now; take the disposition as its own packet | **superseded by D15** — taken under delegation; `forms` declared, ratchet reached 41 |
| **N5-05 D-H** 🔴 | ditto | **ADR-20 acceptance** | motion: adopt · amend-and-defer · drop §7 | **Amend and defer**, then adopt as its own packet. Accessibility consequence | **open** |
| **N5-05 D-I** 🔴 | ditto | **ADR-20 acceptance** | record adoption counts for direction, test ids and defaults in ADR-20's Consequences | **Amend.** Without them the Consequences describe a system in use that is not | **open** |
| **N5-05 D-J** | ditto | Nothing | strike ADR-20 Rollout §4 (P4-04) as Done, recording 18/15 | **Strike it** | **open** |
| **N5-05 D-K** | ditto | Nothing | restate ADR-20 §5's formatter migration as complete | **Restate.** A clean win the ADR under-claims | **open** |
| **N5-05 D-L** | ditto | Nothing; batched with D-N | should `DZ_THEME_KEY` move to contracts, **and** should `useDzTheme` stop throwing? | **Take both or neither** — one question about whether theme stops being special | **open** |
| **N5-05 D-M** | ditto | Nothing | deprecate the 15 `portalTo` props · keep permanently | **Keep and say so.** P4-04 was chartered to decide and did not | **open** |
| **N5-05 D-N** | ditto | Nothing | batch the three contracts-shape questions — N5-02 D1, D-L's two halves | **Batch.** One blast radius instead of three | **open** |
| **N5-05 D-P** | ditto | D-L | write ADR-09 before answering D-L; with D-E the ceiling goes 14 → 12 | **Recommended.** D-L amends an ADR that has no document | **open** |

> N5-05 numbered its decisions `D-A`…`D-P` and **skipped `D-O`** (a deliberate
> avoidance of `O`/`0`), which is why the packet says 15 and the letters run to P.

### 3.3 Policy, contracts and criteria

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N5-01 D4** 🟠 | [`N5-01-release-policy-handoff.md`](../../program-2026-09/reports/N5-01-release-policy-handoff.md) §10 | Three documents that contradict `VERSIONING.md` | reconcile ADR-19 §6 ("removal is a major"), `TOKENS.md` ("removed in the next major") and `Versioning.mdx`'s 1.x bump table | **Reconcile all three.** All are wrong for a 0.x library; none was edited. `VERSIONING.md` §7 is marked RESOLVED 2026-09-04 — **the three downstream documents were not updated with it** | **open** |
| **N5-01 D5** 🟠 | ditto | **Which of the 15 criteria are 1.0 exit criteria** | [`1-0-exit-criteria-2026-09.md`](../../program-2026-09/reports/1-0-exit-criteria-2026-09.md) — 15 criteria, 4 new, each measured | Decide which are exit criteria and which are post-1.0. **This is the meta-decision behind C11/C12/C15** | **open** |
| **N5-01 D7** 🟢 | ditto | Blocked on N5-01 D1 | was `@dzup-ui/mcp` meant to be under `validate:changelog`? It is a public package governed as one everywhere else | Resolve as part of D1 — it is the same regex | **open, blocked** |
| **N5-02 D1** 🟠 | [`N5-02-aria-prop-gaps-handoff.md`](../../program-2026-09/reports/N5-02-aria-prop-gaps-handoff.md) §7 | The contracts base-prop shape | should `ariaInvalid` move from `BaseAccessibilityProps` to `BaseValidationProps`? | **Batch with N5-05 D-N.** The *addition* shipped (TASK-R5-O1); **the removal is D10** and touches 30 measured components | **partially taken** — additive half shipped; removal open as **D10** |
| **N5-02 D5** 🟠 | ditto | `VERSIONING.md` §3/§4 reading | does §3's removal path owe §4's one-minor-series deprecation window? | Shipped on the reading that §4 governs *deprecated* APIs and these were *inert*. **A policy reading, and it is the owner's** | **open** |
| **S1-D1** 🟠 | [`N2-S1-anatomy-rollout-handoff.md`](../../program-2026-09/reports/N2-S1-anatomy-rollout-handoff.md) §9 | ADR-19 §3's vocabulary | 13 declared part names sit outside ADR-19 §3's shared vocabulary | **Fold in at acceptance (N5-05 D-F).** Renaming a shipped part name is breaking | **open** |
| **S1-D2** / **S1-D6** 🟠 | ditto | Nothing | does a part need a scope marker? `data-scope`: adopt · defer · refuse | **Defer** (N5-05 D-A agrees) | **open** |
| **S1-D3** 🟠 | ditto | `validate:rtl` completeness | close the regex gap — 14 measured sites, `DzDialog`'s pinned close control a real defect, 8 deliberate FAB positions needing `rtl-physical-ok` | Close it; the centring idiom needs an explicit allowance | **open** |
| **S1-D5** 🟠 | ditto | Anatomy conformance | should every declaring component call `expectAnatomy`? `DzFileUpload`'s declaration was false for eight days | **Yes** — a declaration nothing asserts is the defect class this found | **open** |
| **S1-D7** 🟠 | ditto | Ledger trust | re-verify `validate:all` from a clean shell and reconcile the ledger — three N2 tasks reported exit 0 over a stale `quality-matrix.json` | Done repeatedly since; **the underlying lesson is the S1-F10 lane rule**, now in `<repo_conventions>` | **open** (informational) |

### 3.4 Further 1.0-gating decisions from the 09-04 sequence

| Id | Raised by | Decision (what it gates is stated in the text) | Options / recommendation (source) | State |
|---|---|---|---|---|
| **D1** | [`TASK-R3-O1`](./TASK-R3-O1-handoff.md) | Schedule Pro TASK-R1-P4 so TASK-R3-O1 can complete — the OSS half of criterion C7 is already built and idle for want of one JSON file | (a) run it now and resume immediately · (b) defer C7 to the next cycle — **rec. (a)**, cost is one generator run in the Pro repo | open |
| **D12** 🟢 | [`TASK-R5-O1`](./TASK-R5-O1-handoff.md) | ADR-19 §4's recipe-attribute clause (`data-size/variant/tone/density/orientation`) is now honestly marked **unenforced** — no gate exists and the gap is **unmeasured**; the ADR's own "most of them" was never c… | (a) build it with the `data-scope` emitter as one `useAnatomy()` attribute bag · (b) build the reporter alone now, defer the emitter · (c) drop the clause and stop calling the attributes public — **rec. (b) th… | open |
| **D28** 🟠 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **TASK-R5-O3's `<success_criteria>` "ADR-20 divergences 9 → ≤ 1" is unreachable by any agent.** Five of the nine (D20-5…D20-9) are **amend-ADR or open-question** rows whose only action is editing ADR-20 and ac… | (a) re-state the criterion as "the four fix-code divergences", leaving the amend-ADR halves to TASK-R0-O2 where ADR acceptance already lives · (b) authorise the packet to edit ADR-20 · (c) leave the criterion… | open |
| **D122** 🔴 | `'gap' — there is no 'open'. Written against the shape it intends, it returns 0 and a fresh agent records [x] found-done while three surfaces sit open. Check 4 is the same class: grep -n 'target' packages/core/vite.config. matches two lines of English prose in a doc comment, so piping it to head -1 "finds" a build target in a repository that declares none` | **A fourth task's `<done_check>` is defective, and this one fails in the dangerous direction.** R2-O5's check 1 (`d.filter(x => x.criterion === '2.5.7' && x.state === 'open')`) is wrong three times over: the J… | TASK-R2-O5 | open |

### 3.5 The R0 owner packets' own 1.0-gating decisions (2026-09-22)

Appended by the 2026-09-22 fix pass; see the note at §2.5 for why they were missing.

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D181** 🔴 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | ADR-20 acceptance (**R0-O2**); criterion **C10**; the Node-floor decision (**D160**/**D176**). **The ADR-20 §4 amendment has no owner.** Both `N5-04 D3` and `D176` record that `getTextInfo()` needs Node 24, not 22.13 — so ADR-20 §4 and its "Alternatives considered" are wrong **whichever Node floor is chosen**. TASK-R0-O2's scope as written covers ADR-18/19/20 *acceptance*, not this correction | (a) widen TASK-R0-O2 to carry the §4 correction · (b) a separate amendment packet · (c) accept the ADRs with a known-wrong §4 and correct later | **(a)** — accepting an ADR whose §4 is known wrong makes the acceptance itself unciteable, and the correction is two sentences. **(c) rejected outright.** Executed as ADR-20 **A8.1** by TASK-R0-O2, so the *text* is corrected; the decision that remains is whether that discharges the amendment or a separate packet still owes it | **decided 2026-09-26 — (a).** The §4 correction is carried inside the TASK-R0-O2 acceptance. Recorded by [`DZUP-UI-OWNER-DECISIONS-20260926-R1`](../../qa/owner-decisions-2026-09-26/ADMISSION.md) |
| **D188** 🔴 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | criterion **C1**; `maxProposedCitedFromCode`; the 1.0 memo. **Are the three acceptances one decision or three, and in what order?** ADR-19 and ADR-20 have no unmet precondition but the signature. **ADR-18 does** — its Decision 1 is disputed (D160/D176/N5-04 D3) and its Decision 3 is measurably false on the 20.x branch | (a) sign 19 and 20 now, hold 18 behind the floor decision · (b) hold all three until the floor is decided, so C1 moves once · (c) sign all three and amend 18 after · (d) none yet | **(a).** C1 is *"ADR-18, ADR-19, ADR-20 carry `Accepted` in the ADR file itself"* and it can honestly go 0/3 → 2/3 today. (c) is what D181 rejects in a different costume — signing a document whose Decision 3 the tree falsifies. **Accepting an ADR is an owner act: it flips the document's own `Status:` line and `maxProposedCitedFromCode` must fall in the same change** (§3.3) | **open** |

---

## 4. Evidence — gates a claim the repository makes about itself

### 4.1 The AT programme — one decision gates 534 cells

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N1-O4 #1** 🔴 / **D112** | [`N1-O4-at-matrix-handoff.md`](../../program-2026-09/reports/N1-O4-at-matrix-handoff.md) §5.4, §8c · [`TASK-R2-O2-handoff.md`](./TASK-R2-O2-handoff.md) · [`TASK-R2-O2-wave-1-runbook.md`](./TASK-R2-O2-wave-1-runbook.md) | **Criterion C9; the whole AT programme.** `0/534` today. Wave 1 = 44 cells ≈ 21 h, Windows only, everything else ready. JAWS demo mode is 40 min/boot | (a) internal tester · (b) contract tester · (c) defer | **This is the gate.** Name a tester and adopt a cadence; everything else is downstream. **An agent never fills a manual result cell** | **open** |
| **N1-O4 #2** 🔴 | [`N1-O4-at-matrix-handoff.md`](../../program-2026-09/reports/N1-O4-at-matrix-handoff.md) §5.1 Blocker 2 | Wave 1's honesty | fix `at-manual` cell resolution — **a failed run currently publishes as `pass`** | **Fix before any execution.** Executing first *"converts honest emptiness into published overstatement"* | **decided 2026-09-2x** — `.changeset/an-at-matrix-that-cannot-turn-a-failed-run-into-a-pass.md` ships the fix |
| **D113** | [`TASK-R2-O2-handoff.md`](./TASK-R2-O2-handoff.md) | Cadence after wave 1 | (a) event-driven ≈16 h/quarter · (b) one full sweep per release · (c) ad hoc | **(a)** — a cell goes stale when its component changes, and this repository moves daily | **open** |
| **D114** 🔴 | ditto | **Which C9 claim 1.0 makes.** 0 of 132 today | (1) narrow to Tier A/B · (2) narrow the pair set · (3) move C9 post-1.0 · (4) claim the tier honestly | **One of the three honest options must be picked** — the current claim is unmeetable | **open** |
| **D115** | ditto | Tier B scripting — 67 components × 1 pairing = **67 cells, none scripted** | (a) script Tier B (~3–4 d) · (b) leave unscripted | Only worth doing **once wave 1 proves the format survives a real tester** | **open** |
| **D109** / **D110** / **D111** | [`at-pairing-decision-packet.md`](./at-pairing-decision-packet.md) · [`TASK-R2-O2-handoff.md`](./TASK-R2-O2-handoff.md) | Cross-repo AT vocabulary | one pairing vocabulary across doc 06, OSS and Pro · is `forced-colors` a pairing? · which tier owes `jaws-chrome`? | **Unify; drop `forced-colors` (the 18-project browser matrix already measures it); `jaws-chrome` is Tier C as OSS ships** | **open** (cross-repo) |

### 4.2 Browser, visual and performance evidence

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N1-O2 D1** 🔴 / **D141** | [`N1-O2-firefox-webkit-handoff.md`](../../program-2026-09/reports/N1-O2-firefox-webkit-handoff.md) §8 | Every browser claim. `e2e/matrix/browser-evidence.json` is **not in git**, so the degradation gate compares against nothing | persist the browser record in git · keep it ephemeral | **Persist it.** A gate whose baseline is untracked provides zero protection | **open** |
| **N1-O2 D2** | ditto | Ledger shape | the `46` is proven cross-engine (FF 46/46, WebKit 44/46); `known-failures.json` still says `measuredOn: "chromium only"` | Update the ledger to match the measurement | **open** |
| **N1-O2 D3** / **N1-O3 D2** | ditto · [`N1-O3-wcag-fixes-handoff.md`](../../program-2026-09/reports/N1-O3-wcag-fixes-handoff.md) §9.2 | Ledger history | `DzLightbox:touch` was recorded as measurement instability; it was a **component defect** that made every `DzLightbox` render unstyled | Carry the correction into the ledger's own history | **open** |
| **N1-O2 D4** | ditto | One engine divergence | `DzOrderList` reflow sits 1 px from tolerance | Cheapest of the 18 reflow fixes | **open** |
| **N1-O2 D5** | ditto | Upstream tracking | the WebKit `display:none` animation divergence has no verified upstream issue (`upstreamIssue: null`) | File it or record why not | **open** |
| **N1-O2 D6** | ditto | SSR correctness | `DzOrderList` binds `:ariaLabel`; `renderToString` emits `<ul arialabel="…">` — unnamed until hydration | A defect class **no browser matrix can see** | **open** |
| **N1-O2 D7** 🟠 | ditto | Whether WebKit evidence counts | Playwright's WebKit on Windows is **not Safari** (WinCairo port, macOS UA) | **Decide whether WebKit-on-Windows is acceptable for the Baseline claim.** Same shape as N1-O5 O5-5 | **open** |
| **N1-O2 D8** / **N1-O3 D9** 🔴 | ditto | **All release evidence from those runs** | the runs were worktree-dirty and therefore inadmissible | **Commit the tree and re-run.** 278 uncommitted paths at `527dbd1` — still true today | **open** |
| **N1-O3 D1** 🔴 | [`N1-O3-wcag-fixes-handoff.md`](../../program-2026-09/reports/N1-O3-wcag-fixes-handoff.md) §9.2 | **WCAG 2.5.7**; criterion C8 | pane resize and column resize have no single-pointer non-drag path; no APG precedent | **TASK-R2-O5's packet.** A design decision, not an engineering one | **open** |
| **N1-O3 D3** | ditto | Ledger trust | two of the 46 measurements were never reproducible at HEAD and the ledger is now empty | Record the irreproducibility rather than the measurement | **open** |
| **N1-O3 D4** 🔴 | ditto | `validate:tokens` completeness | **31 `--dz-*` references named a token that does not exist, 26 silently dropping a declaration, every gate green for months** | Close the gap — this is the defect class that motivates the token gates in R5-O7 | **open** |
| **N1-O3 D5** 🔴 | ditto | `validate:tv-slots` | a `tv()` slot bound without calling it is invisible to every gate; `DzLightbox` shipped ten | **Gate built** (`validate:tv-slots` is a chain link); the *decision* is whether the ceiling is 0 | **largely executed** — gate exists at `527dbd1` |
| **N1-O3 D6** | ditto | The chromium six-condition record | the 1,056/1,056 run wrote no JSON, deliberately, to protect the 2026-08-25 file | Same as N1-O2 D1 — decide how the record is persisted | **open** |
| **N1-O3 D7** 🟠 / **N1-O6 D7** | ditto · [`N1-O6-visual-regression-handoff.md`](../../program-2026-09/reports/N1-O6-visual-regression-handoff.md) §7.2 | Design sign-off | **six unreviewed visible geometry changes** (V1–V4; V5 tree rows 21 → 33 px; V6 `DzLightbox` unstyled → styled) | **A designer must review them.** The visual lane that would hold them now exists | **open** |
| **N1-O6 D1** 🟠 | [`N1-O6-visual-regression-handoff.md`](../../program-2026-09/reports/N1-O6-visual-regression-handoff.md) §7.2 | Chromatic | **finish it or retire it.** Workflow, dependency, TurboSnap and light/dark modes — and no token, so it runs and does nothing | **Either, but not the status quo.** A configured-but-dead visual gate is worse than none. Packet: [`TASK-R2-O6-chromatic-finish-or-retire.md`](./TASK-R2-O6-chromatic-finish-or-retire.md) | **open** |
| **N1-O6 D2** 🟠 | ditto | Whether the per-component visual lane can gate CI | **the authoritative baseline platform.** 34 committed baselines are `chromium-linux`; this host writes `chromium-win32` — a different file, never compared | **Pick Linux** (CI is `ubuntu-latest`) and run one accept pass there | **open** |
| **N1-O6 D3** 🟠 | ditto | When visual regressions can fail a build | `ci.yml:528-533` is `continue-on-error: true` | Decide when the lane becomes blocking; sequence after D1 and D2 | **open** |
| **N1-O6 D4** | ditto | What all 144 components owe | promote `visual-baseline` to an `EvidenceKind`? | Packet: [`TASK-R2-O6-evidence-kind-proposal.md`](./TASK-R2-O6-evidence-kind-proposal.md) | **open** |
| **N1-O6 D5** | ditto | Baseline trust | the 34 pre-existing baselines are **grandfathered, not reviewed** | Recorded as-is with true capture commits; decide whether a review pass is owed | **open** |
| **N5-03 D1** 🟠 | [`N5-03-toolchain-currency-handoff.md`](../../program-2026-09/reports/N5-03-toolchain-currency-handoff.md) §10 | `validate:all` link 16 | (a) regenerate — artifact becomes truthful · (b) re-capture the 7 visual baselines · (c) leave red | **(a) now, (b) scheduled.** *"the committed artifact claims more evidence than exists"* — an overstating artifact is worse than a red gate | **open** |

### 4.3 Security and provenance

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N1-O5 O5-1** 🔴 | [`N1-O5-security-corpus-handoff.md`](../../program-2026-09/reports/N1-O5-security-corpus-handoff.md) §7 | Six navigation URL sinks; TASK-R2-O4 | the allowlist, the rejection shape (no `href` vs rewritten `#`) and the escape hatch | Closing it is **breaking** — `javascript:void(0)` renders today and would stop. Changeset `six-navigation-components-…` is at `minor`, correctly | **open** (shipped at minor; the *policy* shape is still owed) |
| **N1-O5 O5-2** 🟠 | ditto | Matrix expressiveness | `securityBoundary` is **one value per component**, so `DzQRCode` cannot declare both `payload` and `url` | Make it set-valued — changes what an unknown number of components owe | **open** |
| **N1-O5 O5-3** 🟠 | ditto | Matrix rows | compound sub-parts are sinks but not matrix rows | Either they become rows (144 → more, owing evidence), or the matrix documents that a parent's boundary covers its parts | **open** |
| **N1-O5 O5-4** 🔴 | ditto | Strict-CSP consumers | **78 components emit a static `style` attribute a strict CSP blocks; 38 bind a dynamic `:style`** | Needs a repo-wide answer (custom properties from a stylesheet, or an accepted `'unsafe-hashes'`) | **open** |
| **N1-O5 O5-5** 🟠 | ditto | Every CSP claim | **no browser has verified any of them** — jsdom does not enforce CSP | A Playwright lane serving a real header is owed. Same shape as N1-O2 D7 | **open** |
| **N1-O5 O5-6** | ditto | Generator inputs | the capability-matrix generator learned `packages/core/security/coverage.json` | Confirm the input and its failure mode | **open** |
| **N0-05 D1** 🔴 / **D145** | [`N0-05-rebind-handoff.md`](../../program-2026-09/reports/N0-05-rebind-handoff.md) §8 · [`TASK-R1-O1-handoff.md`](./TASK-R1-O1-handoff.md) §6 Decision 1 | Every `sourceCommit` in every artifact | **`sourceCommit` is off by one by construction and always will be** | The *copied-binding* half is fixed (one shared `headCommit()`); the off-by-one is inherent. **Retire the `<done_check>` line comparing `sourceCommit` to `git rev-parse HEAD` — it can only pass in an uncommitted tree** | **partially fixed; open** |
| **N0-05 D2** 🔴 | ditto | **The barrel.** This is **D158** and **N2-A1 D2** under an older id | regenerating the barrel drops 5 composables and adds 2 | See **D158** (§2.1) — the same measurement, now with R1-O3's API diff behind it | **superseded by D158** |
| **N0-05 D3** | ditto | 11 `perf-baseline` cells | legitimately stale, cannot be re-bound by regeneration | Re-capture or accept | **open** |
| **N0-05 D4** | ditto | Ratchet accuracy | the anatomy ratchet is documented as 137 in four places; the checked-in ceiling is 136 | Correct the four documents | **open** |
| **N0-05 D5** 🔴 | ditto | The chromium record | `test-results/matrix-report.json` is git-ignored; if missing, every `browser-matrix` cell is silently marked | Same family as N1-O2 D1 / D141 | **open** |
| **N2-D2 D2-D1** 🔴 | [`N2-D2-evidence-pages-handoff.md`](../../program-2026-09/reports/N2-D2-evidence-pages-handoff.md) §12 | **Criterion C13**; the Baseline claim | adopt a browser-support target, or keep publishing none. **No `browserslist`, no build `target`, no Baseline tier anywhere** | Adopting **Baseline Widely Available** obliges the library to refuse features below it — that is the decision | **open** |
| **N2-D2 D2-D2** | ditto | `non-drag-alternative` cells | the cell cites SC 2.5.7, measures SC 2.1.1, and disagrees with the audit on 4 of 9 surfaces | Fixing the note is one line; fixing the measurement is N1-O3 D1 | **open** |
| **N2-D2 D2-D3** | ditto | Criterion coverage | **five WCAG criteria apply to no component**, incl. 2.5.2 and 3.3.3 | Widen the assignment, or record why the library cannot fail them | **open** |
| **N2-D2 D2-D4** 🟠 | ditto | Three independent lanes | **`CellState` has no `fail` value** — a failed AT run reads `pass`, a 2.5.7 failure reads `present`, a measured corpus failure reads `present` | Add it. Shipped in part by `.changeset/an-at-matrix-that-cannot-turn-a-failed-run-into-a-pass.md` | **partially shipped; open** |
| **N2-D2 D2-D5** | ditto | Site information architecture | should `/evidence/` be the site's front door? | A positioning decision | **open** |
| **N2-D2 D2-D6** | ditto | Public honesty | `/evidence/accessibility` publicly states that the capability matrix would summarise an all-failed AT run as a pass | **It is the honest thing and also a public description of a defect.** Fix the defect (N1-O4 #2) and the question dissolves | **open** |
| **N2-D2 D2-D7** 🔴 | ditto | Deployment | same as D1-D2/D165, **with more at stake** — the evidence layer exists only on one developer's disk | Sequence behind A4-D1 (D172) | **open** |

### 4.4 Further evidence decisions from the 09-04 sequence

| Id | Raised by | Decision (what it gates is stated in the text) | Options / recommendation (source) | State |
|---|---|---|---|---|
| **D7** 🟠 | [`TASK-R3-O2`](./TASK-R3-O2-handoff.md) | Pro's `measureHtmlDepth` is now duplicated — Core owns the ceiling, Pro still carries its copy (ported with provenance; `<scope>` forbade touching Pro) | (a) Pro TASK-R5-P2 imports Core's and deletes its own (Pro already imports `@dzup-ui/core` in 75 files) · (b) keep both plus a cross-repo agreement test — **rec. (a)** | open |
| **D9** 🟠 | [`TASK-R5-O1`](./TASK-R5-O1-handoff.md) | The six-slot ordering statement is now emitted in **both** `base.css` and `tokens.css`. CSS registers a layer at its first appearance, so a single statement left the shipped order dependent on which sheet a bu… | (a) keep both, as shipped · (b) `base.css` only, accepting `dz-tokens, dz-reset, …` when tokens.css loads first · (c) generate both from one shared constant so they cannot drift — **rec. (a) now, (c) as cheap… | open |
| **D13** 🟠 | [`TASK-R5-O1`](./TASK-R5-O1-handoff.md) | **The program's known-red list is stale in two places.** Measured at `99b963a`: `eslint e2e/` is **53 errors, not 9** (44 `style/no-multiple-empty-lines` + 22 markdown findings, almost all in `e2e/at-matrix/sc… | (a) correct the list from these measurements · (b) re-measure the whole known-red list first — **rec. (b)**: a known-red list wrong in two of five places is the list that will be used to wave through a real re… | open |
| **D16** 🟠 | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **Five components declare more than `root` and carry no `ui` prop of their own.** `DzDropdownMenu`, `DzContextMenu`, `DzPopover`, `DzTooltip`, `DzSheet` render reka's `*Root` around a bare slot — no element at… | (a) ship as-is, reading the criterion as *"`ui` wherever the component renders the node"* · (b) add `ui` on each shell and forward it through the existing family context · (c) full `ui` on every compound part… | open |
| **D17** 🟢 | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **Seven part names the ADR-19 vocabulary does not have**, so those nodes ship unaddressable: `media`/`image` (`DzImageCard`, `DzImageComparison`, `DzLightbox` — 3 components, 2 families), `value` + `trend` (`D… | (a) fold `media` into `ANATOMY_PART_VOCABULARY` — it passes §3's "names a job that recurs" test on measured evidence — and record the other five in `ANATOMY_PART_EXTENSIONS` as `reviewed` · (b) fold all seven… | open |
| **D19** 🟢 | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **Two declared deviations in how the aggregate was run.** `validate:all` ran end-to-end **twice** (after the fifth slice and after D11 moved the manifest) rather than after each of five slices; and because `ya… | (a) accept both — link-by-link is *stronger* evidence, reporting all 38 codes rather than stopping at the first failure, and the S1-F10 failure was reporting green over a **stale artifact**, which a full chain… | open |
| **D20** 🔴✅ | [`TASK-R5-O2 session 2`](./TASK-R5-O2-handoff.md) | **CLOSED — option (a) is what happened.** The owner took D15, session 3 declared `forms`, the ratchet reached 41, and **all three blocked checks are closed** (`DzColorModeToggle` `switch`, `DzDataGrid selectab… | (a) take D15 now — its recommended option (d) needs **no new mechanism**, `validate:anatomy-parts` rule 1 already implements it, only the seven host declarations — and run `forms` next, restoring 44 and closin… | open |
| **D21** 🟠 | [`TASK-R5-O2 session 2`](./TASK-R5-O2-handoff.md) | **`rtl:translate-x-full` is the first Tailwind *variant* this library relies on for correctness, and no browser has rendered it.** F-C2 fixes `DzSidebar`'s RTL drawer with `-translate-x-full rtl:translate-x-fu… | (a) accept and cover it when the visual lane runs (the `e2e/visual` RTL condition already exists among the 18 Playwright projects) · (b) replace the pair with a logical transform driven from a `--dz-sidebar-*`… | open |
| **D23** 🟢 | [`TASK-R5-O2 session 3`](./TASK-R5-O2-handoff.md) | **Three part names the vocabulary still does not have, and one of them now recurs.** `tag` — the removable selected-value chip inside a control's field — is wanted by **`DzMultiSelect` and `DzTreeSelect`**, wh… | (a) fold `tag`, `track` and `range` into `ANATOMY_PART_VOCABULARY` alongside D17's `media`, then declare the five nodes · (b) fold in `tag` only · (c) record all three as `reviewed` extensions and leave the no… | open |
| **D31** 🟠 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **`DzButtonGroup` is excluded from the defaults slice on purpose, and the exclusion is a precedence argument rather than a backlog item.** Its `size`/`variant`/`tone` default to `undefined` *so that they can b… | (a) leave it unadopted and record the reason at the code so a later sweep does not "finish" it — **rec.** · (b) adopt it and accept the inversion for hosts that write both keys · (c) give `resolve` a per-compo… | open |
| **D34** 🟠✅ | [`TASK-R5-O5`](./TASK-R5-O5-handoff.md) | **TAKEN as (b) — the keyboard contract is a field on `ComponentAnatomy`, not a new file type.** The `<done_check>` suggested `Dz{Name}.keyboard.ts` "or the chosen declaration file". A second per-component decl… | (a) `Dz{Name}.keyboard.ts` per component — rejected, a second mechanism · **(b) a `keyboard` field on `ComponentAnatomy` — TAKEN** · (c) a central catalogue keyed by component — rejected, the hand-kept list ev… | open — the owner need only confirm the consequence |
| **D37** 🟠 | [`TASK-R5-O5`](./TASK-R5-O5-handoff.md) | **`@intent` is 144/144 undeclared and is the one section of the ten a machine cannot produce.** Page-contract section 1 is "what is this for, and when should you reach for something else". The field is extract… | (a) leave the ratchet at 144 and let it fall as components are touched — **taken, as the only option available to an agent** · **(b) commission an authoring pass over the 144 headers** — roughly a day, and the… | open |
| **D39** 🟠 | [`TASK-R5-O6`](./TASK-R5-O6-handoff.md) | **`yarn test:contracts` could not run on Windows at all; fixed, please confirm.** The script shell-expands `packages/**/*.contract.spec.ts` to **9,692** argv characters against cmd.exe's **8,191** limit. **Pre… | (a) **keep the vitest filter substring — taken**: platform-independent, coverage-identical, verified at 150 files / 1,575 tests exit 0 · (b) revert and have Windows contributors run `npx vitest run .contract.s… | open |
| **D41** 🟢 | [`TASK-R5-O6`](./TASK-R5-O6-handoff.md) | **Teach `validate:anatomy-parts` to credit a delegate's emissions — D26's second half.** With `delegatesTo` now declared, the validator could credit `DzPersonaSelector` with `DzCombobox`'s emissions and let it… | (a) **leave it — taken here**: the declaration was the blocking half, and the validator change moves `optionalParts`, a number another packet ratchets · (b) teach the validator, in the packet that owns that ra… | open |
| **D51** 🟠 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **A3-D3 SETTLED — `DzAccordion` passes a flat `DzAccordionRootProps` to `defineProps`; the exported union is untouched.** The measurement that makes it safe rather than a loosening: **the union was destroying… | **(a) flat interface + assertion — TAKEN** · (b) resolve the union inside the extractor — rejected: it means re-implementing props *and* events *and* slots *and* exposed for one component, the second extractio… | open |
| **D54** 🟢 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **`DzDataGridBodySlots` describes a slot the component does not render and omits the one it does.** `DzDataGrid.types.ts:292` declares `default?: () => unknown`; `DzDataGridBody.vue` renders only `<slot name="… | (a) **leave the stale interface — taken**, the contradiction now on record; correcting an exported type is an API change this task's `<scope>` forbids · (b) rewrite `DzDataGridBodySlots` to declare `cell` and… | open |
| **D55** 🟢 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **`validate:rtl` reads prose as CSS utilities.** The gate flags any `right-*`/`left-*` token in a component declaring `mirrors: 'layout'` and does not exclude comment ranges: the phrase *"opens on right-click"… | (a) **reword the comment — taken**, zero risk, the gate stays as strict as it is · (b) make the scanner skip comment ranges — correct, but it changes what an existing ratcheted gate sees and belongs to the RTL… | open |
| **D81** 🟢 | [`TASK-R5-O3 (continuation 2026-09-17)`](./TASK-R5-O3-handoff.md) | **The motion-policy browser lane is a second spec on the existing reduced-motion projects, not a seventh condition.** `e2e/matrix/motion-policy.spec.ts` runs only on `matrix-{chromium,firefox,webkit}-reduced-m… | (a) keep as built — **rec.**: no new matrix name for the capability matrix to learn, and no ~210 skipped cells in the other projects · (b) a seventh `matrix-*-motion-policy` project per engine · (c) fold it in… | open |
| **D84** 🟢 | [`TASK-R5-O9 (phase 1)`](./TASK-R5-O9-handoff.md) | **"Vitest 4 browser mode for the memo's lanes" = `apps/storybook/vitest.config.ts` and nothing else.** That is the only browser-mode config in the repository (`provider: 'playwright'`, chromium, headless); the… | (a) scope phase 2's browser work to the storybook lane's `provider` factory — **rec.** · (b) add a browser project to the root config — refused for the memo's reason (it would invalidate every unit-level matur… | open |
| **D86** 🟢 | `head. Sixth instance of the pattern behind D42, D57, D66, D72 and D77` | **TASK-R5-O9's `<done_check>` cannot be trusted as written** — check 1 is a **false pass** (it matches the word "browser" inside a comment in `vitest.config.ts` and names `packages/core/vitest.config.*`, a pat… | TASK-R5-O9 (phase 1) | open |
| **D87** 🟢 | [`TASK-R5-O9 (phase 1)`](./TASK-R5-O9-handoff.md) | **The two build gates cannot serve as the byte comparison.** `validate:tree-shake` is **red at baseline** (DzInput/DzSelect/DzAlert bundles contain the sentinel `DzDataGrid`, probably because `i18n/messages.ts… | (a) use `TASK-R5-O9-baseline/dist-{files,summary}.tsv` (+ `hash-dist.mjs`) as the byte gate and treat both validators as "unchanged vs baseline" — **rec.** · (b) fix the substring sentinel and the CSS budget p… | open |
| **D88** 🟢 | [`TASK-R5-O9 (phase 1)`](./TASK-R5-O9-handoff.md) | **Probe P-612 — Storybook `addon-mcp` Vue parity: WATCH.** `@storybook/addon-mcp@0.7.0` installs against the repo's Storybook **10.5.1 with no upgrade** (`latest` 10.6.0 would need one) and registers 8 tools… | (a) keep `@dzup-ui/mcp` as the component-API surface and re-probe at Storybook ≥ 10.6, whose notes claim Vue manifest work — **rec.** · (b) adopt the addon now for story authoring only (dev toolset), accepting… | open |
| **D90** 🟢 | [`TASK-R5-O9 (phase 1)`](./TASK-R5-O9-handoff.md) | **No perf-harness config hash exists**, so the `<stop_conditions>` clause that names one is given a definition: the sha256 set recorded in `reports/TASK-R5-O9-baseline/perf-harness.sha256` (`perf-bench.spec.ts… | (a) adopt that definition for phases 2–3 — **rec.** · (b) add a real config hash to `baselines.json` (TASK-R2-O7's lane, not a toolchain change) | open |
| **D92** 🟠 | [`TASK-R5-O9 (phase 2)`](./TASK-R5-O9-handoff.md) | **The coverage ratchet is unenforced whenever any test fails — so, on this tree, always** (F-8). `coverage.reportOnFailure` defaults to `false` and `vitest.config.ts` never sets it, so `Vitest.reportCoverage()… | (a) set `reportOnFailure: true` in `vitest.config.ts` so the ratchet is measured and printed even on a red suite — **rec.**, folded into **TASK-R1-O1**, which already owns that file's truthfulness · (b) leave… | open |
| **D94** 🟢 | [`TASK-R5-O9 (phase 2)`](./TASK-R5-O9-handoff.md) | **A zero-occurrence grep is not major-upgrade clearance.** Phase 1's pre-flight listed `experimentalAstAwareRemapping: 0` beside genuinely removed APIs and read the zero as "nothing to migrate". It proved only… | (a) a major-version pre-flight greps for removed APIs **and** separately diffs the *defaults* of every option the repo relies on implicitly; the cheap form is to run the one gate that reads each default before… | open |
| **D97** 🟢 | [`TASK-R5-O9 (phase 3)`](./TASK-R5-O9-handoff.md) | **D91 should not be signed off a single Vitest-4 reading.** `vitest.config.ts`'s own comment above these thresholds says the percentages are *"NOT reproducible to the decimal"*, that the function **total** mov… | (a) before taking D91, run the identical `--coverage.reportOnFailure=true` command at least once more **on Vitest 4**, then set each bar to `floor(min observed) − 1` per that file's own rule — **rec.**; the Vi… | open |
| **D99** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **D10 -- `DzTreeSelect` declares one focus mechanism and operates another.** The trigger publishes the active node via `aria-activedescendant` (its own comment at `DzTreeSelect.vue:504` says "without moving DO… | (a) keep focus on the trigger by preventing the popover's `open-auto-focus` -- makes the declaration true, but with `filter` on the panel's searchbox then needs an extra Tab, changing the typing flow for every… | open |
| **D102** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **The mandatory regenerated artifact chain overlaps TASK-R1-O1.** Exporting one type (`UseFocusTrapOptions`) turned `validate:ownership` red, so ownership -> quality -> capability -> component-meta -> llms ->… | (a) **keep the regeneration in this packet's diff -- taken**; leaving a generated artifact stale to keep a diff small is the failure mode `<generated_authority>` exists to prevent · (b) revert the artifacts an… | open |
| **D103** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **11 docs pages now cite the new ARIA-casing SSR spec as their SSR evidence** instead of `ssr-smoke.spec.ts`. `capability-matrix.json` records BOTH artifacts; the docs renderer prints only the first, and the n… | (a) accept -- taken · (b) **have the renderer list every artifact in the cell** -- the JSON already carries them · (c) rename the file so it sorts last, which encodes a display rule in a filename -- **rec. (b)… | open |
| **D107** 🟠 | [`TASK-R2-O4`](./TASK-R2-O4-handoff.md) | **The parent-covers rule is written but not enforced.** `BOUNDARY_COVERS_COMPOUND_PARTS` records it (a parent's declared boundary covers sinks carried by its own compound sub-parts; `boundary-bindings.ts` name… | (a) extend `validate:quality-tiers` to cross-check `boundary-bindings.ts` against the manifest's `compound-part` entries · (b) leave it as documented prose · (c) make the 65 sub-parts matrix rows — **rec. (a)*… | open |
| **D108** 🟢 | [`TASK-R2-O4`](./TASK-R2-O4-handoff.md) | **TASK-R2-O4's own `<done_check>` 2 reads a key that does not exist** (`packages/core/security/coverage.json.deviations`). The deviation count lives in `security-deviations.json` as `ceiling` plus the triples… | (a) correct the prompt to read `security-deviations.json`'s `ceiling` · (b) leave it — **rec. (a)**, the same class as D2's two checks that can never pass | open |
| **D109** 🟠 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **One AT pairing vocabulary across doc 06, OSS and Pro.** Three tables and no two alike: doc 06 names five (no JAWS), OSS six (`AT_PAIRS`, adds `jaws-chrome`, splits `voiceover-ios`, drops `forced-colors`), Pr… | (a) adopt the OSS six everywhere · (b) adopt doc 06's five and delete JAWS from both implementations · (c) accept divergence and stop claiming the tables join — **rec. (a)**: both implementations independently… | open |
| **D110** 🟢 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Is `forced-colors` an AT pairing?** Pro carries it as one and Pro's own comment concedes *"a mode, not a screen reader, and is tracked here anyway"*. OSS does not carry it | (a) drop it — it is already one of the six conditions in the 18-project browser matrix, measured in 3 engines, so carrying it as an AT cell double-counts one piece of evidence under two kinds · (b) re-kind it… | open |
| **D111** 🟠 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Which tier owes `jaws-chrome`?** OSS's new `TIER_AT_PAIR_INCREMENT` puts it at Tier C; Pro's `docs/qa/at/README.md` §3 puts it at Tier D. The two ladders agree at B and disagree at C and D | (a) C, as OSS now ships · (b) D, as Pro ships · (c) each repository keeps its own — **rec. (a)**: wave 1 is `nvda-firefox` + `jaws-chrome` in *both* programmes' plans, and that wave only means something if the… | open |
| **D112** 🔴 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Who runs wave 1, and is JAWS licensed?** **This is the gate — the only thing between the repository and the first non-zero value its AT ratchet has ever had.** 44 cells, 248 step-runs, ~21 h, **Windows only*… | (a) name an internal tester and buy a JAWS seat (~$95/yr home, ~$1,300 professional) · (b) internal tester on JAWS demo mode (40 min/boot — costs reboots, not fidelity) · (c) contract an external accessibility… | open |
| **D113** 🟠 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Cadence after wave 1.** A cell goes stale when its component changes and this repository moves daily, so a one-off wave decays into a pass about different code | (a) event-driven — re-run a component's required pairings when its cell goes stale (≈16 h/quarter) · (b) calendar — one full six-pair sweep per release train (~75 h) · (c) both — **rec. (c)**: event-driven alo… | open |
| **D114** 🔴 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Which C9 option does 1.0 claim?** 0 of 132 Tier C/D cells executed; C9 is the only 1.0 criterion no engineering shortens. The tier ladder this task landed adds a fourth option the exit memo did not have | (1) narrow the claim to Tier A/B, Tier C/D stay `experimental` (~25 h) · (2) narrow the pair set to what ran (wave 1, ~21 h) · (3) move C9 post-1.0 and say so in the release notes (0 h) · (4) **new** — claim t… | open |
| **D116** 🟠 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Done-check clauses that cannot pass as written — three tasks running.** R2-O3's check 4 was substring noise, R2-O4's check 2 read a key that does not exist (D108), and R2-O2's checks 1 and 4 name a file glob… | (a) correct the clauses in the three task files and adopt a convention that a done-check is executed once against the repo before it ships · (b) treat done-checks as advisory and always evaluate intent · (c) l… | open |
| **D120** 🟢 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | **`DzPasswordInput`: the reveal toggle joins the tab order, and `autocomplete` becomes a prop.** The toggle carried `tabindex="-1"`, so the only way to read back what you typed was a mouse (SC 2.1.1 on a contr… | (a) **both taken** · (b) keep the toggle out of the tab order — the pattern some libraries use to keep Tab moving field-to-field, at the cost of a keyboard user never reaching the control · (c) route `autocomp… | open |
| **D124** 🔴 | `head -1 “names the authoritative platform and matches the committed baselines”) greps the same file that declares the platform and compares the value with itself: win32, linux and solaris all pass, and the only failing state (no platform key) is already a hard failure of validate:visual-baselines. Two clauses in the same block fail the other way: check 1 reads git ls-files, which lists the index, so satisfying it means git add on a worktree carrying 427 paths of five other tasks' work — a repository mutation the prompt's own <authority> reserves to the owner; and check 4's first clause (git log -1 -- coverage.json) can only move when somebody commits. The artifact, the stamp and the gate all exist; the checks that would report them cannot be satisfied by the agent the prompt addresses. This is D108/D116/D122's fourth recurrence and the second one that publishes a false “done”` | **A fifth task's `<done_check>` is defective, and one clause is a pure tautology.** R2-O1's check 3 (`grep -rn '"platform"' e2e/visual/ | TASK-R2-O1 | open |
| **D125** 🔴 | [`TASK-R2-O1`](./TASK-R2-O1-handoff.md) | **The 2026-08-25 chromium record is GONE, and the thing that destroyed it was the task that was protecting it.** `test-results/matrix-report.json` (443 KB, MD5 `15b41393…92a3`) — N0-05's X3/D5, N1-O2's D1, the… | **Already remedied, exactly as N1-O2 D1 specified**: a tracked, schema-pinned ledger (`e2e/matrix/browser-evidence.json`) + a `validate:capability-matrix` gate that **fails** on `pass → unrun`/`fail`, proven b… | open (fix landed, D1 closable) |
| **D126** 🟠 | [`TASK-R2-O1`](./TASK-R2-O1-handoff.md) | **The authoritative visual platform is `linux`, and today's baselines are `win32`.** Decided and recorded by TASK-R2-O1 in `e2e/visual/visual-baselines.json` `scope.platformDecision`: the authoritative platfor… | (a) **one `yarn visual:accept` pass per snapshot on a linux host** (58 baselines, 29 snapshots × 2 themes), then flip `platform` to `linux` · (b) a CI job that captures and a human who accepts · (c) keep win32… | open |
| **D127** 🔴 | [`TASK-R2-O1`](./TASK-R2-O1-handoff.md) | **Commit the evidence — the one step of TASK-R2-O1 no agent can perform.** The task asks for a re-run “from the clean committed tree”. At `2d51eec` there is no clean tree: 420 uncommitted paths belonging to R2… | (a) **commit the tree, then re-run the three sweeps and `yarn generate:browser-evidence`** — the ledger is then clean-tree evidence and the degradation gate goes live (it is inert until HEAD carries a copy) ·… | open |
| **D128** 🟢 | [`TASK-R2-O1`](./TASK-R2-O1-handoff.md) | **Should the security corpus get a `corpus`-scoped `pass` cell?** The suite is green and now cites its commit (403/403, exit 0, `2d51eec` + dirty), but the four security cells stay `present` because `pass` mea… | (a) add one `corpus`-scoped cell per security kind, leaving the per-component cells `present` · (b) leave it — the run record in `coverage.json` is already citable — **rec. (b) for 1.0, (a) afterwards**: it ch… | open |
| **D129** 🟠 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | **The SC 2.5.7 browser lane depends on a third-party CDN.** `DzImageComparison`'s story loads both images from `https://picsum.photos/...`, and the component maps a press to a position through the ROOT'S BOX… | (a) **point the story at a repository-local fixture image** — deterministic, and it also takes a network call out of every story render · (b) keep the remote images and accept that this cell can never gate CI… | open |
| **D130** 🟠 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | **`yarn storybook:build` is over its size budget.** It ends `Storybook build 25.02 MB EXCEEDS budget 25 MB`, exit 1. TASK-R5-O9 measured **24.90 MB within** the same budget on the same script, so ~0.12 MB accu… | (a) **find and cut the growth** — `assets/jsx-*.js` is 4.14 MB and `DzVisuallyHidden.stories` is 2.09 MB, both worth reading before the budget is touched · (b) raise the budget with a recorded reason · (c) lea… | open |
| **D132** 🟠 | [`TASK-R2-O7`](./TASK-R2-O7-handoff.md) | **The downward-only ratchet is documented, unit-tested, and not enforced at the point where baselines are written.** `statistics.ts` exports `mayRatchet()` — refuses to move a threshold unless the fresh median… | (a) have `capture-baselines.ts` read the committed file and pass each metric through `mayRatchet()`, writing the **recorded** threshold when the ratchet is refused and printing a per-metric line saying so · (b… | open |
| **D133** 🟠 | [`TASK-R2-O7`](./TASK-R2-O7-handoff.md) | **`DzCombobox` and `DzCommandPalette` block the main thread past the 50 ms responsiveness budget, reproducibly.** First measurement from the new long-task lane (TASK-R2-O7), 15 samples per component over 5 sep… | (a) profile both steps and fix the blocking work (`DzCombobox`'s ArrowDown handler and `DzCommandPalette`'s mount are each a single named step, so the lane already points at the code) · (b) accept both as know… | open |
| **D134** 🟠 | [`TASK-R2-O7`](./TASK-R2-O7-handoff.md) | **`DzCommandPalette` retains ~2.6 MB and `DzTour` ~0.27 MB per mount/unmount cycle — with zero handle growth.** New memory lane (TASK-R2-O7): heap delta over 20 cycles, two forced collections each side, 2 warm… | (a) profile the open-overlay mount path (retained-object snapshot across cycles) and fix what holds the graph · (b) re-measure with `open: false` fixtures to confirm or kill the correlation before any fix — ch… | open |
| **D136** 🔴 | `grep -i visual \` | **A seventh defective `<done_check>` — and this one passes over the exact condition it exists to detect.** TASK-R2-O6 clause 2 is `grep -n 'continue-on-error' .github/workflows/*.yml \ | wc -l` → expect **0**. `grep -n` emits **one line per match**, and the four matching lines are bare `continue-on-error: true` (plus three comment lines); **none contains the string `visual`, and none can** — t… | open |
| **D139** 🟠 | [`independent verification 2026-09-19 (TASK-R2-O7)`](./TASK-R2-O7-handoff.md) | **A comment explaining why a file-scoped lint marker was *rejected* switches that marker on.** `checkSource()` decides the file-wide raw-colour exemption with `content.includes(FILE_ALLOW_RAW_VALUES_MARKER)` (… | (a) **rec.** — make the markers **comment-aware**: require the marker on its own comment line, or in the first N lines, or as `@token-check-allow-raw-values`, so prose about a marker is not an application of i… | open |
| **D140** 🔴 | [`independent verification 2026-09-19 (extends D135, TASK-R2-O7)`](./TASK-R2-O7-handoff.md) | **The 22 per-export `size:*` budgets are enforced by nothing — extends **D135**, which stated it; this row records the measurement and corrects the source comment that denied it.** Three independent confirmati… | (a) **rec.** — wire a size gate into `validate:all`: give `component-size-report.ts` a non-zero exit on breach, or route `size:*` through a spec that does not sit behind `DZUP_PERF_GATE`, and land it **with th… | open |
| **D141** 🔴 | [`independent verification 2026-09-19 (TASK-R2-O1)`](./TASK-R2-O1-handoff.md) | **The `browser-degradation` gate is real, proven, and provides zero protection today, because the ledger it compares against is untracked.** `e2e/matrix/browser-evidence.json` is **not in git** (`git ls-files… | (a) **rec.** — commit the ledger (owner, D127), then **re-run `yarn validate:capability-matrix` and confirm the note reads *"N committed `pass` cell(s) compared"* and not *"no committed browser ledger"*; that… | open |
| **D142** 🟠 | [`independent verification 2026-09-19 (TASK-R2-O5)`](./TASK-R2-O5-handoff.md) | **`DzTableCell` puts `role="separator"` on a `<button>`, and nothing in this repository would catch it.** `packages/core/src/components/data/DzTableCell.vue:278-292` renders `<button type="button" … role="sepa… | (a) keep the `<button>` and **gate the rule** — add `aria-allowed-role` to `BLOCK_GATED_RULES` and land a block that renders a resizable column, which also closes the pre-existing `DzMention` instance · (b) mo… | open |
| **D144** 🟠 | [`independent verification 2026-09-19 (TASK-R2-O1)`](./TASK-R2-O1-handoff.md) | **`DZUP_BROWSER_EVIDENCE_BASELINE` / `_CURRENT` are a live path to a green gate over fabricated data.** The two env vars replace **both sides** of the degradation comparison with arbitrary files on disk (`pack… | (a) **rec.** — keep the hatches, but make the validator **refuse to exit 0 while either is set**: report every violation as it does now, print the banner, then exit non-zero with *"override set — this run cann… | open |
| **D145** | [`TASK-R1-O1`](./TASK-R1-O1-handoff.md) | **`sourceCommit` is off by one by construction and always will be.** N0-05 D1’s *copied-binding* half is fixed (one shared `headCommit()`; quality no longer copies the manifest’s stamp, capability no longer co… | (a) accept and state the invariant (“the tree the artifact was generated from”); every freshness gate already excludes provenance from its byte comparison, so nothing gates on it · (b) post-commit hook that re… | open |
| **D147** | [`TASK-R1-O1`](./TASK-R1-O1-handoff.md) | **Browser/perf/visual evidence goes stale on the commit *after* it is captured, so “0 stale cells” is unreachable under the current workflow.** R2-O1/O6/O7 all ran and passed at `2d51eec`; `527dbd1` then chang… | (a) accept staleness between releases and gate on “no cell stale for more than N commits” · (b) re-run the lanes as the last act before a **release** commit, so the release tree is the one the evidence describ… | open |
| **D148** | [`TASK-R1-O1`](./TASK-R1-O1-handoff.md) | **`packages/tooling` had no type gate at all**, which is how 18 errors accumulated unseen — two of them real signature bugs, not fixture noise. `yarn typecheck` is `vue-tsc -p packages/core/tsconfig.json` and… | (a) keep it chained — it is the only change here that makes `validate:all` stricter for every future commit · (b) run it in a separate lane — keeps the chain’s runtime flat but restores the blind spot — **rec… | open |

---

## 5. Hygiene — real, but gates nothing on the critical path

### 5.1 CI and toolchain (TASK-R1-O4's packet — [`ci-dispatch-request-2026-09.md`](./ci-dispatch-request-2026-09.md))

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D161** 🔴 | [`TASK-R1-O4-handoff.md`](./TASK-R1-O4-handoff.md) §9 | Whether CI can confirm R1-O1's green tree. **`test`, `e2e` and `coverage` have no build step at all**, and four `@dzup-ui/tokens` subpath specifiers do not exist on a clean checkout | (a) add `yarn workspace @dzup-ui/tokens build` to the three jobs and replace `tokens:generate` in `validate` · (b) a `prepare`/`postinstall` · (c) a source fallback | **(a) — four lines, ~15 s per job.** It is the difference between a CI red since July and one that can confirm the green tree. **(c) rejected: there is no source equivalent** | **open** |
| **D162** 🟠 | ditto | **How a CI run becomes evidence.** No file anywhere holds a run id; 6 of 6 evidence pages mention CI nowhere | (a) `packages/core/evidence/ci-runs.json`, append-only, one record per dispatch · (b) fold into the release bundle · (c) leave the pages silent | **(a)** — the same shape the AT matrix already uses. **Use `gh run view <id> --json jobs`, not the run conclusion: `continue-on-error` makes the conclusion `success` over failed jobs, which is how three weeks went by** | **open** |
| **D163** 🟠 | ditto | Eight workflow files | `actions/{checkout,setup-node,cache}@v4` are being force-migrated off Node 20 | **(a) bump all three to `@v5` and dispatch to verify — but only after D161**, so the run is readable. **(c) `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION` rejected — the variable's own name is the argument against it** | **open** |
| **D164** 🟠 / **N5-03 D6** | ditto · [`N5-03-toolchain-currency-handoff.md`](../../program-2026-09/reports/N5-03-toolchain-currency-handoff.md) §10 | The blocking graph | should `min-peer` and `nuxt-majors` block? | **(a) dispatch both; if green, promote `min-peer` into `ci.yml` (one line) and delete `nuxt-majors`'s `continue-on-error`.** N5-03 D6 has been open for three cycles | **open** |
| **D155** 🟠 | [`TASK-R1-O3-handoff.md`](./TASK-R1-O3-handoff.md) §10 | Release rehearsal honesty. **`yarn test` can exit 1 with zero failed tests** — two instances (`AnimationsPage.v2.spec.ts`, `DzMasonry.spec.ts`) | (a) fix each at the defect · (b) tolerate a non-zero exit with 0 failures · (c) `dangerouslyIgnoreUnhandledErrors` repo-wide | **(a).** (b) and (c) **rejected** — an unhandled error in a torn-down environment is exactly how a real leak first shows up | **open** |
| **D156** 🟢 | ditto | `validate:all`'s shape | should any `release:*` script join the chain? | **(a) keep them in `rehearse:release`.** If the owner wants barrel drift *gated* rather than reported, **(c) split a cheap `validate:barrel-drift` out of `release:api-diff`** — it needs the file on disk, not a tarball, so it costs milliseconds | **open** |
| **D157** 🟢 | ditto | A 1.7 MB inadmissible baseline | keep `packages/tooling/api-surface/527dbd1-INADMISSIBLE.json` until a clean commit, then delete · delete now · keep permanently | **(a).** **(c) rejected — an inadmissible baseline that lingers is the one somebody eventually diffs against by accident.** Note: **`release:api-surface:record` on a clean tree is a prerequisite of the first release** | **open** |
| **N5-03 D2** 🟠 | [`N5-03-toolchain-currency-handoff.md`](../../program-2026-09/reports/N5-03-toolchain-currency-handoff.md) §10 | `@dzup-ui/nuxt`'s compatibility floor | keep `>=3.0.0` · narrow to `>=4.0.0` | **Keep `>=3.0.0`** — all six fixtures build and pass on `nuxt@3.19.0`. Make the Nuxt 3 leg the tripwire (D164) | **open** |
| **N5-03 D3** 🟠 | ditto | `CLAUDE.md` Quick Rule 5 | *"Use `.ts` extensions in all relative imports"* is **actively harmful in a package `tsc` emits** — `@dzup-ui/contracts` followed it and shipped a package Node could not load | Qualify the rule per build strategy **and gate it**. The repository already disagrees with itself and no gate noticed | **open** (the contracts defect itself is fixed — `.changeset/the-contracts-package-can-now-be-loaded-by-node.md`) |
| **N5-03 D5** / **D3-D3** / **D1-D4** / **D170** 🟠 | ditto · [`N2-D3-playgrounds-handoff.md`](../../program-2026-09/reports/N2-D3-playgrounds-handoff.md) · [`N2-D1-docs-site-handoff.md`](../../program-2026-09/reports/N2-D1-docs-site-handoff.md) · [`TASK-R1-O5-handoff.md`](./TASK-R1-O5-handoff.md) | **Criterion C14.** Four reports, one subject: **no size ceiling on a publishable artifact** | add a ceiling · leave it to `check:bundle` | **Add one, but not from a stale artifact.** D170: the only local `apps/landing` build is from 2026-08-28 and carries a 230 MB Storybook with duplicated `iframe` chunks — **a ceiling seeded from that is a fabricated number.** Needs one fresh build | **open** (raised four times) |

### 5.2 Docs site (TASK-R1-O5's packet — [`docs-deployment-packet-2026-09.md`](./docs-deployment-packet-2026-09.md))

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D165** 🟠 / **D1-D2** / **D2-D7** | [`TASK-R1-O5-handoff.md`](./TASK-R1-O5-handoff.md) §8 · [`N2-D1-docs-site-handoff.md`](../../program-2026-09/reports/N2-D1-docs-site-handoff.md) §12 | **Criterion C14's second half** | (a) Cloudflare Pages · (b) GitHub Pages · (c) the existing `datazup.com` host | **(a)** — only (a) can set the `/r/*` and immutable-asset cache headers the packet §4 specifies; (b) collapses the cache row to "host defaults" | **open** |
| **D167** 🟠 | ditto | The `docs-deploy` workflow | (a) `workflow_dispatch` + `push: tags: ['v*']` · (b) `push: main` · (c) manual | **(a)** — the site documents a published API and **`ci.yml` has not been green since 2026-07-03**, so `main` is not a quality signal | **open** |
| **D168** 🟠 | ditto | Consumer correctness of `shadcn add` | (a) host `_headers` · (b) accept fixed host defaults | **(a)**, and it is a consequence of D165, not an independent choice. `/r/*.json` is a machine-read API a 10-minute blanket cache governs badly in both directions | **open** |
| **D169** 🟢 | ditto | The release checklist | (a) re-run `docs-deploy` at the previous tag · (b) repoint a stored deployment | **(a)** — every input is generated from the tree, so **the tag *is* the artifact** | **open** |
| **D173** 🟢 | ditto | R1-O3's bundle accuracy. `candidate-content-digest.json` names a **deleted** file (`apps/docs/scripts/report-size.mjs`) | (a) re-run `yarn release:report` · (b) leave — a candidate digest is a point-in-time record | **(b) for the existing bundle, (a) at the next candidate.** It is an output, not a gate input — nothing reads it | **open** |
| **D1-D1** 🟠 | [`N2-D1-docs-site-handoff.md`](../../program-2026-09/reports/N2-D1-docs-site-handoff.md) §12 | Four gated documents' bytes | fix the 12 unescaped-HTML JSDoc descriptions at source? | **Yes** — it removes a hazard for every present and future consumer of the metadata | **open** |
| **D1-D3** | ditto | Positioning | does the docs site replace Storybook as the public face, or sit beside it? | Storybook remains the internal workbench; the docs site is the public face | **open** |
| **D1-D5** / **N2-A1 D4** / **N2-A3 D8** / **D148** 🟠 | ditto · [`N2-A1-mcp-governance-handoff.md`](../../program-2026-09/reports/N2-A1-mcp-governance-handoff.md) · [`N2-A3-llms-gate-handoff.md`](../../program-2026-09/reports/N2-A3-llms-gate-handoff.md) · [`TASK-R1-O1-handoff.md`](./TASK-R1-O1-handoff.md) | Type coverage. **Raised four times across three programmes** | put `apps/docs` and `packages/tooling` in `typecheck:all` | **Done for tooling** — R1-O1 chained `typecheck:tooling` at link 2 after fixing 18 errors, two of them real signature bugs. **`apps/docs` is still typechecked by nothing** | **tooling decided (R1-O1); `apps/docs` open** |
| **D1-D6** 🟢 | [`N2-D1-docs-site-handoff.md`](../../program-2026-09/reports/N2-D1-docs-site-handoff.md) §12 | Visual identity | the site ships VitePress's stock theme — it does not look like dzup-ui and does not render with `--dz-*` tokens | A visual-identity decision, coupled to D3 (theme builder) | **open** |
| **D1-D7** 🟢 / **N2-A3 D3-adjacent** | ditto | Four gated documents' bytes | should the `@example` blocks render? (~8 lines in `render-llms.ts`) | **Yes** — improves `llms-full.txt`, the MCP metadata tools and 144 pages at once | **open** |
| **D3-D1** … **D3-D5** | [`N2-D3-playgrounds-handoff.md`](../../program-2026-09/reports/N2-D3-playgrounds-handoff.md) §12 | The playgrounds lane | `playground.config.ts` remains hand-maintained · the sandbox has a hard runtime dependency on **jsDelivr** · the docs-site ceiling (see 5.1) · the playground bundles from `src`, not `dist` · 11 of 15 refusals are `no-runnable-story` | **D3-D2 is the sharpest: a consumer-facing surface with a third-party CDN as a hard runtime dependency.** D3-D4 means the playground does not exercise what consumers install | **open** (5 items) |

### 5.3 Agent surfaces — MCP, metadata, `llms.txt`

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N2-A1 D1** 🟠 / **D68** | [`N2-A1-mcp-governance-handoff.md`](../../program-2026-09/reports/N2-A1-mcp-governance-handoff.md) §12 | **Ownership-manifest schema 1.2.0**; the 29 unclassified symbols | (a) a new `kind` family (`tool-surface`, `service-client`, `pure-function`) · (b) a second manifest per non-component public package · (c) leave it out | **A1 took (c) provisionally.** **D68 later took (a)** under owner delegation — `utility` + `injection-key`, unclassified **29 → 0**. `@dzup-ui/{testing,codemods,nuxt}` have the same gap | **superseded by D68 (taken 2026-09-17, reversible)** |
| **N2-A1 D2** 🔴 | ditto | **30 % of the catalog invisible to every AI client** | close the 43-component visibility gap; the fix is upstream in `public-api.manifest.json` | **This is D158 / N0-05 D2** — running `generate:exports` is a public-API decision, not a refresh | **superseded by D158** |
| **N2-A1 D3** / **N2-A2 D5** / **N2-A3 D1** 🟠 | ditto | Three published MCP behaviour changes with **no changeset** | write a `minor` changeset for `@dzup-ui/mcp` | **Write them.** All three handoffs deferred to N5-01 "because 17 changesets are already unreleased" — that reason has expired; there are now 36 and none of them covers these | **open** (three reports, one gap) |
| **N2-A1 D6** 🟠 | ditto | `@modelcontextprotocol/sdk` floor | `^1.12.0` declared, `1.29.0` installed and tested | Raise the floor to what is tested, or add an SDK-floor lane. **Same class as ADR-18's Node floor** | **open** |
| **N2-A1 D7** 🟢 | ditto | `@dzup-ui/mcp`'s public surface | `dist/tools.js` ships and is unreachable (not in `exports`) | Declare `./tools` — making the tool functions public API, a real decision — or stop emitting it | **open** |
| **N2-A2 D1** 🟠 | [`N2-A2-component-meta-handoff.md`](../../program-2026-09/reports/N2-A2-component-meta-handoff.md) §10 | Undocumented public API | **26 `defineExpose` members across 21 components, 0 descriptions, no evidence cell covers them** | (a) move the prose onto the members · (b) add an `exposed` cell kind · (c) accept name-and-type-only | **open** |
| **N2-A2 D2** | ditto | Emit descriptions | accept the `emits-interface` recovery (70.5 %) · change the declaration style repo-wide | **Keep the recovery**; decide whether the 35 authored-but-undescribed emits are worth authoring | **open** |
| **N2-A2 D3** | ditto | The generated prop table | which default is canonical? `DzButton.variant` declares `undefined` in source and `'solid'` in `argTypes` | **(c) make `argTypes` generated from this artifact** — the option consistent with `<generated_authority>` | **open** |
| **N2-A2 D4** 🟠 | ditto | Two published MCP tools' data source and output format | repoint `list_components` / `get_component` at the metadata artifact? | It closes A1's F-1 for every consumer **without** running `generate:exports` | **open** |
| **N2-A2 D7** | ditto | Artifact stability | `vue-component-meta` pinned exact (`3.3.7`) to dedupe with Storybook's `^3.2.7` | Keep the exact pin and bump both together, **or** add an extractor-version assertion | **open** |
| **N2-A2 D8** 🟢 | ditto | The architecture reference every agent reads | `CLAUDE.md` says 11 component families; there are **12** | One-line doc fix | **open** |
| **N2-A2 D9** / **N2-A3 D5** | ditto | `validate:all` wall time | the freshness gates cost **~17 s** and **~26 s** | **Flagged as a cost, not proposed as a change** — a cheaper input-digest check would miss a generator-logic change | **open** (informational) |
| **N2-A3 D3** 🟠 | [`N2-A3-llms-gate-handoff.md`](../../program-2026-09/reports/N2-A3-llms-gate-handoff.md) §13 | One component publishing no API | `DzAccordion`'s union props type: (a) split the component · (b) hand-author an override · (c) accept, ratchet stays at 1 | (b) reintroduces hand-typed facts, which `<generated_authority>` forbids | **open** |
| **N2-A3 D4** 🟠 | ditto | `@dzup-ui/core`'s tarball | should `packages/core/docs/llms.txt` ship to npm? Cost ~**460 KB per install** | **A distribution decision, deliberately not changed.** Ties to A4-D1 | **open** |
| **N2-A3 D6** | ditto | Third-party indexing | Context7 opt-in | Terminates in an `[!owner]` line by design; README §8 lists it on the quarterly watch list | **open** |
| **N2-A3 D7** 🟢 | ditto | Every agent-facing surface | 3 components with no header comment and 30 with lower-case descriptions | 33 one-line edits; content edits to 33 component files | **open** |

### 5.4 Component and contract residuals

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **N1-O1 O1** / **N1-O1 O3b** | [`N1-O1-story-dod-handoff.md`](../../program-2026-09/reports/N1-O1-story-dod-handoff.md) §7 | Defects D1/D2 and D11 | should container-level `disabled` propagate (`DzTree`, `DzResizable`)? · `DzDropdownMenu`'s dangling `aria-controls` | **D11 fixed** (`.changeset/a-form-reset-now-resets-the-control.md`, 5 overlay components). The `disabled` propagation question is still open | **D11 decided; O1 open** |
| **N1-O1 O2** | ditto | Defect D3 | who owns `DzMention`'s `loading`? | **Taken as D101** — OR it with the resolver's pending state; *"a host cannot un-say a resolver"* | **superseded by D101** |
| **N1-O1 O3** | ditto | Defect D4 | nested interactive controls in `DzCascader` / `DzTreeSelect` triggers | **Recorded and gated as D98**, not fixed — moving the affordances out of the `role="combobox"` button changes DOM structure | **superseded by D98** |
| **N1-O1 O4** | ditto | Defect D7 | focus restoration in `useFocusTrap` | **Taken as D100** — `restoreFocus: true` by default; a behaviour change to a published composable | **superseded by D100** |
| **N1-O1 O5** 🟠 | ditto | Four families' axe coverage | should `forms`, `data`, `navigation` and `layout` opt into `a11yError`? Those stories run axe at `test: 'todo'` | **Opt in** — a story that proves keyboard behaviour but not axe is half an assertion | **open** |
| **N1-O1 O6** 🟠 | ditto | The Storybook budget | 0.85 MB headroom; closing `gallery` at this density does not fit | Raise the budget, split the non-story bundles, or leave `gallery` open | **open** |
| **N1-O1 O7** 🔴 | ditto | Three weeks of CI signal | **has the Storybook interaction lane ever run in CI?** The config bug meant `yarn storybook:test` could not start | *"If CI runs it, CI has been green on a startup error; if CI does not run it, every `play()` in the repo has been decorative."* Answerable by one `gh run view` | **open** |
| **N5-02 D2** 🟠 | [`N5-02-aria-prop-gaps-handoff.md`](../../program-2026-09/reports/N5-02-aria-prop-gaps-handoff.md) §7 | `DzOrderList` | is `title` the right answer for `dragHandleLabel`, or should the prop be removed as a `minor`? | The fix renders the string and corrects the docs from "accessible label" to "tooltip" — **a real accessible name is a different change** | **open** |
| **N5-02 D3** 🟠 | ditto | Matrix expressiveness | `DzInplace`'s reviewed C2 gap is **structurally invisible** — C2 is source-decided and the gap can never reach the matrix | Same family as N1-O5 O5-2/O5-3 | **open** |
| **N5-02 D4** 🟠 | ditto | The Core/compat boundary | **two warn-once utilities now exist in two packages** because stable Core may not import `compat` | Bless the duplication, or move the primitive somewhere both can reach | **open** |
| **N2-T1 D1** … **D9** 🟠 | [`N2-T1-dtcg-export-handoff.md`](../../program-2026-09/reports/N2-T1-dtcg-export-handoff.md) §8 | The token ABI and DTCG export | three cross-tier shadowed `--dz-appshell-*` (**D48** carries T1-D1 forward) · the `codeblock` tier — wire, delete or gate · `ThemeRecipeV1`'s non-mode axes out of DTCG scope · no `hex` sRGB fallback · wire the official DTCG JSON Schema into the gate (it runs today from a **scratch script using `ajv` as an undeclared transitive**) · unfixtured README snippets unguarded repo-wide · `TOKENS.md` does not ship to npm and has no changeset · `DEPRECATED_TOKENS` is hand-maintained · should `tokens:generate` emit the DTCG file? | **D5 is the sharpest: a validation that is not a gate.** D7 is a publication item — an additive public-API change with no changeset. **D48 is the live carry-forward** | **open** (9 items) |

### 5.5 Further hygiene decisions from the 09-04 sequence

| Id | Raised by | Decision (what it gates is stated in the text) | Options / recommendation (source) | State |
|---|---|---|---|---|
| **D2** | [`TASK-R3-O1`](./TASK-R3-O1-handoff.md) | Correct the TASK-R3-O1 `<done_check>`: **two of its five checks can never pass** — the `m.tiers` probe (no ownership manifest has a `tiers` key) and the check-4 grep (requires `package.json` and `canResolvePro… | (a) rewrite both against the real signals (`OWNERSHIP_TIERS` grep + `PRO_PACKAGE}/package.json` grep) · (b) commit the merged map so `tiers` becomes inspectable — **rec. (a)**; left as-is the next agent reads… | open |
| **D4** | [`TASK-R3-O1`](./TASK-R3-O1-handoff.md) | Fix F2 + F3 inside step 2: the runtime-lookup generator ignores `collision-decisions.json` and reports unresolved collisions nowhere, so the first Core/Pro collision silently deletes a name from the resolver t… | (a) fold both into step 2, exercised against the real Pro manifest · (b) separate follow-ups — **rec. (a)** | open |
| **D5** | [`TASK-R3-O1`](./TASK-R3-O1-handoff.md) | Two packets (R3-O1, R3-O2) edited one working tree concurrently; two `yarn test` runs 7 min apart disagreed by 4 failures and needed a `git status` diff to attribute. A dirty shared tree is not described by an… | (a) one git worktree per concurrent packet · (b) serialise packets that regenerate artifacts · (c) require `git status` before/after in every handoff — **rec. (a)** with (c) as immediate mitigation | open |
| **D8** 🟢 | [`TASK-R3-O2`](./TASK-R3-O2-handoff.md) | `validate:exports` does not catch an *undeclared* export: it checks declared→actual only, so `useDzSanitizer` was live in the barrel with the gate green until `validate:ownership` caught it | (a) add the reverse check in R1-O1 or R5-O7 · (b) leave it to ownership — **rec. (a)**: two gates disagreeing about which is authoritative is how open owner decision #9's drift accumulated | open |
| **D14** 🟠✅ | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **CLOSED by D15 being taken.** Session 3 declared `forms` and the ratchet reached **41**, below the ≤ 55 target; the arithmetic in this row was correct and option (a) is what happened. — **The prompt's `maxWit… | (a) take S1-D4 (D15) and schedule `forms` as its own packet, restoring 44 · (b) restate as *"Tier B+ complete outside `forms`; ratchet ≤ 65"* and track forms separately · (c) extend the next packet into Tier A… | open |
| **D25** 🟢 | [`TASK-R5-O2 session 3`](./TASK-R5-O2-handoff.md) | **`maxWithoutAnatomy` is now a Tier A number and the task that owns it has ended.** The ratchet reads **41** and all 41 are **Tier A** — the tier TASK-R5-O2's scope boundary excludes. It cannot move again with… | (a) open a Tier A packet that takes it to 0, family by family, the way this one ran · (b) re-scope the ratchet to "Tier B+ only", which would make it read 0 and stop being informative · (c) declare Tier A oppo… | open |
| **D29** 🟢 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **The "`yarn <script>` exits 127 here" environment note is FALSE for `ui/dzup-ui`.** Measured this session: `yarn typecheck` → **exit 0**, `yarn validate:rtl` → **exit 0**. The 127 is real but belongs to **`ui… | (a) correct the note to name the repo it applies to, in the program README and in the task briefs that copy it · (b) leave it and have every session re-measure — **rec. (a)**: as written the note makes every f… | open |
| **D30** 🟢 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **`formats` consumers = 2 is the COMPLETE number, not a thin rollout.** The applicable set is the four `Intl` construction sites enumerated in `useDzFormats.ts`'s own header; all four are wired, `new Intl.` ou… | (a) record the enumeration beside the ratchet so the number is read correctly · (b) widen the ratchet to count indirect consumers, which reads 3 and still needs the same footnote — **rec. (a)**, done in the ra… | open |
| **D42** 🟢 | [`TASK-R5-O7`](./TASK-R5-O7-handoff.md) | **The TASK-R5-O7 `<done_check>` names two files that do not exist**, and a third check reads the wrong script. Token gates live in `packages/tooling/src/token-checks/`, not `.../validators/tokens*.ts`; and the… | (a) **amend the task file's `done_check`** to the corrected block in the handoff §7 — no code change · (b) chain `validate:tokens:refs` into `validate:tokens` to match the brief's wording, which would run it *… | open |
| **D48** 🔴 | [`TASK-R5-O7 (from N2-T1)`](./TASK-R5-O7-handoff.md) | **T1-D1 carried forward — the three cross-tier shadowed `--dz-appshell-*` declarations** (originally N2-T1 D1 §8). `--dz-appshell-header-border` ships `neutral-300` while `semantic/light.ts` declares `neutral-… | (a) delete the appshell block from `semantic/{light,dark}.ts` — **changes a shipped border colour in light mode** · (b) **delete the colliding entries from `component/appshell.ts`** — pixel-neutral today, remo… | open |
| **D49** 🟢 | [`TASK-R5-O7`](./TASK-R5-O7-handoff.md) | **The ABI has no `--dz-input-foreground`** (ceiling HC-5). `--dz-input-bg` maps to `Field`, but input text inherits `--dz-foreground` (`CanvasText`), so `Field`/`FieldText` is the one OS-guaranteed pair the hi… | (a) **add `--dz-input-foreground`** across all three cascades — a token **addition**, which the task's `<scope>` forbids without an owner pick · (b) leave it and keep the ceiling. **rec. (a)** | open |
| **D50** 🟠 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **A2-D2 SETTLED — `update:*` events get a SYNTHESISED description carrying its own `descriptionSource`.** 71 of the 106 undescribed events are generated by `defineModel`; Vue creates the emit, so there is **no… | **(a) synthesise, stamped `descriptionSource: "model-synthesised"` and counted in `totals.eventsModelSynthesised` — TAKEN** · (b) hand-author an `update:modelValue` member in all 71 Emits interfaces — a second… | open — the owner need only confirm |
| **D57** 🟢 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **The TASK-R5-O8 `<done_check>` cannot run as written** — the **D42** pattern for the second time in this program. It requires `packages/core/docs/component-meta.ratchets.json`, which does not exist (the ratch… | (a) **amend the task file to the corrected four-command block in handoff §8 — no code change** · (b) add a `component-meta.ratchets.json` alias so the check works as written — a second artifact for one number… | open |
| **D63** 🟠 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **Arabic typeface (AR-2 decision 1 / D-10)** — sheet `reports/TASK-R5-O4-typeface-decision.md` | (a) vendor OFL face · (b) Arabic system stack token · (c) consumer obligation — **rec. (c) now, (b) opt-in, (a) never inside core.css**; (a) needs a binary-asset licence gate first | open |
| **D65** 🟢 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **~12 components still render script/template-literal English outside the catalog** (inventory in `packages/core/docs/i18n.md` §1); `validate:hardcoded-strings` reads static template attributes only. `DzFileUp… | (a) extend the gate to bound template literals, convert in one packet · (b) leave — rec. (a) | open |
| **D66** 🟢 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **TASK-R5-O4 `<done_check>` item 6 does not filter**: `yarn test:e2e:landing -- --grep rtl` runs all 107 tests (the `--` swallows `--grep`) — the D42/D57 pattern again | (a) amend to `yarn test:e2e:landing --grep rtl` — rec. (a) | open |
| **D72** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **TASK-R3-O3 `<done_check>` fragility**: item 4's `**` glob works only because the file is one level deep; item 3 counts name mentions, not state walks | (a) literal path + count `AsyncOptions` exports · (b) leave — rec. (a) | open |
| **D77** 🟢 | `head turns grep's exit 2 into a pass` | **The TASK-R3-O4 `<done_check>` cannot run as written.** This is the D42/D57/D66/D72 pattern again. Checks 1–3 name `packages/testing/src/security-corpus/`, a directory that never existed (the module is `src/s… | TASK-R3-O4 | open |
| **D78** 🟠 | [`TASK-R3-O4`](./TASK-R3-O4-handoff.md) | **The type half of the types ⇔ schema spec runs only under `tsc -p packages/tooling/tsconfig.json`**, which is not a `validate:all` link and carries 12 errors. A union member or interface field changed without… | (a) TASK-R1-O1 clears the 12 errors and adds tooling `tsc` as a link · (b) vitest `--typecheck` for `*.spec.ts` · (c) leave to review — rec. (a) | open |
| **D82** 🟢 | [`TASK-R5-O3 (continuation 2026-09-17)`](./TASK-R5-O3-handoff.md) | **Four overlays have no enter motion, so the end-state arm is trivially true for them.** With every governed duration forced to 2 s, `DzContextMenu`, `DzDropdownMenu`, `DzPopconfirm` and `DzSheet` ran **no** g… | (a) accept "no enter motion" as the design, and delete the dead `transition-*` utilities so the classes stop implying motion · (b) give them a real enter transition (`data-state` keyframes or `@starting-style`… | open |
| **D83** 🟠 | `\` | **The Vitest target is 4.1.11, not the memo's 5.x.** `vitest@5.0.1` declares `engines.node "^22.12.0 \ | ^24.0.0 \ | open |
| **D95** 🟠 | `\` | **Vite 8 on `vitest@3.2.6` fails *silently*, not loudly — and no gate in this repository can see it.** `vite` is a **`dependencies`** entry of `vitest@3.2.6` (`^5.0.0 \ | ^6.0.0 \ | open |
| **D101** | [`TASK-R2-O3`](./TASK-R2-O3-handoff.md) | **`DzMention.loading` semantics** now that the dead prop works (D3) | (a) **OR it with the resolver's own pending state -- taken**: a host cannot un-say a request that is genuinely in flight · (b) let the prop override, so a host can force the indicator off -- **rec. (a)** | open |
| **D104** 🟠 | [`TASK-R2-O4`](./TASK-R2-O4-handoff.md) | **`sms:` is in the default allowlist and the task prompt's Step 0 list does not name it.** `url-boundary.threat-model.md` §2a and N1-O5 F-U1 both recommend it; the prompt's summary omits it. **No corpus fixtur… | (a) **keep it — taken**: it cannot execute in a document, it is ordinary contents of a mobile navigation menu, and refusing it makes the policy the thing consumers route around · (b) drop it — one line in `DZ_… | open |
| **D115** 🟠 | [`TASK-R2-O2`](./TASK-R2-O2-handoff.md) | **Does Tier B get scripted?** Wave 1 covers Tier C/D. Tier B is **67 components × 1 required pairing = 67 cells and not one of them has a script** — N1-O4 wrote scripts for Tier C/D only | (a) script Tier B (~3–4 agent-days) then run 67 cells (~25 h) · (b) run Tier B unscripted against the generated task table · (c) leave Tier B `unrun` and say so — **rec. (a), sequenced after wave 1** so the sc… | open |
| **D119** 🟢 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | **`DzOtpInput.otp` defaults to `true`, and that is a behaviour change.** Cells now render `autocomplete="one-time-code"` (platform SMS autofill — the SC 3.3.8 mechanism) and focus lands on the first empty cell… | (a) **default `true` — taken**: the component is named for one-time codes and 3.3.8 is a conformance gate, so the safe default is the conformant one · (b) default `false`, opt-in — no behaviour change, but eve… | open |
| **D131** 🔴 | `longTask\` | **A sixth task's `<done_check>` is defective — and R2-O7's is defective in all three clauses, two of them in the dangerous direction.** Clause 1 (`Object.keys(b.metrics??b).filter(/leak\ | memory\ | open |

### 5.6 The R0 owner packets' own hygiene decisions (2026-09-22)

Appended by the 2026-09-22 fix pass; see the note at §2.5 for why they were
missing. Four of the six are about how decisions and numbers are *kept* — which
is why losing them was the failure they each describe.

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D179** 🔴 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 · §7.1 above | Every cross-report citation; this register's own usability. **Namespace the `D<n>` ids.** `D8`–`D11` each name a defect *and* a decision in the same programme; 105 definition sites across 11 ids; the ledger already mis-cites itself | (a) prefix the namespaces (`DEF-8` / `OD-8`) going forward, leave existing ids alone · (b) qualify every cross-report citation as the 09 programme already does (`N5-01 D1`, `A4-D3`, `S1-D4`) · (c) renumber one namespace · (d) leave it | **(a) + (b).** **(c) rejected** — 190 decisions and 49 reports cite these ids; renumbering breaks every citation to buy tidiness. Leave the `D43`/`D44` gap as a gap for the same reason. Still open, and already re-offended against: `TASK-R0-O2-handoff.md:86` cites bare **`D6`**, inside the band, in the same hour (**S11**) | **open** |
| **D183** 🟠 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | The next programme's cost of entry. **Is this register the system of record, and do the ledgers stop restating decisions?** Five ledgers now point at it. Without a rule, the next programme re-derives 312 decisions — which is the failure this task exists to end | (a) register is authoritative; ledgers link, never restate; new decisions are appended here at the packet that raises them · (b) ledgers stay authoritative and the register is a snapshot that goes stale · (c) fold the register into `EXECUTION-STATUS.md` | **(a)**, with the rule written into README §5's `<handoff>` block so every future packet appends here. **(b) makes this document a liability within one programme.** The very next packet did not append (**S3**), which is the strongest available argument that the rule has to be written down rather than assumed | **open** |
| **D184** 🟠 | [`TASK-R0-O1-handoff.md`](./TASK-R0-O1-handoff.md) §7 | Future agents' first five minutes. **Correct README §7 and `release-exit-tasks.md`'s stale numbers?** README §7 says *"~125"* (real: **312 raised / 231 open** after this subsection's 14); TASK-R0-O1's `_Gap:_` and `<validation>` say *"20 changesets"* (real: **37**); R1-O1 §7 says *"37 changesets"*, which **is now correct** | (a) correct all three in place, citing this register · (b) leave them and rely on the register · (c) correct only the task files a future agent executes | **(a)** — a `<validation>` block that says `20 expected` will make the next agent think the gate is broken. TASK-R0-O1 did **not** edit them: amending another packet's prompt is outside its scope, and it stays outside this fix pass's scope too | **open** |
| **D187** 🟠 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | Every cross-tier ADR citation; Pro's next ADR. **Do the two tiers share one ADR number space, or split it?** Resolved *for ADR-13* by naming (CLAUDE.md §"ADR-13 is two different decisions"), but the underlying clash is unresolved and the next collision is a matter of time — Pro is at ADR-17, OSS at ADR-20, and neither coordinates | (a) keep one space and require a tier prefix in any cross-tier citation · (b) prefix the numbers themselves going forward (`OSS-21`, `PRO-18`) · (c) give Pro a reserved band (e.g. 100+) and renumber nothing · (d) leave it | **(c)**, with (a) standing for the existing overlap. A reserved band costs one sentence in each repo's registry and makes the next collision impossible; (b) renames a live citation surface, which is what (a) already refused | **open** |
| **D189** 🟢 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | The shape of `validate:adr-references`; reversible in one commit. **Should `adr-registry.json` mirror each ADR's status?** The task's `<status_gate>` said *"the registry records status per ADR"*; TASK-R0-O2 deliberately did **not** mirror it and reads the document's own `Status:` line instead | (a) keep it as built — one source, the document · (b) mirror the status into the registry and gate that the two agree · (c) mirror it and make the registry authoritative | **(a) as built.** A mirrored status is a second place for one fact to be wrong, and the programme has recorded that failure six times. (b) is defensible — it makes a status change visible in a JSON diff — at the cost of a third rule; (c) would let a document lie about itself | **open** |
| **D191** 🟢 | [`TASK-R0-O2-handoff.md`](./TASK-R0-O2-handoff.md) §7 | The ADR ratchet's usefulness; nothing else. **Is `maxProposedCitedFromCode` the right meter?** It counts **distinct Proposed ADRs cited from code** (3), not citation *sites* (871 across the three), and it treats prose as free | (a) keep distinct-ADR counting — moves only on acceptance, so the number means one thing · (b) count citation sites — bigger, more alarming, and it moves on every routine edit · (c) count both, gate on (a) | **(a), as built**, with (c) as a cheap follow-up if the owner wants the pressure visible. (b) turns an acceptance ratchet into edit noise | **open** |

### 5.7 Gates that structurally cannot fail (2026-09-22)

Raised by the release-exit fix pass, continuing the sequence from **D192**. Both
are the programme's recurring defect in miniature — *a clause that is printed
like a check and cannot fail* — and both were **judged rather than reflexively
changed**, because in each case making it fail today would either be dishonest or
land a red the owner has not agreed to. The two documentation corrections that
went with them (the icon module's header, the registry summary line) are in the
tree; the behaviour questions are here.

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D193** 🟠 | [`release-exit-verification-2026-09-22.md`](./release-exit-verification-2026-09-22.md) finding **S7**; judged by the fix pass | Nothing on the critical path — but it decides what a green `validate:icon-duplicates` means. **The icon gate's `[lock-vs-disk]` cross-check is a warning that cannot fail the build.** Proved: with `yarn.lock` seeded to resolve one version while `node_modules` still held two, the gate printed `! [lock-vs-disk] yarn.lock resolves [0.477.0] but node_modules holds [0.475.0, 0.477.0]` and **exited 0**. The module header sold it as *"a second, independent route to the same fact"*; it is not a route to anything the build can act on | (a) **leave it `report`** and stop the header claiming otherwise · (b) promote *set-disagreement* to `error` · (c) add a **narrower** `error`: fail when `node_modules` itself holds **more than one version** of the icon library, independent of the lockfile — a real duplication, and the one thing on disk that is a defect rather than a state · (d) delete the clause | **(a), taken as far as a packet may — the header and the message are corrected in the tree, and the clause is left `report`.** **(b) is not honest today:** `node_modules` is not a governed artifact, it is *absent* on a fresh checkout (`onDisk === null`, so the clause is unreachable exactly where a gate matters most), and `test:min-peer` makes it disagree **on purpose** — an error there is red where it is meaningless and silent where it is not. **(c) is the defensible promotion and is the owner's call**, because it would add a *second* error clause to the one gate TASK-R1-O6 deliberately leaves red, changing what that red means without R1-O6's input. Cost of (a), stated rather than buried: **a duplication present on disk but hidden by the lockfile passes** | **open** |
| **D194** 🟠 | [`release-exit-verification-2026-09-22.md`](./release-exit-verification-2026-09-22.md) finding **S9**; judged by the fix pass | Nothing on the critical path — but it decides whether "nine clauses, 8/8 seeded" means what it sounds like. **`validate:registry`'s 9th clause (`docs`) is report-only, has no seeded case in `--self-test`, and is firing right now**: `--all` lists **103 of 191** items (59 animations + 44 templates) shipping with no markdown mirror, and the gate prints `✓ registry: …` over them and exits 0 | (a) leave `report`, and stop the ✓ line covering it · (b) promote to `error` now · (c) **ratchet it** — record the current 103 in a committed ceilings file and fail when it rises, the shape `anatomy-parts` uses for `maxUnreviewedPartNames` and the one **D143** asks for on `stale` cells · (d) delete the clause and say the mirrors are not governed | **(c), and it is the owner's to authorise** because it creates a governed ceiling file and thereby a standing commitment. **(b) lands 103 reds immediately** and whether every animation and template block ever gets a mirror is the `llms.txt` surface's question (**N2-A3 D2**), not this gate's. **(a) is what shipped in the tree as an interim**: the ✓ line now says *"…and N report-level finding(s) above that NO clause can fail on"*, so the bare green is gone, and the module header records that the clause has no seeded proof. That is a correction to the *claim*, not to the *coverage* — the clause still cannot fail, which is why this decision stays open | **open** |

### 5.8 Evidence binding and lane hygiene (2026-09-22, TASK-R2-O1 completion)

Raised by the TASK-R2-O1 residual — the pass that re-ran all 24 browser projects
at the first clean committed tree this programme has had. Both are defects the
task hit **while doing the thing they obstruct**, which is why they are recorded
with a measurement rather than as a suggestion.

| Id | Raised by | Gates | Options | Recommendation | State |
|---|---|---|---|---|---|
| **D195** 🟠 | [`TASK-R2-O1-handoff.md`](./TASK-R2-O1-handoff.md) §R.4 | **Whether any generated artifact can ever truthfully claim "commit-bound" again while more than one session works in the repository.** `worktreeDirty` / `dirtyPathCount` is a raw `git status --porcelain` count, so it cannot distinguish **"the measured code differs from HEAD"** (which invalidates the evidence) from **"somebody left untracked files around"** (which cannot). Measured: at every sweep launch `git status --porcelain --untracked-files=no -- packages/ apps/ e2e/` returned **zero** paths other than this task's own generated artifacts — every file the 4,293 executions ran was byte-identical to `589be13` — yet the artifacts are stamped `worktreeDirty: true, dirtyPathCount: 4` because a **concurrent session** (`internal-dev-ff`) holds two untracked doc directories and one modified README. `dirtyPathCount: 0` is now **unobtainable by any agent**: reaching it would mean deleting another session's work | (a) **decompose the probe** into `trackedDirty` + `untrackedCount` and let only the first qualify the evidence · (b) scope the probe to the paths the lane actually executes (`packages/`, `apps/`, `e2e/`) · (c) leave the probe and carry the decomposition in prose, as this task did · (d) require a clean worktree before any generator runs | **(a)**, with (b) as the narrowing that makes it exact. It is a change to one helper and it is the difference between an artifact that can say *commit-bound* and one that can never say it again while anybody else works here. **(c) is what shipped** — the decomposition is written into `browser-evidence.json`, `coverage.json` and the handoff — but prose does not survive being quoted by the next packet, which is the failure mode this register exists to stop. **(d) is refused**: it hands any concurrent session a veto over evidence generation | **open** |
| **D196** 🟢 | [`TASK-R2-O1-handoff.md`](./TASK-R2-O1-handoff.md) §R.2.1 | Nothing on the critical path — but it decides how much time the **next** interrupted sweep costs. **An interrupted matrix run orphans the Storybook preview server on `:6106`, and `--strictPort` makes the next run die on it** with an error that reads like a config mistake rather than like leftover state. Measured, by identity not inference: after the harness reaped a sweep for host memory pressure, PID 36868 (`vite preview --outDir apps/storybook/storybook-static --host 127.0.0.1 --port 6106 --strictPort`) was still listening, started **two seconds after** the reaped sweep launched, parent `cmd.exe` already gone — a true orphan, and **this task's own**, not a concurrent session's | (a) **pre-flight the port** and fail with the real reason (*"an earlier run left a server on :6106; stop PID n or wait"*) · (b) reap the orphan automatically before starting · (c) document it in the lane's README and leave the behaviour · (d) drop `--strictPort` | **(a).** `--strictPort` is *correct* — silently moving to 6107 would let two sweeps drive two different Storybook builds, which is a worse failure than a confusing error — so **(d) is refused**. **(b) is tempting and wrong here**: this repository demonstrably runs several sessions at once, and a reaper that guesses wrong kills a server another session owns. (a) costs one `Get-NetTCPConnection`-equivalent check and turns a ten-minute diagnosis into a one-line message | **open** |

---

## 6. Already decided — cite, do not reopen

Listed so no future packet reopens them. **The evidence is the citation, not a restatement.**
Nine were taken by agents under explicit owner delegation and are reversible before release;
three were taken by the owner on 2026-09-19; the rest were settled by the packet that raised them.

Additionally, **45 N-programme decisions were closed by the 09-04 programme** and appear in
§2–§5 marked `superseded by <id>` rather than being listed twice.

### 6.1 Decided in the 09-04 sequence

State column carries the date and the mechanism. No option text is restated.

| Id | Raised by | Decision (what it gates is stated in the text) | Options / recommendation (source) | State |
|---|---|---|---|---|
| **D11** 🟠 | `loading\` | `maxUndeclaredStates` was initialised at **1**, not 0: `DzTableRow.vue:99` emits `data-state="expanded"` and `DzTable` — the parent owning the compound part — declares `ready\ | selected`. The one-line fix makes `validate:ownership` stale, and regenerating the manifest was forbidden here | **taken — option (a), TASK-R5-O2** (`DzTable.anatomy.ts` declares `expanded`, manifest re… |
| **D15** 🔴 | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **S1-D4 — `DzOptionsState`'s status. Prepared, not taken.** An unexported internal with no manifest entry, no metadata record, no docs page and no contract, injecting `options-state`/`options-message`/`options… | (a) inline it into the seven · (b) `compound-part` with a `parentComponent` — *which of seven?*; the field is singular · (c) new ownership `kind` for a shared internal · (d) **each of the seven hosts declares… | **taken — owner directive, option (d)** (2026-09-04, TASK-R5-O2 session 3). Taken by the… |
| **D18** 🟠 | [`TASK-R5-O2`](./TASK-R5-O2-handoff.md) | **Ratify the composition rule.** Three clauses — *parent-covers* (a compound part emits a vocabulary name, never `root`), *boundary-stops* (a nested `data-part="root"` is another component's root), *wrapper-co… | (a) TASK-R5-O6 builds the gate from the rule as written · (b) ratify it into ADR-19 §3 first, then gate · (c) leave it a spec-level convention — **rec. (b) then (a)**: the rule tells a consumer which element c… | **partially settled (TASK-R5-O6)** — the NEGATIVE clause is now gated repo-wide: `AS_CHIL… |
| **D24** 🟠 | [`TASK-R5-O2 session 3`](./TASK-R5-O2-handoff.md) | **Six components where `class` does not reach the root, now written down and published.** On `DzSlider`, `DzRangeSlider`, `DzRating`, `DzDatePicker`, `DzDateRangePicker` and `DzMention`, `$attrs.class` lands o… | (a) ship as declared: the contract records where `class` lands, `ui.root` reaches the wrapper, nothing moves · (b) re-point `class` at the root in all six, as a breaking layout change in the minor position · (… | **SETTLED (TASK-R5-O6), option (c) — and (a) preserved exactly: no component’s rendering… |
| **D26** 🟠 | [`TASK-R5-O2 session 3`](./TASK-R5-O2-handoff.md) | **A pure wrapper has to declare every part as `optional`, and that is a schema gap.** `DzPersonaSelector` renders no element of its own — its root *is* a `DzCombobox` — so by the *wrapper-covers-union* clause… | (a) ship as-is — "every part optional" is the idiom for a pure wrapper and the doc comment explains it · (b) add `delegatesTo: 'DzCombobox'` to `ComponentAnatomy` and teach the validator to credit the delegate… | **partially settled (TASK-R5-O6), option (b) first half.** `ComponentFallthrough.delegate… |
| **D27** 🟠 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **The defaults slice is 4 of an enumerated 23, and the remaining 19 are work, not a sweep.** 23 public components carry both canonical axes (`size` + `tone`); only `DzButton` resolved them at `99b963a`, and TA… | (a) finish all 19 in a follow-up packet, family by family, running each family's suite as the gate · (b) adopt opportunistically, whenever a component is edited for another reason · (c) stop at 4 and re-scope… | **TAKEN — (a), executed 2026-09-15.** 18 of the 19 adopted family by family, each family'… |
| **D32** 🟠 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **D27's premise that "the existing per-component suites are what catch it" is FALSE for most of the 19.** Measured before editing: `DzFab.spec.ts` and `DzSplitButton.spec.ts` carry **zero** assertions on `data… | (a) keep the central table and make a row **mandatory**, wired as a `validate:*` cross-check that every `useDzDefaults` consumer in `component-meta.json` has one — **rec.**: the only option with a forcing func… | **taken (a), under delegation, 2026-09-17 → D79** |
| **D33** 🟢 | [`TASK-R5-O3`](./TASK-R5-O3-handoff.md) | **The real residual for ADR-20 §6 is 79 components, not 19, and it should be generated rather than hand-listed.** From the regenerated `component-meta.json`: **101** components declare at least one canonical a… | (a) re-scope D20-4's closing condition to "every component with a canonical axis resolves through `useDzDefaults`, or its exclusion is argued at the code", and track the 79 as a **generated** ratchet the way `… | **taken (a), under delegation, 2026-09-17 → D80** |
| **D46** 🟢 | [`TASK-R5-O7`](./TASK-R5-O7-handoff.md) | **High-contrast ships as a separate opt-in stylesheet**, `dist/tokens.high-contrast.css` + subpath `./css/high-contrast`, rather than folded into `tokens.css` | (a) fold into `tokens.css` — costs every consumer 115 declarations for a theme no component implements yet, and moves the 674 that `token-references.spec.ts` pins · (b) **separate opt-in sheet — taken**; `toke… | taken |
| **D47** 🟠 | [`TASK-R5-O7`](./TASK-R5-O7-handoff.md) | **High-contrast is deliberately absent from `tokens.dtcg.json`: DTCG 2025.10 cannot express a CSS system colour.** Measured against the vendored official schema — `$type` is an enum of thirteen and `color` req… | (a) **omit the group — taken** · (b) emit a fabricated oklch/sRGB approximation — puts a value in the interchange file the library does not ship · (c) a non-standard `$type` — fails the schema gate this task a… | taken |
| **D56** 🟢 | [`TASK-R5-O8`](./TASK-R5-O8-handoff.md) | **Five slot-interface members were declared required and are optional in fact.** `DzAccordionSlots.default` and the four `DzCard*Slots` members were `name: () => VNode[]`, not `name?:`. Nothing enforced it unt… | (a) **make them optional — taken**, a loosening that matches shipped behaviour and the convention every other `Dz…Slots` interface already follows · (b) keep them required and change the specs — would make a l… | taken |
| **D58** 🟠 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **R1-O2's i18n-export slice done by R5-O4; the catalog ships as JSON data, not a named JS export.** A runtime `enMessages`/`formatMessage` export from `./i18n` adds `unclassified` ownership entries (ceiling 29… | (a) **JSON data + composable/type subpath — taken** · (b) add an ownership kind (schema 1.2.0) and export values · (c) raise the ceiling — rec. (a) | taken — agent choice under owner delegation 2026-09-17 (reversible: add a named export on… |
| **D59** 🟠 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **First locale pack: `de` scaffolded, 0/110 translated, unpublished** — never machine-translated. `bs` would exercise the `few` plural category and has contributor availability in the owner's team | (a) `de` (task default) · (b) `fr` · (c) `bs` — rec. owner names locale + human translator | taken — agent choice under owner delegation 2026-09-17 (reversible: delete `de.json`, `--… |
| **D60** 🟢 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **"Interpolation escapes by default" read as: values are data, output is text, HTML sinks go through the sanitizer seam.** HTML-entity-escaping values by default would render `&lt;` literally through Vue's esc… | (a) **text + seam — taken** · (b) entity-escape values by default — rec. (a) | taken — agent choice under owner delegation 2026-09-17 (reversible: add an `escape` optio… |
| **D61** 🟢 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **`DzPlainDate`/`DzPlainTime` are `string` aliases.** Narrowing today's `string` date props to template-literal types rejects every consumer `string` variable — a `minor` | (a) **aliases + behavioural spec — taken** · (b) narrow props in a minor — rec. (a) until 1.0 | taken — agent choice under owner delegation 2026-09-17 (reversible) |
| **D62** 🟠 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **APP-1's RTL arrow-key fix was inert on every landing route**: the Direction control wrote `<html dir>` only, so `useDzDirection()` said `ltr` (e2e red before, green after). Fixed at the shared shell: `App.vu… | (a) **shell `DzProvider` — taken** · (b) Core `useDzDirection` falls back to `<html dir>` (ADR-20 amendment, SSR mismatch) · (c) a shared arrow-key composable in Core | taken — agent choice under owner delegation 2026-09-17 (reversible: remove the wrapper) |
| **D64** 🟢 | [`TASK-R5-O4`](./TASK-R5-O4-handoff.md) | **8 visual fixture baselines (CJK, combining marks, 4,096-char run, +40 % pseudo) accepted by the agent**, win32, 3 byte-identical probes each. They record a **finding**: button labels neither wrap nor truncat… | (a) keep as current-behaviour evidence — taken · (b) owner re-accepts; design decides wrap vs truncate | taken — agent choice under owner delegation 2026-09-17 (reversible: remove 8 ledger entri… |
| **D67** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **DzGrid span API** — sheet `reports/TASK-R3-O3-decisions.md` | (a) `data-span` attributes read by grid-level selectors · (b) `DzGridItem` compound part with typed `span` — **rec. (b)** | **taken — agent choice under owner delegation 2026-09-17 (reversible: delete `DzGridItem… |
| **D68** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **`utility` ownership kind** (FORM-OSS-03 Q1) | (a) schema 1.2.0 adds `utility` + `injection-key` (unclassified 29 → 0) · (b) codecs as `contract` · (c) leave — **rec. (a)** | **taken as recommendation — agent choice under owner delegation 2026-09-17; NOT executed*… |
| **D69** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **`time` profile has no offset** (FORM-OSS-03 Q4). Finding: Pro `dz.date` matches `format: 'time'` and passes an offset-less string through | (a) control stays wall-clock, codec owns zone · (b) `timeZone` prop on `DzTimePicker` · (c) leave — **rec. (a)** | **taken — agent choice under owner delegation 2026-09-17 (reversible: no Core change made… |
| **D70** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **C9 `future` → `n-a` for `DzCheckboxGroup`, `DzRadioGroup`, `DzTagsInput`** — they own no option source; the seam exists | (a) reviewed `n-a` (probe re-derives if the seam is ever declared) · (b) build option sources · (c) keep `future` — **rec. (a)** | **taken — agent choice under owner delegation 2026-09-17 (reversible: restore the three r… |
| **D71** 🟢 | [`TASK-R3-O3`](./TASK-R3-O3-handoff.md) | **DzMention resolver after the seam** — private loader deleted; resolver runs against the seam signal | (a) keep resolver as adapter · (b) also deprecate it · (c) remove (breaking) — **rec. (a) now, (b) later** | **(a) taken — agent choice under owner delegation 2026-09-17 (reversible: restore `resolv… |
| **D73** 🟢 | [`TASK-R3-O4`](./TASK-R3-O4-handoff.md) | **A fifth corpus outcome, `admitted`**: the value reaches the sink live because a named policy admits it (Pro's `unicode-homoglyph-url`). Same > 80-char rationale rule as `inert`, and the rationale must name t… | (a) add `admitted` · (b) record as `stripped` (false: nothing removed) · (c) drop the payload (the policy loses its fixture) — rec. (a) | **taken** by session 1 (agent; reversible until first publication); recorded in the ledge… |
| **D74** 🟢 | [`TASK-R3-O4`](./TASK-R3-O4-handoff.md) | **Sinks `markdown` and `mermaid-svg`**, spelled as the sanitizer seam's `DzSanitizeSink` contexts, rather than folding Markdown/Mermaid source into `html`. No OSS fixture uses them yet (Core has 0 HTML sinks)… | (a) separate sinks · (b) fold into `html` (Markdown in a raw-HTML sink proves nothing) — rec. (a) | **taken** by session 1 |
| **D75** 🟢 | [`TASK-R3-O4`](./TASK-R3-O4-handoff.md) | **Pro's `mustNotSurvive` and file-level `policy` go under `extensions["com.dzup.pro"]`**, not into the shared format. Unknown top-level keys are rejected | (a) namespaced extension · (b) shared optional `mustNotSurvive` (OSS measures structure, not tokens, so it could never assert it) — rec. (a) | **joint**: OSS half taken; Pro TASK-R2-P9 confirms the namespace |
| **D76** 🟢 | [`TASK-R3-O4`](./TASK-R3-O4-handoff.md) | **The executable incompatible-peer example uses a required peer** (`vue` 2.7.16 vs `^3.5.0`) because OSS declares no optional peer. The `optional` flag is exercised synthetically (`pdfjs-dist`), and Pro instan… | (a) required peer · (b) add an optional peer to an OSS package just for the example — rec. (a) | **taken** by session 1 |
| **D79** 🟠✅ | [`TASK-R5-O3 (continuation 2026-09-17)`](./TASK-R5-O3-handoff.md) | **D32 TAKEN as (a), under delegation — a raw-binding audit row is mandatory for every `useDzDefaults` consumer, and gated.** `yarn validate:provider-defaults` (link 29 of 43, straight after `validate:component… | taken: (a). Reversible: remove the link, the validator and its ceilings file | **taken** |
| **D80** 🟢✅ | [`TASK-R5-O3 (continuation 2026-09-17)`](./TASK-R5-O3-handoff.md) | **D33 TAKEN as (a), under delegation — the ADR-20 §6 residual is a generated, downward-only ratchet with argued exclusions.** Canonical axis = a `size`/`tone`/`variant` prop whose type minus `undefined` is non… | taken: (a). Residual choice for the owner: (i) keep reasons in data only, gated in one place · (ii) also mirror each at the component when the adoption tranche next edits it — **rec. (ii)**: cheap then, and th… | **taken** (ii open) |
| **D85** 🟠 | `\` | **tsdown: recommended refusal.** Every release from **0.22.0** declares `engines.node ^22.18.0 \ | >=24.x` (ADR-18 forbids the floor change, and `tsdown` is not in `validate:engines`' dependency list, so the gate would stay green while the floor broke). The newest floor-compatible release is **0.21.10 (2026… | >=26.0.0`, i.e. *worse*; 0.21.10 is still the last compatible release and has no backport… |
| **D117** 🟠 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | TAKEN by the owner 2026-09-19 — option (a), steppers revealed on the handle, no opt-out prop. **The SC 2.5.7 resize affordance — the design pick this task exists to hand over.** Three surfaces (`DzResizable`… | (a) **steppers revealed on the handle** — discoverable, viable on a table column, reuses `DzNumberInput`'s and `DzOrderList`'s vocabulary, resting state unchanged, ~1.5 days · (b) tap-to-place on the gutter —… | **DECIDED — owner, 2026-09-19: option (a).** Implemented the same day by TASK-R2-O5's res… |
| **D118** 🟠 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | TAKEN by the owner 2026-09-19 — option (a), `browserslist` in the six published packages. **Adopt a supported-browser floor, or keep publishing none?** N2-D2's D2-D1, now with the engineering done: `/evidence/… | (a) **declare `browserslist` in the published packages** (`@dzup-ui/core`, `tokens`, `contracts`, `mcp`, `nuxt`, `testing`) — the channel PostCSS, Autoprefixer, esbuild and the Baseline tooling all read; the p… | **DECIDED — owner, 2026-09-19: option (a).** A `browserslist` key naming one range is dec… |
| **D121** 🟠 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | REVIEWED by the owner 2026-09-19 — accept all six. **The six N1-O3 geometry changes — accept, revert or adjust, per item.** Re-measured at `2d51eec` and photographed for the first time (11 before/after pairs… | (a) **accept all six — rec.**: V2/V3/V8 *are* the SC 2.5.8 fix, V5/V6 restore designs the build was silently discarding, and V1's alternative (the calendar glyph jumping to the start of the field) is the more… | **DECIDED — owner, 2026-09-19: option (a), accept all six.** Recorded per item in the rev… |
| **D123** 🟢 | [`TASK-R2-O5`](./TASK-R2-O5-handoff.md) | **The widened matrix sweep exits 1 for a teardown fault.** Running all six new projects in one invocation ended with `Error: worker-5 process did not exit within 300000ms after stop, force-killed it`, **after*… | (a) **run the lane one engine at a time in CI** — which is also what N1-O3's own reproduction note already instructs, for a different reason (`--strictPort` collides and CPU contention perturbs `reduced-motion… | (a) **run the lane one engine at a time in CI** — which is also what N1-O3's own reproduc… |
| **D137** 🟠 | [`TASK-R2-O6`](./TASK-R2-O6-handoff.md) | **TASK-R2-O6's `<done_check>` clause 4 can never pass, for two independent reasons.** It expects `validate:capability-matrix` output to show **visual cells `pass`/`fail`** for Tier B+. (i) **There are no visua… | (a) rewrite the clause as *"…shows `covered`/`stale` for Tier B+"*, which is checkable today, and invoke by module path not `npx` · (b) take O6-D5 and make it a real cell, then the original clause becomes mean… | (ii) closed, (i) open |
| **D138** 🟢 | [`TASK-R2-O6`](./TASK-R2-O6-handoff.md) | **The capability matrix over-reported visual coverage by 50 % on all 144 generated docs pages.** `visualInputNote()` in `generate-capability-matrix.ts` built its component count from `new Set(ledger.baselines… | closed in the working tree; needs the owner's commit like every other generated artifact in this tree | closed (uncommitted) |

---

## 7. Contradictions and collisions — flagged, not resolved

The task's `<stop_conditions>` say: *"Stop and report when two ledgers record
contradictory states for the same decision (record both, flag it)."* Seven were
found. **None is resolved here.**

### 7.1 🔴 The `D<n>` id collision — the same token means three different things

This is the most consequential finding in the register, because it makes every
cross-report citation ambiguous.

| Namespace | Range | Example | Where it lives |
|---|---|---|---|
| **Defect register** | `D1`–`D11` | **`D8`** = *`useDualModel` silently ignores external writes after the first user edit, across 7 controls* | `docs/program-2026-09/EXECUTION-STATUS.md:78`, `N1-O1-story-dod-handoff.md:308`, `N1-O4-at-matrix-handoff.md:463`, `TASK-R2-O3-handoff.md:38` |
| **Owner-decision register (09-04, global)** | `D1`–`D178` | **`D8`** = *`validate:exports` does not catch an **undeclared** export* | `docs/program-2026-09-04/EXECUTION-STATUS.md` §"Owner decisions raised by this program" |
| **Per-report local sequences (09 programme)** | `D1`–`D9` per report, ~21 reports | **`D8`** = *this entire run is worktree-dirty* (N1-O2) · *two pre-existing `yarn test` failures* (N1-O3) · *`DEPRECATED_TOKENS` is hand-maintained* (N2-T1) | each handoff's own decisions section |

**Measured collision surface.** A scan of all 83 `.md` files under
`docs/program-2026-09{,-04}/` for table rows and headings whose first token is a
`D<n>` id found **13 distinct definition sites for `D1`, 12 for `D2`, 15 for
`D3`, 13 for `D4`, 11 for `D5`, 9 for `D6`, 9 for `D7`, 7 for `D8`, 7 for `D9`,
5 for `D10`, 4 for `D11`** — 105 definition sites across eleven ids.

**The three worst live collisions**, because both meanings are cited in the
*same programme* and neither is qualified:

| Id | Meaning A (defect) | Meaning B (decision) |
|---|---|---|
| **`D9`** | `DzCombobox`'s clear button ignores `disabled` — `TASK-R2-O3-handoff.md:46` | *the second ordering statement in `tokens.css`: keep it, or make `base.css` the only one?* — `TASK-R5-O1-handoff.md:376` |
| **`D10`** | `DzTreeSelect` runs two focus mechanisms simultaneously — `TASK-R2-O3-handoff.md:47` | *`ariaInvalid`: when does the removal from `BaseAccessibilityProps` happen?* — `TASK-R5-O1-handoff.md:399` |
| **`D11`** | `DzDropdownMenu` has a dangling `aria-controls` — `TASK-R2-O3-handoff.md:48` | *`maxUndeclaredStates` starts at 1, not 0. Close it, or ratchet it?* — `TASK-R5-O1-handoff.md:432` |

README §6 compounds it: *"R2-O3 defect register close-out (**D8** controlled/uncontrolled first)"*
is the **defect** `D8`, while `EXECUTION-STATUS.md`'s decision table row `D8` is
the `validate:exports` gap. A reader following the README to the decision table
lands on the wrong item.

**It is already causing mis-citation inside the ledger.** `EXECUTION-STATUS.md`
rows `D98` and `D99` open with `**D4 --` and `**D10 --` respectively: decision
`D98` is *about* defect `D4`, and decision `D99` is *about* defect `D10`. The
row is correct; the notation is indistinguishable from a duplicate id.

**Recommendation (a naming decision, therefore the owner's):** prefix the
namespaces — `DEF-8` for defects, `OD-8` for owner decisions — or at minimum
qualify every cross-report citation as the 09 programme already does for
`N5-01 D1`, `S1-D4`, `A4-D3`, `D1-D2`, `D2-D4`, `D3-D1`. **Do not renumber**:
178 decisions and 28 handoffs cite these ids, and renumbering breaks every
citation to buy tidiness. The `D43`/`D44` gap in the global sequence (never
issued — no reference exists in any file) should be left as a gap for the same
reason.

### 7.2 🟠 `A4-D2` and `D166` are the same decision, raised twice, both open

Both ask whether to register `dzup-ui.com` or change `SITE_ORIGIN`; both measure
NXDOMAIN; both say decide before the first publish. `D166` is the 2026-09-21
re-measurement at `527dbd1` and adds the file-count cost (3 registry indexes,
87 markdown mirrors, both `llms*.txt`, `sitemap.xml`, every canonical link,
282 `/r/**` files). **Not a contradiction — a duplicate.** Recorded as one
decision with two ids so the older citation still resolves.

### 7.3 🔴 The Node floor — three reports, two answers, and an ADR that is wrong either way

| Report | Answer | Argument |
|---|---|---|
| `N5-04-peer-hygiene-handoff.md` §3 (**N5-04 D3**, 2026-09-03) | **`>=22.13.0`** | ADR-18 already calls it "defensible today"; Node 20 left maintenance in April 2026 |
| `TASK-R1-O4-handoff.md` §9 (**D160**, 2026-09-21) | **keep `^20.19.0 \|\| >=22.13.0`**, fix the one `globSync` import | one import in one spec; `^20.19.0` is what every consumer's `engines` check reads |
| `TASK-R1-O6-handoff.md` §9, §7.3 (**D176**, 2026-09-21) | **`>=22.13.0`**, with D160's fix as a legitimate interim | **explicitly differs from D160** and says so in its own §7.3 |

**Both R1-O4 and R1-O6 landed on 2026-09-21, at the same commit, and neither is
stale.** D176 also rejects `>=24.0.0` on the ground that Node 22 LTS runs to
April 2027.

**The compounding fact, and it is not optional either way:** D176 states that
**`getTextInfo()` needs Node 24, not 22.13**, which makes **ADR-20 §4 wrong as
written** — and N5-04 D3 independently records the same thing (*"that needs
Node ≥ 24.0.0 (F6)"*). So whichever floor is chosen, **ADR-20 §4 and its
"Alternatives considered" section must be amended in the same change**, or the
repository ships two documents promising a delegation the floor still cannot
perform. That amendment is **TASK-R0-O2's** to execute, and it is currently not
in R0-O2's scope as written.

### 7.4 🟠 `D175` re-levels `N5-04 D2`'s answer, and says so

`N5-04 D2` recommended the `lucide-vue-next` → `@lucide/vue` swap as
*"mechanical, separately reviewable"* with **no public API change** — i.e. a
`patch`. `D175` re-levels it to **`minor`** on three measured consumer-visible
changes: every icon's rendered `class`, a new default `aria-hidden="true"`, and
three different drawings. Under `VERSIONING.md` §1, `minor` **is** the breaking
position for a 0.x package.

**Both records stand in the register.** D175's argument is the later and the
measured one; N5-04 D2's level was an aside to a different question. Recorded,
not resolved.

### 7.5 🟢 The changeset count is quoted at 16, 20 and 37; it is **37**

> **Corrected 2026-09-22.** This subsection said *"it is **36**"* and listed
> **37** among the wrong quotes. **37 is correct.** TASK-R0-O2 added the 37th
> changeset 45 minutes after this register was compiled, so the register brands
> the current truth as an error. Downgraded 🟠 → 🟢 because only two of the
> three quotes are now wrong, and neither is load-bearing on a gate.
> (`release-exit-verification-2026-09-22.md` §C1, finding **S4**.)

See §1.2. `N5-01` (16) was right on its date; `release-exit-tasks.md` (20) is
stale (**D184**); `TASK-R1-O1-handoff.md` §7 (37) reached the right number the
wrong way — by counting `.changeset/README.md` against 36 real changesets — and
is now right on the merits as well, there being 37 real ones.
`yarn validate:release-policy` says **37** and is the authority, because it is
the gate that reads them and it reads the working tree, which is what
`changeset version` will act on.

### 7.6 🟢 `N5-01` says both 10 and 11 over-declared changesets, and both are right

§6's outcome line says **10 over-declared**; §10's `D3` row says **the 11
over-declared**. The reconciliation is in §6's own table: 10 changesets are
wholly over-declared and an 11th (`selection-controls-can-be-driven-by-a-remote-option-source`)
is correct for `@dzup-ui/core` and over-declared for `@dzup-ui/contracts`.
`release-exit-tasks.md`'s `_Gap:_` quotes the 10. **Not a contradiction, a
precision difference** — but a re-levelling packet must touch 11 files, not 10.

### 7.7 🟠 "Three registries ship 282 files that resolve nothing" — the 282 is right, the shape is worth stating

Measured at `527dbd1`: `git ls-files apps/landing/public/r/` → **282**, and
`find apps/landing/public/r -type f` → **282**, with one file modified in the
working tree. So the artifacts exist, are tracked, and are complete. What does
not exist is the origin they point at (§7.2) and the packages their install
commands name (**A4-D1**). **The registry files are not stale; they are
correct descriptions of a thing that has not been published.**
