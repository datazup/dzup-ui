services:
  web:
    image: nginx:1.27-alpine
    restart: unless-stopped
    expose:
      - "8080"
    labels:
      - traefik.enable=true
      - traefik.http.routers.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.rule=Host(`${COOLIFY_DOMAIN:-dzup-ui-static.invalid}`)
      - traefik.http.routers.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.entrypoints=https
      - traefik.http.routers.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.tls=true
      - traefik.http.routers.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.tls.certresolver=letsencrypt
      - traefik.http.routers.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.service=${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web
      - traefik.http.services.${COOLIFY_RESOURCE_NAME:-dzup-ui-static}-web.loadbalancer.server.port=8080
      - traefik.docker.network=coolify
    networks:
      - coolify
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  coolify:
    external: true
